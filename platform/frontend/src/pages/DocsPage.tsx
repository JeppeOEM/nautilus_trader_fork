// Re-exports the real implementation from ./docs/ -- kept alongside its supporting data/CSS
// files (content.ts, kbData.ts, diagram.ts, TrustedHtml.tsx, docs.css) rather than flattened
// into this one file (Story 15.1 Task 4).
export { default } from "./docs/DocsPage";
