# Data Integrity Audit — wrong-data dangers, old and new

Audit date: 2026-09-19; revised 2026-09-20 (Epic 22 moved the collector paths to
`collector_core/`; minute rollups retired, D-35). Scope: everything between the venues and
a pixel on the chart — collector ingestion, the Parquet catalog, the candle store,
`data_api`, the frontend. Governing rules:
`platform/CLAUDE.md` DATA-01…DATA-06. Every entry states the mechanism, the evidence, the
treatment, and what is left open. "Open" is never rounded up to "fixed" (DATA-02).

## 1. Analysis of the triggering incident (fake spike candles)

**Symptom.** Candles with an ~$850 high–low range and ~21 BTC volume in a single second,
recurring every 15–40 minutes, with identical high (81694) and low (80841).

**Mechanism (proven).** dYdX's `v4_trades` `subscribed` reply carries up to 1000
*historical* trades. Verified against the live indexer on 2026-09-19: 1000 trades, oldest
17 h old, 26.27 BTC total. The Rust adapter (`parse_trade_ticks`) emits each as an ordinary
`TradeTick`; `collector._process_data` folded all of them into the current second's
OHLC/volume. Every (re)subscribe therefore stamped hours of price range and volume onto one
second, and that row flowed into 1s snapshots → the candle store → every candle timeframe (minute rollups at the time; retired in D-35).

**Why it recurred.** The collector was being OOM-killed (`docker events`: ~10 `oom`/exit-137
events between 09:23 and 13:32), and each restart re-subscribed. Each spike landed ~6 min
after a restart (10:34→10:40, 11:26→11:32, 12:31→12:37, 12:51→12:57, 13:07→13:13).

**Independent cross-check.** The raw dYdX websocket, read with a client sharing no code with
our pipeline (`websockets` → `wss://indexer.dydx.trade/v4/ws`), reproduced the 1000-trade
replay. Not inferred from code.

**Conclusion.** Root cause identified and fixed at ingest (§2, D-01, D-02). Historical rows
remain until `repair_catalog` is run (§2, D-03). One thing is *not* proven: the exact delay
of ~6 min between restart and spike row (the replay arrives at subscribe; the first row
carrying it is emitted later). It does not affect the fix (the filter acts on trade age, not
arrival time) but is left labelled open.

## 2. Register of dangers

Status: **FIXED** (code + test), **GUARDED** (canary/detection, cause outside our control),
**OPEN** (known, not yet treated), **DOCUMENTED** (accepted, with detect/repair path).

### Ingestion

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-01 | Subscribe-time trade history counted as live | Above. Proven vs raw WS | `config.stale_trade_seconds` (10 s) filter in `_process_data`; drops counted and logged each flush | FIXED — no flagged row written after 2026-09-19 16:05 UTC in 25 h of local collection (scan below, D-03). VPS deploy not re-verified since `make redeploy-all` |
| D-02 | Replayed trades within the age window double-counted (short reconnect) | Reconnect resubscribes → replay of trades already counted, still < 10 s old for a brief outage | Bounded per-instrument `trade_id` dedup (`config.seen_trade_ids`, 2000); duplicates counted, logged at WARNING | FIXED (same scan evidence as D-01) |
| D-03 | Old spike rows already in the catalog | Rows written by the pre-fix collector. **Report-only scan, 2026-09-20, local catalog: BTC 4, ETH 5, SOL 6 flagged seconds, newest 2026-09-19 16:05 UTC — none after the fix. The other 26 instruments are unscanned** | `python -m collector_core.repair_catalog --catalog …` (report-only by default; `--apply` clears the trade fields of each flagged second, and `--candles-db` rebuilds the candle-store days they touch). Back up `catalog/` first — it rewrites Parquet | OPEN on the local catalog. **Moot on the VPS**: that catalog was created 2026-09-19 17:53, after the fix (D-33) |
| D-04 | Any future bug that writes impossible prices | Class of failure D-01 belonged to | `integrity.ohlc_outside_book`: a second's trade high/low must lie inside that second's own top-20 book range (±0.1%). Live: logged at ERROR in `_second_loop`. Offline: the same function drives D-03's scan | GUARDED |
| D-05 | BONK-USD-PERP book unparseable | 505 `Failed to parse orderbook deltas for BONK-USD … Raw value … exceeds QUANTITY_RAW_MAX` in 30 min. Sizes overflow the Rust `Quantity` in this build. Every BONK book message is dropped, so BONK has no valid book: no snapshots (the second loop skips it), candles/rankings absent or stale, and constant log/IO churn (ws raw-debug log rotates every ~20 s). Cannot be fixed here: `crates/` is off limits (FORK-01) | Unsubscribe BONK / add it to `exclude` via `collector:control` (runtime config lives on the VPS, not the repo). Any other coin with huge size scaling will fail the same way | OPEN — needs the control action on the VPS |
| D-06 | Collector OOM-killed every 15–40 min → unflushed 60 s buffer lost, gaps (BTC: 1459 of 3600 expected snapshots in an hour) | `docker events`, exit 137; host 3.8 GB with 2.9 GB used, no swap, no `mem_limit`; orphan `dydx-dashboard` container (585 MB) still running | Order-book deltas of instruments outside `_delta_store` are no longer buffered for the 60 s flush interval (they were buffered then discarded). Swap file + removing the orphan dashboard are host actions | PARTLY FIXED — effect of the buffer fix unmeasured; host actions pending |
| D-07 | Unbounded `_ingest_queue` under sustained load (collector at ~108 % CPU) | Same shape as the documented `DataEngine` OOM | None yet. Needs a queue-depth metric first; do not add a drop policy blind (DATA-05) | OPEN |
| D-08 | Crossed dYdX book | Architectural (DATA-04) | Per-level message-id uncrossing already in place; resync only as fallback | GUARDED (pre-existing) |
| D-09 | Stale book stamped as live | DATA-01 | `config.stale_book_seconds`, accumulators discarded on a skipped second | GUARDED (pre-existing) |
| D-10 | Detection loop itself stalls silently | DATA-02 | `second_loop` wall-clock lag canary; four `second_loop_lag` incidents today (12:22, 12:26, 12:50, 13:27) are a symptom of D-06/D-07 | GUARDED; incidents are open evidence for D-06 |
| D-11 | Open interest dropped by the Rust bindings | Known | Polled separately over REST | GUARDED (pre-existing) |
| D-12 | Mark/index price precision labels vary per tick | Known incident | `_at_fixed_precision()` | FIXED (pre-existing) |
| D-13 | `Price(decimal, precision)` returns wrong values (NAUT-01) | Known bug | Only `scaleb()` + `from_raw()` | FIXED (pre-existing) |

### Storage / derived data

| ID | Danger | Treatment | Status |
|----|--------|-----------|--------|
| D-14 | Restart loses the minute in progress; first minute of a mid-minute start is not emitted | Idempotent `backfill_minute_rollup` (DATA-05) | SUPERSEDED (D-35): rollups retired; the store has no minute-close seam |
| D-15 | Rollup `partial_start` does not cover gap-created partial minutes | Epic-17 review ledger. With restarts every ~20 min this is hit often, so wide candles can be understated at gaps | MITIGATED (Story 21.1/21.2): rollup-sourced candles carry `partial: true` when observed < 90% of their span (`ml_signals.candles.PARTIAL_OBSERVED_FRACTION`). Not a fix: the missing seconds stay missing; the flag is on the wire only, the frontend does not draw it differently (ANSI 16-colour palette) |
| D-16 | A rollup inherits any bad raw row | `repair_catalog` regenerates affected minutes | SUPERSEDED (D-35): `repair_catalog --candles-db` rebuilds the affected days in the store |
| D-17 | Non-atomic `screener_columns.toml` write; technicals params unvalidated | Epic-17 ledger | OPEN (low; not market data) |

### Read path / presentation

| ID | Danger | Treatment | Status |
|----|--------|-----------|--------|
| D-18 | Live forming bar overwrote the history bar with a partial open/high/low/**volume** (bucket start unseen by `LiveCandleBus`) → jumping candles | Story 21.3: `LiveCandleBus.seed()` fills the current bucket from the catalog on subscribe (buckets ≤ 1 h; wider stay partial), the client-side merge is deleted, and live volume now drives the volume pane | FIXED in code + tests (undeployed) |
| D-19 | Chart shows only trade-seconds; thin coins show sparse candles | By design (no fabricated values, DATA-01); gaps drawn as whitespace | DOCUMENTED |
| D-20 | `has_more=False` on an empty page truncates scroll-back on sparse coins | An in-progress, uncommitted rewrite of `routes/candles.py` (file-range paging) is addressing this; not verified by me | OPEN pending that work |
| D-21 | Chart load latency (7–9 s at 1m, >45 s at 15m on the VPS) | **Local measurement (960 MB catalog, BTC, `scripts/bench_candles.py`): 1m 1.14 s → 0.40 s, 15m 1.78 s → 0.66 s, 1h 1.63 s → 0.64 s, 4h 5.9 s → 3.0 s.** Cause: 95% of a request was the catalog decoder deserialising 20-level books nobody reads; candles now read 7 columns straight from the Parquet files (`query_second_ohlc`). VPS not measured — OPEN until measured there. Remaining: 4h+ falls back to raw 1s before the first rollup; original contributors: | Contributors: host oversubscribed (D-06), a second full catalog scan per page for `has_more`, per-request `ParquetDataCatalog`. Paging rewrite in progress | OPEN — not measured on the VPS in this pass |
| D-22 | Live-candle path: out-of-order/duplicate snapshots, unbounded queues, unbounded subscriptions, non-numeric bar guard | Epic-15.5 review ledger | OPEN (low) |
| D-23 | `NaN`/`Infinity` metric reaches the History chart | Epic-17.2 ledger | OPEN (defensive; not observed) |

### Found during Story 21.5 (new)

| ID | Danger | Evidence / treatment | Status |
|----|--------|----------------------|--------|
| D-24 | **Root cause found; migration written (Story 21.6).** `second_snapshot` Parquet files come in two schemas: 11 columns (no `open/high/low/close_price`) up to 2026-09-10, 15 columns from 2026-09-16. `ParquetDataCatalog.query()` builds `pds.dataset(file_list)` (nautilus `persistence/catalog/parquet.py:2064`), which takes its schema **from the first file** — the oldest. Any query whose file set includes an old-schema file therefore silently drops the OHLC columns of every new-schema file in it: all trade seconds come back with `None` OHLC. Reproduced with plain pyarrow (`[old,new]` → no `open_price` column; `[new,old]` → has it) and by window bisection on BTC: windows centred on 2026-09-16 lose 0 rows at ≤ 7 d and 284 at 14 d and 30 d (the 14 d window is the first to reach a pre-09-10 file). All 29 instruments have both schemas; no other data type has mixed schemas. | Candles now use `query_second_ohlc` (per-file read, immune). Other consumers read only book/volume columns present in both schemas, or read day-sized chunks that never span the 09-10→09-16 boundary (`repair_catalog`), so they are unaffected today — but any *future* reader of OHLC over a range crossing that boundary via the catalog will silently lose it. Durable fix: `python -m dydx_collector.normalize_snapshot_schema --catalog … [--backup-dir … --apply]` rewrites the old files with the four OHLC columns as nulls (backup required, atomic replace, idempotent, refuses unknown columns); tested by reproducing the bug on a mixed-schema fixture. Local catalog: 22,143 files need it (report-only run; NOT applied). | FIX WRITTEN; OPEN until run on the production catalog after a backup |
| D-25 | Because D-24 hid some rows, the new candle read also shows some historical spike seconds (D-01) that the old path hid by accident. They are the same rows `repair_catalog` targets | Run `repair_catalog` (D-03) | OPEN on the local catalog until D-03 is run; moot on the VPS (D-33) |

### Story 21.6 — no hidden errors (DATA-07)

| ID | Danger | Treatment | Status |
|----|--------|-----------|--------|
| D-26 | Sites that carried on past a failure with only a log line (collector enqueue/process/flush-write/rollup/OI-poll/status/control/quarantine, live-candle decode, ranking snapshot entry + price backfill, metrics snapshot, technicals per coin, catalog reads of unreadable data types) | All now `error_ledger.record(...)`: ERROR + traceback + counted, exposed at `GET /api/errors`, shown by the frontend `ErrorBar` | FIXED (undeployed) |
| D-27 | An unparseable venue `volume24H` silently became **0** (a coin reclassified illiquid / zero-volume) | Coin left unclassified/absent + ledger error; no fabricated 0 | FIXED |
| D-28 | An impossible candle was dropped (or would have been drawn) | `/api/candles` returns HTTP 500 + ledger error; the frontend shows the failed load and the bar | FIXED |
| D-29 | One coin's failed technicals read was returned as `{}` ("no data") and cached 90 s | Response carries `errors{iid}`; failures are never cached | FIXED |
| D-30 | Collector-side ledger counts live in the collector process only (its errors are in Dozzle at ERROR, not in `/api/errors`) | — | OPEN: needs the collector to publish its counts (e.g. in `collector:status`) |
| D-06 | OOM kills | Explicitly excluded from this story | OPEN |

### Found 2026-09-19 — chart "missing bars / missing volume" deep dive

Method: `/api/candles` on the VPS (tunnel) and on the local `frontend-dev` stack, compared
minute-by-minute with dYdX's indexer candles (independent source), plus a CDP read of the
React chart's own props to prove the frontend holds what the API served.

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-31 | **A trade near a minute boundary lands in the next minute** — 1m+ candles are wrong at the boundary, not just 1s. | Trades are accumulated into the *receive* second (`_process_data` → `_second_loop` pops per tick); dYdX delivers each trade 1–3 s after its `createdAt`. VPS vs indexer, 2.5 h: BTC 19:59 ours v=0.0358 c=81351 / indexer v=0.0379 c=81353, and ours has a 20:00 candle (v=0.0021, c=81353) where the indexer has **0 trades**; SOL 18:49 (1 trade, 4 SOL) absent from ours, ours 18:50 = 49 vs 45; ETH 19:33/19:34 = 0.002/0.004 vs 0.003/0.003. Rate: ETH 3/138, BTC 2/36, SOL 3/33 minutes. The spec-21-x backlog called this "visible only at 1s" — disproved. | Attribute trades to their venue second: hold each second's book sample and emit the snapshot D s later (D ≈ 5, covers the measured lag) with the trades whose `ts_event` falls in that second; count late trades (> D) as a loud canary. Costs D s of live latency for every `snapshots:raw` consumer — operator decision **22.13:** fixed for trades by the nightly rebuild (`rebuild_seconds` buckets on `ts_event`; the live second stays arrival-timed and provisional). Live evidence: dYdX trades arrive 1.0-1.8 s after `ts_event` (200 s run), the rebuilt minutes then equal the indexer's klines. **22.12:** on Bybit/Hyperliquid the *live* row is exchange-timed too (`book_time_source = "venue"`: trades bucketed by `ts_event`, a trade arriving after its second closed is archived + counted `collector.late_trade` and placed by the rebuild); dYdX's live row stays arrival-timed (D-49) and is corrected by the rebuild only. | FIXED for trades in code (undeployed); CLOSES on the first verified day on the VPS |
| D-32 | **Trades during a WS outage are lost, by design.** | Same root as D-31. During an outage the book goes stale, snapshots are skipped and accumulators discarded (D-09); the resubscribe reply *does* carry the outage's trades (with venue timestamps) but D-01's 10 s filter drops them because a receive-stamped second cannot hold them honestly. Local 2026-09-19 20:16:49–20:17:30: all 29 books stale 41 s, reconnect replay dropped (1000/coin); BTC 20:16 ours 0.0002 vs indexer 0.0013 (4 trades). VPS reconnect 20:06:34: ETH 20:05 ours 0.001 vs 0.002. | D-31's venue-second attribution lets replayed outage trades be placed in their true seconds (book for those seconds stays absent — the snapshot would carry trades only, or the loss stays named). Until then: each reconnect = a known, logged loss (`Dropped subscribe-time trade history`) | OPEN |
| D-33 | **VPS catalog reset**: `data/catalog/data` on nifelheim was born 2026-09-19 17:53:17 (`stat %w`); every data type dir has that mtime; oldest ETH file 17:53:50. All VPS chart history before then is gone (`/api/candles` → `has_more:false` at 17:54). | No backup/tar/other copy on the host; no repo tool or Makefile target deletes the catalog; `.bash_history` (last flushed 17:00) shows only `make redeploy-all` + swap setup. Cause not established. Side effect: D-03 / D-24 / D-25 are moot on the VPS (no pre-fix rows, one schema) **if** the reset was deliberate. | Operator to confirm whether the wipe was intentional. If not: it is unrecoverable; treat as a DATA-05 incident and add a catalog backup (rsync/cron) before it matters again. **Backup target exists (Story 22.11):** `make backup-catalog` -- host `rclone sync` of `catalog/data` to `$RCLONE_REMOTE:$RCLONE_BUCKET` (from `platform/.env`, never committed), today's (UTC) files and `*.tmp` excluded, overwritten/deleted remote objects moved to a dated `catalog-replaced/` prefix (`--backup-dir`) so a wiped local catalog cannot erase the backup; refuses when unconfigured or the local catalog is missing or holds no closed-day file. **Not yet configured or scheduled on the VPS** (operator: rclone remote + `.env` + cron, README "Nightly maintenance"). | OPEN — needs operator answer; backup written, not scheduled |
| D-34 | Volume histogram "missing bars" / 1m bars "that are only a line" | Not loss. dYdX volume is tiny now (indexer 24h: ETH 2654 trades = 1.8/min, BTC 1.1/min, SOL 0.5/min; 135 of 200 BTC minutes had 0 trades), so a 1m bucket with one trade is a genuine o=h=l=c bar (ETH dojis: ours 25 vs indexer 24 over the same minutes). lightweight-charts draws a bar shorter than 1 px as a 1 px tick on the baseline (`PaneRendererHistogram`), so 0.002 ETH next to a 120 ETH bar is invisible. CDP read of the chart's props: volume bars == real candles (148/148 VPS, 240/240 local); last candle == API's last. | None needed for correctness. If wanted: log-scale the volume pane, or a per-bar min height (custom primitive) | DOCUMENTED |
| D-05 | (update) BONK book still unparseable | VPS 24 h: 4433 `Failed to parse orderbook deltas for BONK-USD … exceeds QUANTITY_RAW_MAX`; local 3 h: 2315. BONK `/api/candles` on the VPS: empty. | Unchanged: exclude BONK (and any coin whose level sizes exceed ~3.4e13 units) | OPEN |

### Found 2026-09-20 — candle store (Parquet was the read path)

| ID | Danger | Treatment | Status |
|----|--------|-----------|--------|
| D-35 | **Minute rollups retired.** Every candle read re-opened thousands of tiny Parquet files (~0.5 ms/file regardless of rows; one file per coin per flush), and the rollup did not help: same file count, 60x fewer rows. | Finished 1m..1D bars now live in SQLite (`candles_<venue>.db`, `ml_signals/candle_store.py`), fed from raw 1s by the collector at :02 past each 60 s flush and rebuilt with `python -m collector_core.build_candles`. `minute_rollup.py`, `backfill_minute_rollup.py`, the rollup read paths and `/catalog/candles` are deleted. **Existing rollup files are now unused: delete `<catalog>/data/custom_dydx_minute_rollup/` by hand when convenient (nothing reads it; the code never runs this).** | FIXED (undeployed) |
| D-36 | **Parquet file count grows without bound**: one file per coin per data type per 60 s flush, about coins x types x 1,440 per day (locally 155k files in ~19 days; a VPS running 24/7 would reach inode limits in weeks, and backup/rsync/rclone/listing cost scales with file count). | `python -m collector_core.consolidate_catalog --catalog … --apply` (`make consolidate`, nightly cron): merges each closed UTC day into one file per (type, instrument) with plain pyarrow -- Nautilus's `consolidate_data_by_period` is NotImplemented for `MarkPriceUpdate`/`IndexPriceUpdate`/`FundingRateUpdate` and deletes sources before any check -- one full schema per day -- names, types and Arrow metadata such as `price_precision` (Story 22.11: `pa.concat_tables` silently keeps the first file's metadata, so a names-only check would have relabelled a mid-day precision change) -- or refused (D-24), row count verified before sources are deleted, interrupted runs self-heal, today untouched. **Local copy trial (BTC, 5 types, 97 closed days): 7.7 s, 9,007 -> 351 files, 61 -> 21 MB; `query_second_ohlc` rows on closed days identical before/after (168,771 = 168,771, every field; today's files untouched).** **Story 22.11:** venue-agnostic by construction and now tested on `.DYDX`, `.BYBIT`, `.HYPERLIQUID` snapshot leaves plus a `MarkPriceUpdate` leaf; reader equivalence tested (`ParquetDataCatalog.query`, `query_second_ohlc`, `data_file_ranges`, `rebuild_instrument` -> identical `candle_store.window` for every bar size); midnight-crossing files left alone; `bar` leaves never merged (`backfill_bars` plans from their file intervals -- merging runs across a hole would seal it, DATA-05); an interrupted run is recognised only when the covering file's `ts_init` column equals its sources' (a row count alone could delete another writer's file); an unreadable file refuses its own day only (`consolidate.error`), never the whole run; one run at a time (flock); each run self-reports days done/refused, files and MB before -> after, wall seconds and its own peak RSS (`ru_maxrss`, inside the container) and exits 1 on any refused day or failed leaf. VPS: NOT measured -- first full run and one nightly run owed by the operator (copy the summary line here). Existing history must be consolidated once; then nightly (cron line in README "Nightly maintenance"). | FIX WRITTEN; OPEN until scheduled and measured on the VPS |
| D-37 | The store is fed every 30 s and can be ahead of the archive by up to that after a crash; the forming bar is up to 30 s stale (the live WebSocket bar covers it). | SUPERSEDED (97abc08b45): the store is fed with exactly the seconds each 60 s flush wrote, in one transaction after the Parquet write, so it is never ahead of the archive; startup catch-up applies archive seconds beyond the watermark. The forming bar is covered by the live WebSocket bar. | SUPERSEDED |
| D-40 | **Open-interest history orphaned by the type merge (Story 22.3).** `DydxOpenInterest`/`BybitOpenInterest`/`HyperliquidOpenInterest` became one `collector_core.open_interest.OpenInterest`; the catalog maps a class to `data/custom_<class>/`, so new rows land in `custom_open_interest/` while history sits in `custom_{dydx,bybit,hyperliquid}_open_interest/`, readable by nothing (the old classes no longer exist). No reader of the stored field exists today, so nothing shows wrong -- but the history would be lost to a future reader (DATA-05). | `python -m collector_core.migrate_open_interest --catalog … [--apply --backup-dir …]`: moves each file (name unchanged) into `custom_open_interest/`, rewrites the Arrow `type` metadata to `OpenInterest`, backup first, temp-then-rename, idempotent, aborts if a target file exists. Tested against a legacy-layout catalog. **Not yet run on the VPS** | OPEN until run |
### Story 22.1 — collector core (Bybit/Hyperliquid)

`platform/collector_core/` now owns the ingest/flush/sample/write path for the Bybit and
Hyperliquid collectors, so dYdX's venue-neutral guards run for every venue (dYdX itself
moves onto the core in Story 22.2).

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-38 | Bybit/Hyperliquid had none of dYdX's ingestion guards (19.3/19.4 left them out) | Each sibling had its own ~200-line copy of the loop with no stale-trade filter, no dedup, no OHLC canary, and a bare `logger.exception(...); continue` at every drop site (DATA-07) | `collector_core.Collector`: D-01 stale-age filter (`stale_trade_seconds`, 10 s) + D-02 bounded `trade_id` dedup (2000) with drops reported each flush; D-04 `ohlc_outside_book` ERROR canary; D-09 stale-book skip with accumulator discard; crossed book = skip + `error_ledger` `collector.crossed_book` once per episode, forced resync only after `crossed_resync_seconds` and only for a client with `resync_orderbook` (Bybit; Hyperliquid's full-snapshot book never resyncs); D-10 `_second_loop` lag canary; OBS-01 watchdog; every drop site is an `error_ledger` entry. Both venues now sample at 1 s | GUARDED |
| D-39 | Subscribe-time replay on the Bybit/Hyperliquid trade channels is unverified against the raw feed | First live run on the core (2026-09-20, 100 s, mainnet): Hyperliquid's `trades` subscribe reply **does** replay history — the age filter dropped 21 (BTC) / 19 (ETH) trades at subscribe (`Dropped subscribe-time trade history`), none afterwards; Bybit's `publicTrade` dropped nothing in the same run. Neither has been compared with the raw WS payload yet, so the replay size/age bounds (and whether the 10 s filter always covers Hyperliquid's) are unproven | **Both venues now verified against the raw feed** (D-42, D-43): Hyperliquid replays 30 trades on subscribe (up to 353 s old on a thin coin), Bybit replays none. Core treatment unchanged: 10 s age filter + 2000-id dedup, with the residual in D-44 | VERIFIED |

### Found 2026-09-20 — Story 22.5 (per-venue book validation)

Each venue's book is now checked against an independent source of truth (a REST snapshot),
and Bybit's one silent-desync path (an unchecked update id) has a canary. Evidence below is
from raw captures taken with clients that share no code with the collector, the same
instrument-of-truth pattern that proved the dYdX crossed-book case.

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-41 | **Bybit's book can silently desync**: it is the only snapshot+delta venue here whose stream carries a sequence (`u`) that nothing checked. The Rust client stamps `u` onto `BookOrder.order_id` (`crates/adapters/bybit/src/websocket/parse.rs:232-316`, one value per message) and performs no gap check, so a dropped or reordered message leaves a book that is wrong until the next full snapshot — with no signal at all. dYdX opts out (connection-global `message_id`, not per-market); Hyperliquid opts out (every `l2Book` is a full snapshot, `sequence=0`) — both documented in their `client.py`. | Class of failure, not an observed incident. **`u` is contiguous in a healthy stream, measured**: raw capture 2026-09-20 (`scripts/capture_hl_ws.py --venue bybit --coin BTCUSDT`, 800 s, mainnet), 31,862 `orderbook.50` frames after 1 snapshot — **31,860 of 31,860 delta steps were exactly +1, zero gaps, zero regressions, max step 1**. A skipped `u` therefore means lost messages, not a venue quirk. **Scope of that evidence: one instrument, one topic** (BTCUSDT linear `orderbook.50`); spot and thinner instruments are unmeasured, so if `u` behaves differently there the canary self-reports it as a rising `collector.book_sequence` count with a resync each time, rather than silently. | `BybitCollector._apply_deltas` canary (pure `_sequence_verdict`, unit-tested): **both** a regress (`u` <= previous) and a gap (`u` skips) → `error_ledger` `collector.book_sequence` + counter, message dropped, book dropped, resync queued; `u == 1` or a snapshot message re-baselines. Resync is the fallback, never the fix (DATA-03): a rising count is an open incident. | GUARDED |
| D-42 | **A local book drifting from the venue's would look perfectly healthy** on both new venues (no crossed-book signal on Bybit's tight books; Hyperliquid has no deltas to lose). | Hyperliquid raw capture (`platform/scripts/capture_hl_ws.py`, aiohttp, 630 s, BTC + PURR, mainnet): `l2Book` arrives **every ~5.38 s median** and **every push changed the levels** (630 s run: 5.38 s median, 4.26–5.96 s, 118 frames each for BTC and PURR, 117/117 changed; confirmed by an independent 800 s run: 5.39 s median, 3.07–5.72 s, 149 frames, 148/148 changed) — the docs' "at least 0.5 s since last push" is only a lower bound, and silence means an unchanged book only while the feed itself is alive. Same capture on `trades`: the subscribe reply **replays exactly 30 historical trades** — BTC 1.5–21.5 s old, thin coin PURR 6.5–**353 s** old (D-39's Hyperliquid half, now proven). | Periodic REST cross-check (`book_crosscheck_seconds`, default 300 s, per instrument, only for a client exposing `fetch_book_levels`): Bybit via pyo3 `request_orderbook_snapshot`, Hyperliquid via stdlib-shaped `POST /info {"type":"l2Book"}`. Pure `collector_core/book_check.py` diff of the top 20; a level is ledgered (`collector.book_crosscheck`) only when the **same** level is still wrong on a second round 2 s later, because the first live Bybit run showed single-round mismatches on nearly every check (size drift on a churning top of book, not loss). Staleness now separates **feed dead** (`feed_stale_seconds`, no WS message of any kind) from **instrument silent** (`stale_book_seconds`), and Hyperliquid's guard moved 30 s → **12 s** (≈2× the measured cadence). | FIX WRITTEN; **local evidence so far: zero confirmed mismatches** — a 20 s-interval Bybit run logged 6/6 rounds `clean (depth 20, 0 unconfirmed)` across linear + spot, and a 13 min 60 s-interval run ledgered nothing; Hyperliquid's REST path returns 20 uncrossed levels. **OPEN until the full hour per venue and the VPS deploy** |
| D-43 | **Bybit's subscribe-time trade replay and `u` continuity were unprovable from the collector's own logs**, leaving D-39's Bybit half and D-41's gap classification resting on inference. | **Root cause of the failed capture, not a mystery**: the Rust client's raw-frame line is `log::trace!` (`crates/adapters/bybit/src/websocket/handler.rs:376`), and the workspace `Cargo.toml:190` builds `log` with `release_max_level_debug`, which compiles every `trace!` **out** of the release wheel. A pyo3 `init_logging` TRACE file sink therefore captured 1237 DEBUG and 2 WARN lines and **zero** raw frames in two runs (2026-09-20 15:47 and 15:53), and no logging configuration can recover them. The temporary TRACE hook was removed from the collector rather than shipped. Bybit's `publicTrade` dropped **no** trades to the age filter in either run, consistent with "no replay" but not evidence of it. | `python platform/scripts/capture_hl_ws.py --venue bybit --coin BTCUSDT --seconds 800 --out <file>` (independent aiohttp client, no shared code with the collector), then `--summarize <file>` for the replay bounds and the `u` step histogram. **Done 2026-09-20** (800 s, BTCUSDT, mainnet): the first `publicTrade` frame held 29 trades **all 0.2 s old** and 7,506 frames followed — Bybit's subscribe reply is live trades, **no history replay**, which is why the age filter dropped nothing in the collector runs. The same capture settled `u` contiguity (D-41). | **CLOSED** — replayed nothing; `u` contiguous |
| D-44 | A replayed trade **younger than the 10 s age filter** is counted into the second it arrives in, not the second it happened — on a fresh subscribe there is no earlier copy for the id-dedup to match. | Falls out of D-42's capture: Hyperliquid replays 30 trades on subscribe and the newest are ~1.5 s old, so they pass `stale_trade_seconds` legitimately. `_process_data` filters on `ts_event` but accumulates into the current sampling second. Bounded by the filter: at most 10 s of misattribution per instrument per subscribe/reconnect, affecting the first second's volume only. | None. Bucketing trades by `ts_event` instead of arrival would fix it and also fixes D-31 (boundary-minute attribution) — the same change, worth doing once. **22.13:** the rebuild places each archived trade in its `ts_event` second, so a young replayed trade no longer lands in its arrival second once the day is rebuilt. **22.12:** on Bybit/Hyperliquid it no longer lands there live either -- it is bucketed by `ts_event`, and one whose second already closed is counted late (the 2026-09-21 Hyperliquid live check counted 24/28 late trades in 130 s, most in the subscribe window). | FIXED for trades in code (undeployed); CLOSES on the first verified day on the VPS |

### Found 2026-09-20 — candle-accuracy review (raw trades, exact fold, reconciliation)

Operator goal: 1 s aggregates and candles that equal the exchange's own numbers, and live
trading on the same fold. Review of what the pipeline can and cannot guarantee today.
Treatments are Stories 22.13 (archive + fold + rebuild + reconciliation), 22.14 (gap
closure) and the rewritten 22.12 (exchange-time semantics).

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-45 | **The 1 s snapshot is the only record of trades.** Every `TradeTick` is folded into the second's OHLC/volume/counts in `_process_data` and discarded; `order_book_deltas/` is a per-instrument opt-in nobody sets (0 files locally); no `trade_tick/` directory exists. Any fold error, late trade, reconnect gap or replay that passes the 10 s filter (D-44) changes history with no way to rebuild it. | `collector_core/collector.py:455-483`; `ls catalog/data/` shows no `trade_tick`; `DATA_DICTIONARY.md` §retention confirms deltas are off. Cost of archiving trades measured from today's snapshot counters: ETH ~2.9k, BTC ~1.4k, median dYdX coin ~150 trades/day → ~1 MB/venue/day. | 22.13: archive `TradeTick` with `ts_event` + `ts_init` every flush; nightly rebuild of closed days from trades; retention 7 days after the day is verified. **Built (22.13):** every accepted `TradeTick` is buffered and flushed to `data/trade_tick/<iid>/` via `write_data()`, both clocks untouched; readable by `catalog.trade_ticks` and `BacktestDataConfig(data_cls=TradeTick)` (tested). **Review (22.13):** paths where the archive can miss trades the live fold used -- a failed trade write whose snapshots landed, a crash after a flush carried a trade group (its rows are now carried with it), a quarantined or a pruned trade file -- are recorded as archive-gap markers (`collector_core/archive_gaps.py`) and the rebuild keeps those rows' live values instead of zeroing them (tested per path). Live mainnet run 2026-09-21 (200 s, 7 instruments, 3 venues): 22,037 trades round-tripped, 0 with `ts_event > ts_init`, 0 duplicate ids, one precision label per leaf. Footprint per venue-day: **NOT measured** (operator, `DEPLOY_CHECKLIST.md`). | FIXED in code (undeployed); footprint owed |
| D-46 | **Volume is a float sum.** `_process_data` accumulates `data.size.as_double()`; `candle_store._fold` then sums `float64`. Exact equality with the venue's kline volume (an integer sum) is not guaranteed over a day. | `collector_core/collector.py:479-483`, `ml_signals/candle_store.py:123`. | 22.13: `collector_core/fold.py` accumulates `Quantity.raw` integers, converts once; shared by live loop, rebuild and strategy code. Snapshot Arrow schema unchanged (D-24). **Built (22.13):** `collector_core/fold.py` `fold_trades` (live `_sample_tick` + `rebuild_seconds`); equals a `Decimal` sum exactly on 10k real `TradeTick`s and the old float fold within one unit of the last place; `0.1 + 0.2` at 8 dp is exactly `0.3`. | FIXED in code (undeployed) |
| D-47 | **No trade backfill across a reconnect.** The Rust clients reconnect and resubscribe silently; the core only sees the replay through `trade_id` dedup. Trades executed while the socket was down are absent from every aggregate and nothing reports how many. | `collector_core/collector.py:297,314,505` (reconnect handled by dedup only). None of the pyo3 clients passes `Reconnected` to Python (Bybit `websocket.rs:426` and dYdX `:803` only log it; Hyperliquid `client.rs:331` consumes it), so detection must be evidence-based. | 22.14: REST backfill on reconnect (dYdX indexer trades; Bybit `recent-trade`, last 1000); backfilled trades archived, not folded live; placed by the nightly rebuild. **Written (22.14):** per-feed reconnect detection in `collector_core/collector.py` (`feed_states()` inactive -> active, book-feed silence past `feed_stale_seconds or stale_book_seconds`, same-feed trade-id replay after the startup grace), coalesced into one backfill per feed 3 s later; `collector_core/trade_backfill.py` fetches `[last archived ts_event - 5 s, now]` per instrument over stdlib REST with exact decimal-string conversion, archives only unseen ids (`ts_init` = archive time, never older than the 300 s arrival margin -- refused and counted otherwise), one `collector.trade_backfill` ledger entry per backfill (backfilled / already archived / refused / unrecoverable seconds / no baseline / errors); dYdX `collector:status` carries `trade_backfill`. Tested on recorded venue responses and every I/O-matrix case. **Wire-verified 2026-09-21:** WS id == REST id on every venue (Bybit linear 999/999, spot 60/60; Hyperliquid 19/19; dYdX 3/3; price, size and aggressor equal on all), and the two feeds of `trade_feeds = 2` delivered identical id sets (Bybit 1,755/1,755 and 116/116, Hyperliquid 66/66 and 140/140). **Local 30 s network cut** (real collectors into a scratch catalog; the Rust clients noticed and reconnected ~27 s after the network came back, so the true gap was ~57 s; detected by the silence signal): dYdX archive == venue REST for the outage window on BTC/ETH/SOL (15 trades backfilled, 0 missing); Bybit ADAUSDT 155/155; Bybit BTCUSDT linear 472 backfilled + 36 s reported `unrecoverable` (1000-trade depth), ETHUSDT spot 27 + 28 s; Hyperliquid 57-60 s `unrecoverable` (10-trade depth). The first Bybit run exposed a real bug -- the baseline was read when the backfill ran, after live trades had advanced it (ADAUSDT: 368 of 382 not fetched) -- fixed by capturing it at detection (`_BackfillRequest.since`). **Owed:** the 24 h / 1-week measurements (`DEPLOY_CHECKLIST.md` §3). | FIX WRITTEN (22.14); measurement owed |
| D-48 | **Hyperliquid has no public trade-history endpoint**, so a reconnect gap there is unrecoverable from one connection. | Hyperliquid `/info` documents `candleSnapshot`, `l2Book`, `userFills`; no public historical trades (verify `recentTrades` on the day). **Verified 2026-09-21:** `POST /info {"type":"recentTrades","coin":"BTC"}` exists but returns **exactly the last 10 trades** and ignores `startTime` (fixture `collector_core/tests/fixtures/hyperliquid_recent_trades_btc_20260921.json`). | 22.14: second independent trade feed with `trade_id` union (A/B arbitration); capability table in the data dictionary as a `Known limit:`. **Written (22.14):** `trade_feeds = 2` opens a trades-only `HyperliquidWebSocketClient` (feed `main-trades`, same group as `main`); both copies go through the one bounded dedup (first archived, the other `duplicate_feed`); cumulative "Trade feed arbitration" line per flush; OBS-01 one-sided-outage alert when one feed's last trade falls 30 s behind the other's. The 10-trade REST backfill still runs and reports the rest as `unrecoverable`. Capability table: `DATA_DICTIONARY.md` §1.1. Shipped with `trade_feeds = 1`; **owed:** the 24 h run with `trade_feeds = 2` (trades by both feeds / one feed only per feed, kline pass rate before and after) and the 1-week re-record (`DEPLOY_CHECKLIST.md` §3). | FIX WRITTEN (22.14); measurement owed |
| D-49 | **dYdX book deltas carry no venue timestamp** (`OrderBookDelta.ts_event = ts_init`), so a dYdX 1 s book snapshot is arrival-timed by necessity; trades on all three venues and the book on Bybit/Hyperliquid do carry venue time. | `crates/adapters/dydx/src/websocket/parse.rs:520-539`. | 22.12: trades exchange-timed everywhere in the rebuild; book exchange-timed on Bybit/Hyperliquid; dYdX book documented arrival-timed. Revisit only if the adapter changes. | DOCUMENTED |
| D-50 | **A live hold-back cannot make a bar correct, only later.** The first 22.12 design closed second `S` at `S + 1 + hold_back` and dropped later trades; the epic AC said "never dropped". Once a second is flushed and folded (watermark), a late trade has nowhere to go without an archive and a rebuild. | Story review 2026-09-20; `candle_store.apply_seconds` skips seconds ≤ watermark by design. | Live loop stays arrival-timed and provisional; correctness comes from 22.13's rebuild; `hold_back_seconds` kept only as an optional, measured knob (22.12). Late trades archived + counted, never dropped. **22.12 as built:** the knob exists only in venue mode (Bybit/Hyperliquid, config refuses it for arrival mode), defaults to 0.0, and is set per venue from `measure_lag`'s trade p99.9 (D-63); a late trade is archived, counted (`collector.late_trade`) and excluded from every live row. | RESOLVED (design) |
| D-51 | **No reconciliation against the exchange.** Nothing compares our bars with the venues' klines, so "accurate" is asserted, not measured. | — | 22.13: nightly `compare_klines` per venue, exact integer comparison, `verified_days` in `candles_<venue>.db`, every mismatch to `error_ledger`, pass rate recorded here; raw-trade prune gated on `pass`. **Written (22.13):** `collector_core/compare_klines.py` (stdlib fetchers for all three venues, wire-verified paging, exact `Decimal.scaleb` parse; Bybit bars compared in Bybit's seeded definition, D-58) + `verified_days` + `make nightly`. **Live evidence, 2026-09-21:** on every fully collected minute of a 200 s mainnet run, 7 instruments x 2-3 minutes = 19 minutes: live fold alone 18/19 minutes mismatched; after `rebuild_seconds` 19/19 equal, exact (volume and OHLC). Daily pass rates per venue: **NOT measured** (need a full closed day on the VPS; operator). | FIX WRITTEN; OPEN until pass rates are recorded |
| D-52 | **Backfilled kline OHLCV is `f64`-round-tripped on *both* venues.** `collector_core/backfill_bars.py` writes the venues' own historical klines as `EXTERNAL` `Bar`s; every price and volume is parsed from the venue's decimal **string into an `f64`** before it becomes a `Price`/`Quantity`, so a backfilled bar is not string-exact on either venue. This matters directly for D-51: a kline comparison that treats Bybit backfill as ground truth is comparing against an f64-rounded value. | Bybit `crates/adapters/bybit/src/common/parse.rs:1187-1198` -- `value.parse::<f64>()` then `Price::new_checked(parsed, precision)`. Hyperliquid `crates/adapters/hyperliquid/src/data.rs:1278` -- `Price::new(f64, precision)`. They differ **only in range validation, not precision**; an earlier revision of this register asserted a Bybit advantage that does not exist. | Detect: compare a backfilled bar against the venue's raw kline JSON at full decimal precision. Repair: fix both parsers upstream to build `Price`/`Quantity` from the decimal string, then re-run `backfill_bars` over the affected range into a fresh catalog. Not worked around in `platform/` -- re-deriving prices in Python is the precision laundering NAUT-01 forbids, and `crates/` is off limits (FORK-01). | DOCUMENTED (accepted limit, 22.9) |
| D-53 | **Hyperliquid bar history below the `candleSnapshot` retention floor is permanently unfillable.** The venue returns roughly the last 5000 candles (~3.5 days at 1 minute); anything older cannot be fetched by any client, so a `.HYPERLIQUID` backtest can never reach further back than the collector's own start plus that window. | `crates/adapters/hyperliquid/src/http/client.rs:2726-2760` requests a bounded `candleSnapshot` and drops the in-progress bar; the floor itself is the venue's, not the adapter's. Not wire-verified in this environment (no outbound venue network) -- the ~5000 figure is from Hyperliquid's published `/info` docs. | Detect: `backfill_bars`' coverage canary warns per window with the span, the earliest/latest timestamps served and the missing-bar count; a fully empty window is logged as "no data at or before <ts>" and stops older windows, explicitly as an observation, not a proven floor. The range is **never** marked covered, so it stays visible to every later run. Repair: none from the venue -- only a paid third-party historical feed, loaded through the same `write_data()` path. | DOCUMENTED (accepted limit, 22.9) |

### Found 2026-09-21 — multi-venue rankings (Story 22.10)

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-54 | **Every Bybit and Hyperliquid row on `rankings:live` carried a fabricated `volume24h` of 0** and sank to the bottom of the default (volume) mode. | `ranking_engine` polled USD volume from dYdX's indexer only and read it back as `_VOLUME_24H.get(iid, 0.0)`; `platform/CLAUDE.md` "Adding a venue" step 7 recorded it as known-wrong. | `ranking_engine/engine.py` polls each venue's own USD 24 h volume (dYdX `volume24H`, Bybit linear + spot `turnover24h`, Hyperliquid `dayNtlVlm`) into per-source entries; a failed poll keeps the source's last good values, which expire after 3 missed polls. A fresh instrument with no volume is left **out** of volume mode (kept, with `volume24h: null`, in volatility mode) and counted at `ranking_engine.volume24h` once per poll cycle; so is each failed or expired source. No 0 is published for a missing volume. A Bybit error response (`retCode != 0`) or an empty parse is a failed poll, never a wipe of the last good values. Remaining gap, older than 22.10: dYdX's `parse_volume_24h` still reads an absent/null `volume24H` field as 0 (deferred). | FIXED in code + tests (undeployed) |
| D-55 | **Bybit spot volume is USD only by stablecoin par.** Spot `turnover24h` is in the pair's quote asset; USDT/USDC-quoted pairs are taken 1:1 as USD (a depeg skews them by the depeg ratio) and any other quote (e.g. `ETHBTC`) has no USD volume at all. | `ranking_engine/engine.py` `_BYBIT_SPOT_USD_QUOTES` `Known limit:` comment. Linear contracts are USDT/USDC-settled, same par assumption. | Non-USD-quoted spot pairs are left out of the volume dict; if one is ever collected it is absent from volume mode and counted at `ranking_engine.volume24h` every cycle (never shown at 0). Upgrade path: convert via the row's `usdIndexPrice` (base asset USD index) once verified per pair. | DOCUMENTED (accepted limit, 22.10) |


### Found 2026-09-21 — raw trade archive and reconciliation (Story 22.13)

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-56 | **A flush can split one WS message's trades and lose the next batch.** Every adapter stamps one `ts_init` per WS message; `write_data` refuses a file whose closed `[first, last]` `ts_init` interval touches an existing file's (the batch is lost, `collector.flush_write`), and silently skips an identical file name. A message whose trades straddle the 60 s flush would make the next trade file start on the previous file's last `ts_init`. | `nautilus_trader/persistence/catalog/parquet.py:253-420` (`_are_intervals_disjoint`); reproduced in a tmp catalog (a batch starting on the previous file's last `ts_init` raises `ValueError`). | `_flush_once` sorts each batch by `ts_init` (stable) and, for trades and a non-final flush, carries the newest-`ts_init` group to the next flush while it is under 5 s old; the shutdown flush writes everything. Tested (split message: both batches land). Live: consecutive trade files never touch (e.g. BTCUSDT-LINEAR `..10-38-01.219` then `10-38-01.904..`). A clock regression across flushes still takes the `collector.flush_write` LOST path, loudly. | FIXED in code (undeployed) |
| D-57 | **The sampling loop drifted and skipped whole seconds.** `_second_loop` slept `snapshot_interval_seconds` *after* its work, so each tick was late by the work's duration and a floor second was skipped every few hundred seconds -- that second's trades folded into the next row, and a rebuild could find no row for them. | `collector_core/collector.py` `_second_loop` (`asyncio.sleep(interval)` then work). | `_next_sample_at`: sample at `k * interval + interval / 2` on the wall clock, never twice in one interval bucket (pure function, tested under +-0.4 interval jitter: every bucket hit once). Live run 2026-09-21: 194 snapshots per instrument, 0 duplicate and 0 skipped floor seconds across all 7 instruments; ticks land at `.501`. The lag canary is unchanged. | FIXED in code (undeployed) |
| D-58 | **Bybit's kline is not "first trade to last trade".** Bybit seeds each kline with the previous kline's close: `open` = previous close and `high`/`low` include it. Comparing our first-trade open with it fails nearly every minute. | Wire, BTCUSDT 2026-09-20 (fetched 2026-09-21): open == previous close in **999/999** minutes, linear and spot; open never outside [low, high], equal to high or low in 17-24 % of minutes. Hyperliquid (782/1439) and dYdX (32/506 traded minutes) show only natural ties. Live run: after the rebuild, every remaining Bybit mismatch was the open alone. | `compare_klines.seed_with_previous_close` puts our Bybit bars in Bybit's definition (open = our previous traded close, high/low widened) -- an exact transformation of our integers, not a tolerance; with it, 9/9 live Bybit minutes equal. The day's first minute needs our previous close (1 m bars kept 30 days); without it (first collected day) that minute mismatches honestly. | DOCUMENTED + handled (22.13) |
| D-59 | **A Hyperliquid trade delivered more than 10 s late is lost from both the live second and the archive.** `stale_trade_seconds` (10 s) drops a trade older than that on arrival before it is archived; measured Hyperliquid arrival lag reached **8.7 s** (median ~390 ms) in a 200 s run, so the filter sits close to the live tail. | 2026-09-21 mainnet run, `ts_init - ts_event` per archived trade: Hyperliquid max 7.8 s (BTC) / 8.7 s (ETH); Bybit max 0.26 s; dYdX max 1.8 s. Dropped trades are counted (`Dropped subscribe-time trade history`) but not distinguishable from a replay. | Measure the lag distribution over a day per venue; if the tail approaches 10 s, archive stale trades raw (flagged) instead of dropping them, and let the rebuild place them. 22.14's gap closure is the other half. | OPEN (measured once, 200 s) |

### Found 2026-09-21 — trade gap closure (Story 22.14)

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-60 | **Bybit spot's REST trade depth is 60 trades** (and Hyperliquid's 10, D-48), so a reconnect backfill covers only a very short outage on a busy spot pair; linear's 1000 trades cover 28-64 s of BTCUSDT. | Verified live 2026-09-21: `GET /v5/market/recent-trade?category=spot&symbol=BTCUSDT&limit=60` returned 60 trades spanning 7.4 s (fixture `bybit_trades_btcusdt_spot_20260921.json`); `limit=1000` on spot still returned 60 (spanning 1.3 s); linear `limit=1000` returned 1000 trades (spanning 64 s in planning, 28 s in a later sample). | The backfill reports the uncovered span as `unrecoverable {iid: seconds}` in its `collector.trade_backfill` entry (never silent). Upgrade path: `trade_feeds = 2` (a second, trades-only spot socket, `spot-trades`). | DOCUMENTED (Known limit, 22.14); measurement owed with D-47 |
| D-61 | **A crash or restart gap is not backfilled.** The backfill window starts at the instrument's last archived trade, which the collector only knows in memory (`_last_trade_ts`); after a restart no instrument has a baseline (`no baseline` in the ledger entry) until its first live trade, so a gap spanning the restart is never fetched. The dYdX side is also bounded by the 300 s arrival margin (older trades are refused, not archived). | `collector_core/collector.py` `_backfill_instrument` / module docstring `Known limit:`. | Upgrade path: seed `_last_trade_ts` from the newest archived trade per instrument at startup, and a backfill-span marker read by the rebuild/prune to lift the 300 s cap. | DOCUMENTED (Known limit, 22.14) |
| D-62 | **Hyperliquid live trade `ts_event` is up to 128 ns off the venue's millisecond.** The adapter converts the venue's integer-ms `time` to ns through `f64`; at ~1.79e18 ns the f64 spacing is 256 ns. A REST-backfilled copy of the same trade is exact, so the archive would hold two clocks for one venue. The error can never move a trade across a second (a second boundary `s * 10**9` is divisible by 512, hence exact in f64), so bucketing was not affected. | `crates/adapters/hyperliquid/src/common/parse.rs:246` `millis_to_nanos(millis as f64)`. Wire, 2026-09-21 (WS vs `recentTrades`, same `tid`): 12 of 19 trades off, e.g. `1789993051206000128` live vs `1789993051206000000` REST; Bybit and dYdX 0 of 1,062 off. | `hyperliquid_collector/client.py` `_at_exact_millis` re-stamps each live trade to the nearest millisecond in integer arithmetic (exact recovery: error ≤ 128 ns ≪ 0.5 ms); the fork stays untouched. Book/mark/index/funding timestamps keep the adapter value (never bucketed by exchange time). | FIXED (22.14) |

### Found 2026-09-21 — venue-timed sampling (Story 22.12)

| ID | Danger | Mechanism / evidence | Treatment | Status |
|----|--------|----------------------|-----------|--------|
| D-63 | **The Bybit/Hyperliquid live row disagreed with the exchange at every second boundary, and nothing measured how late venue messages arrive.** The book was sampled as *received* by mid-second and trades folded into their arrival second, so a message stamped `S+0.98` that arrived at `S+1.05` landed in `S+1` (and one stamped `S+1.02` that arrived early landed in `S`); the hold-back that would reduce the live/rebuild disagreement had no measured basis. | `ts_init - ts_event`, `python -m collector_core.measure_lag`, local dev box, 2026-09-21, 600 s after a 30 s warm-up. **Bybit** (BTCUSDT/ETHUSDT linear + spot): TradeTick n=82,614 p50 105.5 / p99 158.8 / p99.9 208.3 / max 211.5 ms; OrderBookDeltas n=92,870 p50 102.7 / p99.9 160.9 / max 198.0 ms (the flat ~100 ms floor on every kind is consistent with a clock offset between this host and Bybit, not transport). **Hyperliquid** (BTC, ETH): TradeTick n=9,596 p50 382.6 / p99 823.2 / p99.9 2,307.6 / max 2,403.2 ms; l2Book n=224 p50 446.3 / p99.9 2,307.6 ms (mark/index/funding/OI are stamped at receipt by the adapter: 0). **dYdX** (reference only, stays arrival): trades n=163 p50 1,259.9 / max 5,854.8 ms; book 0 by construction (D-49). Live check (130 s, venue mode, scratch catalog): Bybit hold-back 0.5 s -- 125 rows per instrument, one per floor second, all `ts_event = S + 0.5 s`, `ts_init - ts_event` <= 1.0 s, 0 late trades, no `collector.pending_deltas`; Hyperliquid hold-back 1.0 s -- 126 rows each, same invariants, 24 (BTC) / 28 (ETH) late trades ledgered; Hyperliquid at the shipped 2.5 s (150 s, review pass) -- 145 rows each, same invariants, `ts_init - ts_event` <= 3.0 s, 0 late trades, empty ledger. Bybit ordering: held deltas are applied in `ts_event` order, and Bybit's `u` canary runs at that application, so a message whose `orderbook.ts` ran backwards against `u` would trip a `collector.book_sequence` regress and a resync. Zero occurrences across the 130 s x 4-instrument live check; that `ts` is monotonic with `u` is evidenced only that far, and a rising `collector.book_sequence` count after deployment is the canary for it (open DATA-02 question, not a proven property). | `book_time_source = "venue"` for Bybit and Hyperliquid (`collector_core/collector.py` `_hold_deltas`/`_drain_pending_deltas`/`_venue_second_loop`): second `S` closes at `S + 1 + hold_back_seconds` from the deltas with `ts_event < S+1` (applied in `ts_event` order) and the trades with `ts_event` in `[S, S+1)`; late / clock-ahead trades archived + counted, placed by the rebuild; held deltas bounded at `hold_back + 5 s` (`collector.pending_deltas` + resync). `hold_back_seconds`: Bybit 0.5, Hyperliquid 2.5 (trade p99.9 rounded up to 0.5 s) -- **provisional**, from a 10-minute dev-box run. Owed: a >= 3 h `measure_lag` run per venue on the VPS (its clock offset and network path are what matter), the TOML values reset from it, and each venue's `compare_klines` pass rate after one full rebuilt day (with D-51). | FIXED in code (undeployed); OPEN until the VPS lag runs and the first verified day per venue |

## 3. Conclusions

1. The spike candles were an **ingestion bug of ours**, not market data: the venue sends
   history on subscribe and we counted it as live. Proven against the raw feed.
2. It was made frequent by a **second, independent problem**: the collector being
   OOM-killed every 15–40 minutes. That also causes the missing-data gaps. Fixing only the
   filter would leave the gaps.
3. **BONK is a third, separate loss**: its book is unparseable in this build, so it cannot be
   captured correctly at all until the adapter changes or it is excluded.
4. Nothing above is deployed until `make redeploy-all`. Until D-03 is run the catalog still
   contains the old spikes, and until the host actions in D-06 are done OOM kills can
   continue.
5. **Not verified**: the effect of the delta-buffer change on memory; the paging rewrite's
   correctness; chart latency after any fix. These need measurement on the VPS.

## 4. Runbook (in order)

1. `make redeploy-all` — ships D-01, D-02, D-04, D-06 (buffer), D-18.
2. On the VPS: add swap, remove the orphan `dydx-dashboard` container (D-06).
3. Unsubscribe BONK via `collector:control` (D-05).
4. `python -m collector_core.repair_catalog --catalog /app/catalog` — read the report, then
   `--apply --candles-db <candles_*.db>` (D-03). Back up `catalog/` first: it rewrites
   Parquet files. Expected to report nothing on the VPS (D-33); the local dev catalog
   still has the rows as of 2026-09-20.
5. After the first deploy of the candle store: `make build-candles` (populates
   `candles_dydx.db` from the raw 1s archive; until then charts and technicals fall back
   to the slow Parquet path).
6. Schedule `make consolidate` nightly (D-36) and run it once by hand for existing history (record its summary line in D-36); then delete `<catalog>/data/custom_dydx_minute_rollup/` (D-35, unused). Configure the rclone remote + `RCLONE_REMOTE`/`RCLONE_BUCKET` in `platform/.env` and schedule `make backup-catalog` right after it, in the same cron line (D-33; README "Nightly maintenance").
7. Stop the three collectors, then merge the per-venue open-interest history (D-40):
   `python -m collector_core.migrate_open_interest --catalog /app/catalog` (report), then
   `--apply --backup-dir /backup/oi`; start the collectors. Idempotent; refuses to overwrite.
8. Watch logs for `Dropped subscribe-time trade history`, `Dropped duplicate trades`,
   `Dropped duplicate_feed trades`, `Trade feed arbitration (cumulative)` and the
   `collector.trade_backfill` ledger entries (story 22.14), and `IMPOSSIBLE trade OHLC` (must
   stay absent).
9. Story 22.13: after the redeploy, schedule `make nightly VENUE=<v>` per venue (cron line in
   `docs/DEPLOY_CHECKLIST.md`, which replaces the plain `make consolidate` line of step 6) and
   record the first run's summary lines, the trade footprint and the per-venue pass rates in
   D-45/D-51. Do not run `repair_catalog` on a day `rebuild_seconds` has rebuilt: its
   `ohlc_outside_book` detector compares exchange-timed trades with the mid-second book.
