plotly==6.8.0
pandas==3.0.4
# paired with the redis broker image tag in docker-compose.yml -- bump both together, RESP3 (8.x) is not wire-compatible with 7.x servers
redis>=8.0.1
aiohttp>=3.14.1
# stdlib tomllib is read-only -- this is its natural write counterpart, used by
# dydx_collector/config.py's save_config() to persist pin/unpin/start/stop/refresh
# control actions back to config.toml (Story 6.1).
tomli_w>=1.0.0
# bot_tui's terminal UI toolkit -- pinned per architecture Stack table (asyncio-native
# MainLoop, floor Python 3.9+, fits this project's 3.12-3.14 pin). No other file needs a
# matching version (unlike the redis client/broker pair), so no paired-version comment.
urwid==4.0.6
# nautilus-trader-base's uv sync only installs the "dev" dependency-group by default,
# not "test" -- pytest isn't otherwise present in the image `make test` runs against.
pytest>=7.4.4,<8.0.0
pytest-asyncio==0.23.8
