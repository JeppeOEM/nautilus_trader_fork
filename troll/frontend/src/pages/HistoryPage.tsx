// Placeholder -- Story 15.8 implements the 31-day metrics history page (FR44),
// deferred (sprint-status chore commit 0157c2b2e9). No metric tiles exist yet to
// apply a box-drawing treatment to; Story 15.9 only re-skins what's here today.
export default function HistoryPage() {
  return (
    <div className="term-box" data-label="History">
      <p>History — not yet implemented (Story 15.8).</p>
    </div>
  );
}
