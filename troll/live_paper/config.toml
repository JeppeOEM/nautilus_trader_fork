# Paper-mode config -- safe to commit. Loaded by live_paper.config.load_paper_config(),
# which hard-errors if this file ever contains a `mode` key (see config.py's docstring for
# why real-money execution is a separate file/loader, never a field toggle here).
network = "mainnet"
starting_balances = ["10_000 USDC"]
account_type = "MARGIN"
log_level = "INFO"
instrument_id = "BTC-USD-PERP.DYDX"
trade_size = "0.001"
trend_buy_threshold = 0.6
trend_sell_threshold = 0.4
ofi_confirm_threshold = 0.0
