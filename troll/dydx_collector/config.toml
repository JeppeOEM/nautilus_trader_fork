# dYdX market data collector configuration.
network = "mainnet"
catalog_path = "catalog"
flush_interval_seconds = 60
config_reload_seconds = 30
open_interest_poll_seconds = 300

# Empty instruments list = subscribe to every dYdX perpetual automatically.
# Add [[instruments]] entries here to restrict collection to specific coins.
