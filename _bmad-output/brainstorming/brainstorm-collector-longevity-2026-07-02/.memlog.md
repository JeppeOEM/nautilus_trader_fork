---
topic: Ensure the dYdX collector deployment runs unattended on a VPS for years
goal: Long-term reliability/durability of the standalone asyncio collector
mode: partner
status: complete
updated: 2026-07-02T18:24
---

- (technique) started Decision Tree Mapping
- (decision by user) root split = actionable failure modes only: disk filling, crashes, OOM
- (idea by user) disk/logs branch: cap log retention to 1 month
- (idea by user) disk/parquet branch: store parquet in a way that saves GB where possible
- (insight by coach) parquet codec change (snappy->zstd) is lossless; dtype narrowing (float64->float32) is NOT safe given project's precision-corruption history - rule that branch out
- (idea by user) thinning only applies if a coin's config explicitly opts into raw-book storage; default/normal mode already saves 0.5s aggregates, not raw books, so no thinning needed there
- (decision by user) crash/OOM branch: map all three sub-forks - unhandled exceptions, memory growth, external kills
- (idea by coach) wrap top-level asyncio run in try/except that logs-and-restarts; fault-isolate WS callbacks so one bad message doesn't kill the connection
- (idea by coach) check flush-timer buffer can't grow unbounded if write_data() stalls on disk I/O backpressure
- (idea by coach) docker compose restart: always + systemd enables docker on boot, for VPS reboot recovery
- (idea by coach) collector must re-derive last-written state from catalog on startup to avoid gap/dupe at restart boundary
- (idea by user) approved all six crash/OOM/disk sparks as good solutions
- (decision by user) MoSCoW convergence -> VPS prep todo list: Must=[restart:always+systemd boot, try/except restart loop, fault-isolate WS callbacks, zstd codec, docker log rotation to 1mo]; Should=[verify flush-buffer bound under I/O backpressure, verify startup state resumes cleanly from catalog]; Could=[config-gated raw-book thinning]; Won't=[float32 downcast]
- (insight) session pivots from ideation to execution: user approved full Must-have MoSCoW list for implementation (restart:always+systemd, try/except restart loop, WS fault isolation, zstd codec, log rotation, atomic write-then-rename, corrupt-file quarantine, startup state-resume)
