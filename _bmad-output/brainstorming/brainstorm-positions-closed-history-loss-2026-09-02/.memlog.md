---
topic: cache.positions_closed() history-loss bug in Story 4.6
goal: quickly find the right fix approach for durable trade/position history given NETTING PositionId reuse
mode: partner
status: complete
updated: 2026-09-02T16:01
---

- (technique) started Constraint Mapping
- (direction by user) confirmed: Creative Partner mode, batch = Constraint Mapping, Morphological Analysis, TRIZ Contradiction
- (insight by user) history need not come from Cache strictly, but strong preference to reuse existing Nautilus mechanisms over building new custom tracking
- (idea by user) write-through SQLite store for fills instead of reconstructing from Cache; sidesteps NETTING PositionId-reuse bug entirely
- (insight by coach) precedent already exists: troll/ranking_engine/metrics_store.py is a boring sqlite3 file store with thread lock + prune-on-write, matching DESIGN-01/03 (YAGNI, boring over clever)
- (technique) moved to Morphological Analysis
- (decision by user) raw SQL (no ORM) confirmed for both write and read paths, matching metrics_store.py style
- (decision by user) A1 chosen: write fill rows to SQLite on every OrderFilled event, event-sourced, never reads Cache positions_closed()/orders_closed() at all
- (decision by coach) PMI on A1+B1+C1+D1: pluses outweigh minuses; two open items before implementation — verify Position state inside on_order_filled, and add a directory volume mount for live-paper's SQLite file in docker-compose.yml (WAL sidecar gotcha, same as ranking_engine/dashboard)
- (decision by user) converged: adopt write-through SQLite (A1+B1+C1+D1), wrap up session
- (insight) Final direction: event-sourced write-through SQLite (on_order_filled -> raw INSERT, per-bot file, plain sqlite3 no ORM), compute_history() as SQL (window filter + GROUP BY date(ts) + LIMIT 500 DESC), trade_history.run() 30s loop queries SQLite and republishes unchanged bots:history:* Redis blobs. Chosen because it's structurally immune to the NETTING PositionId-reuse bug (never touches positions_closed()/orders_closed()) and reuses the established metrics_store.py sqlite pattern rather than Cache-reconstruction logic. Two follow-ups before implementation: verify Position.realized_pnl/is_closed state inside on_order_filled at fill time; add a directory volume mount for live-paper's SQLite file in docker-compose.yml (WAL sidecar gotcha). Also flagged: Story 4.4's bot_status.py has the same latent positions_closed() bug, worth a follow-up story.
- (decision by user) correction after wrap-up: B2 not B1 - single shared SQLite file across all bots (bot_id column), not one file per bot
