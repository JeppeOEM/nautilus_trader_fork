---
topic: Bot Monitoring TUI UX
updated: 2026-07-24T10:31
---

- (decision) Sources confirmed: PRD §4.6 (FR-17-25), Architecture AD-9/AD-10 + bot_tui structural seed, brainstorm-bot-monitoring-tui-2026-07-23 (all apply, user confirmed)
- (assumption) Form factor: terminal/SSH single interactive session (urwid, asyncio-native), foreground-launched via docker compose exec or on-host, not a daemon — confirmed by architecture deployment note
- (decision) Data available: rankings:live (mode, updated_at, ranks:[{instrument_id,rank,volume24h,volatility_score}]) for Coins pane; bots:status ({bot_id, ...PnL/status fields, exact list TBD}) for Bots pane; snapshots:raw via ml_signals.indicators (OFI/OBI/microprice/spread) + full order book depth for coin-detail; two independent heartbeat-staleness signals (ranking_engine, per-bot live_paper)
- (decision) Nav model: k9s adopted wholesale EXCEPT resource-count header badges and :xray secondary views (both explicit Won't/walked-back) — ':' command bar, esc pop-back (never quits), '/' fuzzy-filter coin list, breadcrumb header
- (decision) Color/typography philosophy (verbatim from brainstorm Synthesis): 'minimum surface area, maximum leverage — rare, deliberate signals over either seeing or acting' — applies to both color (attention-only, quiet baseline, no color on ranking order itself) and controls (only start/stop, nothing more granular)
- (decision) FR-24 (no history) and FR-25 (deep-link browser handoff, Should-tier) are load-bearing counterparts per brainstorm Synthesis: cutting history only works because one keypress restores exact coin+time-window on the dashboard. Creates an MVP-window gap since FR-25 ships after FR-17-24.
- (assumption) Coin-detail origin wording was 'inline' (brainstorm Combine phase) but FR-20/architecture don't specify modal vs full-screen-replace vs split-pane; k9s itself replaces the list view on drill-down
- (event) Source extraction complete via subagent read of PRD/architecture/brainstorm; 11 open questions identified for user
- (decision) Coin-detail view: full-screen replace (k9s drill-down model), not split-pane or modal. esc pops back to coin list.
- (decision) Auto-surface good-to-trade coins: sort order alone is sufficient (top of ranked list per shared ranking engine) — no extra highlight/pin/summary treatment. Defers the brainstorm's broader ambition per user call.
- (decision) Quit mechanism: ':q' command via the command bar, consistent with the '`:`-for-everything' model rather than a bare keypress.
- (decision) Bots pane columns confirmed: bot_id, PnL, strategy/symbol, mode (paper/live), position size/exposure, uptime/last-heartbeat, win rate/trade count.
- (decision) SCOPE TENSION RESOLVED: trades-list + daily-PnL-histogram requested by user, but conflicts with FR-24 (TUI is live-only, no history) and bots:status's live-snapshot-only schema. User chose to KEEP FR-24 as-is: bot-detail view shows live snapshot only (current PnL/position/uptime/win-rate-to-date); an [o] keybinding deep-links to the web dashboard for trades list + PnL histogram — extends the FR-25 coin deep-link pattern to bots symmetrically. FLAG FOR PM/ARCHITECTURE: this bot-deep-link isn't yet in the PRD (FR-25 only covers coins) — worth a PRD amendment once this UX ships.
- (decision) Staleness display: separate indicators per domain — Coins pane's own stale flag (ranking_engine heartbeat), each Bots-pane row its own stale flag (that bot's live_paper heartbeat). Never unified into one indicator.
- (decision) Color baseline: respect terminal's own default fg/bg (portable across SSH themes, matches k9s convention) — only red/yellow/green-family accents used, exclusively for attention signals (stale, big PnL swing). No forced fixed theme.
- (change) REVERSED prior decision (entry 10): user now wants trades-list + PnL-over-time IN the TUI after all — but ONLY if sourced from the exact same store the web dashboard reads, zero duplicated computation/implementation between the two surfaces.
- (decision) Architecture implication (confirmed via codebase check: no existing trade/PnL history store anywhere — live_paper persists nothing beyond Nautilus's in-memory cache, bots:status/AD-10 is live-heartbeat-only): a NEW shared trades/PnL-history store is needed, live_paper as sole writer, dashboard AND bot_tui as read-only readers of the same store — same Gatekeeper-one-layer-up pattern as AD-9's ranking_engine. Flagged for PRD/architecture amendment, not resolved by this UX pass alone.
- (decision) Scope resolution: FR-24's 'no history in the TUI' narrows to market-data/order-book replay specifically (stays dashboard-only, unchanged) — bot/trade performance history becomes a new, distinct, in-scope TUI surface. These are different data domains under one FR number today; PRD needs to split/amend FR-24 accordingly.
- (decision) Bot-detail view (full-screen, same drill-down model as coin-detail) now has 3 regions: (1) live snapshot header — PnL, position, mode, uptime, win-rate-to-date, strategy/symbol; (2) trades list — scrollable blotter (ts, side, price, qty, realized PnL per fill), sourced from the new shared trade-history store; (3) PnL-over-time chart — bar/sparkline rendered from the same store, time range controlled by a preset toggle (day/week/month/all), not free-form scrubbing — keeps closer to the keyboard-only/no-arbitrary-scrub spirit of the original FR-24 intent while still surfacing history.
- (event) Discovery substantially closed: IA = Coins pane, Bots pane, Coin-detail (full-screen), Bot-detail (full-screen, 3 regions above); k9s-style nav (:, esc, /, breadcrumb, :q to quit); attention-only color on terminal-default fg/bg; two independent per-domain staleness indicators. Ready to move toward Finalize (spine drafting).
- (change) CORRECTION (user prompted: 'does nautilus not save the trades'): re-checked nautilus_trader source directly. Cache already exposes orders_closed()/positions_closed()/position_snapshots() query API and supports a durable Redis-backed database (CacheConfig(database=DatabaseConfig(type='redis',...))) — this stack already runs Redis. live_paper/node.py currently builds TradingNodeConfig with no cache= override (defaults in-memory-only, lost on restart, unreachable externally). The real gap is just: (1) enable CacheConfig(database=...) in live_paper, (2) live_paper exposes a thin read surface over that Cache to bot_tui/dashboard (per AD-10, readers still never touch live_paper/Cache internals directly — Nautilus's own Redis encoding is not a stable external contract). NOT a bespoke new SQLite/store as previously logged — spines corrected accordingly.
- (event) 4 key-screen HTML mocks rendered and promoted from .working/ to mockups/: key-coins-pane.html (+ command-bar overlay state), key-coin-detail.html (healthy + thin-book states), key-bots-pane.html (negative-PnL + per-row stale states), key-bot-detail.html (Flow 2 hero screen, 3 regions). All 5 IA surfaces covered (command-bar shown as a Coins-pane state, not a separate file). Inline links added to EXPERIENCE.md's Information Architecture table.
- (decision) Coin-detail order-book ladder: collapsed to top-of-book (1 row) by default on every entry, never remembers expanded state across visits. 'd' toggles open to full 20-level depth and back. Pure display toggle scoped to the ladder region only -- does not affect breadcrumb, indicators, esc, or the pane underneath. Clarifies FR-20's 'never truncated to top-of-book only' as a capability guarantee (full depth always reachable), not a default-open mandate. Updated DESIGN.md (depth-ladder-row token + Components prose), EXPERIENCE.md (IA table, Component Patterns, State Patterns split into Book collapsed/Book expanded/Thin order book, Interaction Primitives 'd' key, Flow 1 step 4), and mockups/key-coin-detail.html (now 3 states: default-collapsed, expanded-healthy, expanded-thin).
- (change) Simplified key-coin-detail.html mock to exactly 2 states (collapsed default, expanded) per user request -- dropped the third expanded+thin-book screen. Thin-book handling stays documented as text in EXPERIENCE.md's State Patterns table, just not mocked as its own screen.
- (event) Structural review (bmad-editorial-review-structure) and prose review (bmad-editorial-review-prose) applied: condensed DESIGN.md frontmatter comment/prose duplication, deduped Interaction Primitives vs Inspiration & Anti-patterns rejected-feature list, standardized 'stale badge' terminology, fixed grammar (missing 'that', 'hasn't heartbeat'->'hasn't sent a heartbeat'), split one 68-word sentence, cut one within-sentence redundancy. Both DESIGN.md and EXPERIENCE.md set to status: final, updated: 2026-07-24.
- (event) spines finalized
