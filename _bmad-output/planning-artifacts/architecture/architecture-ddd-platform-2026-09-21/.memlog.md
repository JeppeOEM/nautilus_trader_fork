---
scope: all of troll/ (to be renamed platform/): capture core + 3 venue collectors, common, ml_signals, ranking_engine, data_api+frontend, live_paper, bot_tui
purpose: build-substrate: DDD spine projecting the Gatekeeper paradigm into bounded contexts; parent = architecture-nautilus_trader_fork-2026-07-01
altitude: initiative
updated: 2026-09-21T13:48
---

- (event) run started 2026-09-21, create intent, Fast path (user-approved plan); seed = planning-artifacts/ddd-redesign-seed-2026-09-21.md; deliverable = spine + pointer paragraph in troll/ARCHITECTURE.md only
- (constraint) inherit parent spine 2026-07-01 AD-1..AD-11 + Consistency Conventions read-only: single write gate both sinks; fail-closed; readers trust the gate; module boundary shared types + pure utils only; precision via scaleb+from_raw; catalog via write_data only; USD liquidity; no TradingNode in data path (live_paper sanctioned); ranking_engine sole ranking publisher; live_paper control plane isolation; one node many bots
- (constraint) user decisions: scope all of troll/; full tactical DDD admitted only where a pattern names an invariant; spine only (no stories yet); rename troll/ -> platform/ as migration step 0 after story 22.12 merges and bmad-loop idle; strangler migration one context per story; every Epic 22 story 22.1-22.14 covered incl. 22.14 code (feed.py, venue_http.py, trade_backfill.py) and in-flight 22.12 as [ASSUMPTION]
- (constraint) project rules binding the design: troll/CLAUDE.md FORK-01/02, DATA-01..08, DESIGN-01 (YAGNI) in tension with tactical DDD, TEST-01..04, SSOT-01..05, MEM-01..03, NAUT-01..03; minimize dependencies (no event-bus lib, no DI container); HFT standard: no per-message allocations on the ingest path
- (decision) paradigm: Gatekeeper expressed as bounded contexts (DDD strategic + tactical); binds all of platform/; prevents the gate dissolving into ten packages with no named owner
- (decision) contexts: capture (core), collection_control, archive (maintenance), candles, ranking (core), bots (core, today live_paper), alerting, research, views (CQRS query side), observability (generic), kernel (shared kernel); data_api/bot_tui/frontend stay interface adapters
- (decision) layering per context domain/ -> application/ -> infrastructure/; domain has no I/O, no asyncio, no Nautilus runtime; ports are typing.Protocol; enforced by an ast-walking pytest (platform/tests/test_boundaries.py), no new dependency
- (decision) shared kernel = DydxSecondSnapshot (class name pinned: catalog dir custom_dydx_second_snapshot derives from __name__ via nautilus_trader.persistence.funcs.class_to_filename), OpenInterest, fold_trades, venue helpers (merge common/venues.py + ml_signals/venue.py), pure Indicator classes, ns/two-clock helpers, venue_http stdlib transport
- (decision) capture -> candles via a SecondSink port defined in capture and implemented by candles, injected at the venue entrypoint; resolves parent Deferred 'writer->reader imports' (candle_store); error_ledger moves to observability which imports nothing
- (decision) hot-path AD: zero per-message allocations on ingest; events per sampled second / state transition only; a benchmark test gates the capture refactor
- (decision) capture aggregates: LiveBook (per instrument, wraps Nautilus OrderBook by reference, venue policies LevelTagger/SequenceCanary/CrossedBookPolicy/BookTimeSource as values), TradeIntake (dedup window, stale-history filter, per-feed first-copy arbitration, late-trade count), FeedGroup (Feed VOs, per-feed liveness, reconnect detection, backfill scheduling); SecondSampler + TradeBackfill domain services; ports VenueFeed, VenueTradeHistory, ArchiveWriter, LiveStream, SecondSink, Notifier
- (decision) archive: ArchiveDay aggregate provisional->rebuilt->verified|mismatched (verified_days), RetentionPolicy (age AND verified), ReconciliationResult exact never tolerance, nightly chain as saga, CatalogFiles port is the only in-place pq.write_table rewriter (AD-6 exception), VenueKlines ACL per venue; backfill_bars (22.9) lives here
- (decision) ranking: RankingBoard aggregate root replaces ranking_engine module globals; InstrumentMetrics entity; VolumeSource port per venue (22.10 parsers stay pure); RankingHistory (metrics_store) port; metrics_computer moves here
- (decision) bots: Bot aggregate (BotId == order_id_tag), PaperFleet vs RealMoneyBot config types (mode from type never from message), FillLedger per bot, NautilusHost ACL (per-venue clients, VENUES table), CacheReader scoped by strategy_id; Strategy subclasses are framework code
- (decision) collection_control: CollectionPlan aggregate (instruments, exclude, pins, cap 30), classify_liquidity USD, CollectionPlanStore TOML port, InstrumentAdded/Removed events drive capture subscribe/unsubscribe; only dYdX has a live plan today
- (decision) migration: rename first and alone; then observability -> kernel -> views -> candles -> alerting -> research -> archive -> ranking -> bots -> collection_control -> capture; re-export shims with DeprecationWarning live at most two stories; published-language freeze (catalog schema, Redis payloads, SQLite schemas, compose service names, env vars) for the whole migration
- (assumption) 22.12 modelled from its story file (book_time_source, hold_back_seconds, measure_lag.py, collector.late_trade); revisit when it merges
- (assumption) platform/ stays a namespace directory (no __init__.py) so it cannot shadow stdlib platform; guard test added at rename
- (question) DESIGN-01 wording: amend to 'no abstraction without a named invariant' or keep and document the tension -- user decision, deferred
- (event) reviewer gate dispatched: rubric walker (+ input reconciliation), version/reality lens, adversarial lens -> reviews/
- (event) story 22.12 merged into troll as 70847351c8 during the run; code matches its story (CoreConfig.book_time_source/hold_back_seconds config.py:53,57; measure_lag.py; _drain_pending_deltas collector.py:838; Bybit 0.5 s / HL 2.5 s hold-back in config.toml) -> 22.12 rows become [ADOPTED]; bmad-loop idle, rename gate open
- (decision) hot-path acceptance uses stdlib tracemalloc baseline test, not pytest-benchmark (plugin only in root dev deps, .benchmarks/ empty)
- (event) reviewer gate returned: rubric NOT READY (C1 graph, H1-H5), versions no critical (H-1..H-3), adversary C1-C4/H1-H6; all clear fixes applied in one re-distill
- (decision) AD-D2 graph corrected: API/TUI -> kernel+observability; alerting reaches views' forming bar via a views-declared BarObserver port wired in data_api's composition root; kernel imports nothing; boundary test ships in story 1 with a legacy-module->context map and a cross-row private-name ban
- (decision) kernel gains SecondOHLC, performance_metrics, clocks.py (CatalogFileSpan + single MAX_TS_INIT_SKEW_NS = 300 s), archive_markers.py (ArchiveGap), catalog_files.py (read helpers), parquet_compat.py (zstd patch); venues.py is the only InstrumentId parser; every venue REST request via venue_http
- (decision) AD-D17 collection plan: intent vs applied set (CaptureService.apply -> Applied), cap 30, USD pins, one config loader, control holds no prune loop; AD-D18 one writer process per catalog leaf (capture lock, maintenance lock, repair refuses), archive markers capture-written (write_failed/quarantined) archive-written (pruned) archive-read
- (decision) AD-D9: verified_days is the only day-status store; reconcile_day only after rebuild_day in the same saga run; RetentionPolicy the only deleter incl. dropped-instrument and delta retention; CatalogFiles replaces four rewrite sites
- (decision) AD-D8: exactly two folds (fold_trades, fold_arrays); forming_bar is a candles query service; SecondSink called with the flushed batch; candle prune is a candles process manager; migration order candles before views
- (decision) AD-D13: step 0 also moves durable stores to platform/data/ and the dYdX plan file to platform/data/dydx_config.toml so later context moves never touch mounts; ruff/isort stdlib classification verified for the platform name
- (decision) AD-D15 narrowed to the paper/non-paper split [ADOPTED]; ExecConfig.mode demo/real is a validated value, Known limit with upgrade path
- (decision) AD-D16: ledger is per process (Known limit, upgrade path errors:ledger channel); observability.notify is the one outbound transport; watchdog generic; incident handler takes iid pattern from the venue
- (decision) AD-D5 acceptance: tracemalloc allocations <= baseline AND wall time <= 2x baseline, baseline recorded in the first migration story
- (assumption) target paths and names (LiveBook, ArchiveDay, kernel/clocks.py, platform/data/...) are [TARGET]; each context story ratifies or renames them
- (event) spine finalized 2026-09-21 (status: final)
