---
scope: React/React-Query/lightweight-charts frontend rewrite of troll's dashboard (ml_signals/dashboard.py) + backend refactor to serve it (data_api), on branch troll
purpose: build-substrate + companion diagram/walkthrough doc
altitude: feature (inherits initiative-altitude parent: dYdX Collector & ml_signals — Data-Integrity Spine, 2026-07-01)
updated: 2026-09-13T20:11
---

- (constraint) Inherited AD-3 (parent spine, initiative altitude): readers trust the gate completely -- the new frontend + refactored data_api are readers only, must never re-implement data-quality checks (crossed-book, staleness, precision) against catalog/stream data.
- (constraint) Inherited AD-4: module boundary is shared data types + pure side-effect-free utilities only. New frontend/backend code may import DydxSecondSnapshot-derived types and ml_signals.indicators pure classes, never dydx_collector's stateful ingestion internals.
- (constraint) Inherited AD-6: all catalog access only through ParquetDataCatalog's official API / already-existing ml_signals query functions (catalog_stats, chart_data) -- no hand-rolled Parquet reads, no unbounded catalog.trade_ticks()-style loads (MEM-01).
- (constraint) Inherited Fork boundary convention: nautilus_trader/ and crates/ stay untouched; all new frontend/backend code is additive under troll/.
- (constraint) Project-wide rule SEC-01 (troll/CLAUDE.md, not in parent spine but binding): every Docker port stays 127.0.0.1-bound; no bare host:container port mapping ever.
- (constraint) Project-wide rules SSOT-01..05 (troll/CLAUDE.md): stateful/rolling metrics (OFI/OBI z-scores, rankings, book_features) are computed exactly once by ranking_engine and published -- new frontend/backend must be pure readers of that output, never recompute independently. New UI must not duplicate a metric that already exists in dashboard.py/bot_tui without consolidating onto one shared source first.
- (direction) Supersedes epic-14 (Plotly /chart/{id} page in ml_signals/dashboard.py); dashboard.py's other pages (rankings list, coin list, docs, history) are in scope per user's 'complete refactor of the frontend' framing -- confirm exact page scope with user before drafting Structural Seed.
- (decision) Scope: full dashboard.py surface (rankings list, coin list, chart page, history, docs) becomes one React SPA -- not a chart-page-only island. No permanent split-stack (some pages React, some server HTML).
- (decision) Live data model: real-time push (WS/SSE) is kept, not replaced by polling -- backend relays Redis pub/sub (snapshots:raw, rankings:live, bots:status) to the browser, never recomputing (AD-3/SSOT-02 compliant relay only).
- (decision) Backend shape: collapse into one API service. data_api absorbs every JSON route + the live WS/SSE layer; becomes sole backend for the React app. dashboard.py's HTML-rendering/Jinja-esque string-building code is deleted once React replaces what it rendered; its Redis-subscriber logic moves into data_api.
- (version) lightweight-charts 5.2.1 (verified current stable via GitHub releases, 2026-09-13) -- native multi-pane API (addPane/addSeries on IPaneApi) with auto-synced time scale across panes in one chart instance.
- (version) TanStack React Query 5.102.8 (verified npm, 2026-09-13).
- (version) Vite 8.3.0 + @vitejs/plugin-react v6 (Oxc-based, no Babel dep), React 19.3.0 (verified 2026-09-13).
- (decision) Chart sync mechanism: lightweight-charts v5 native multi-pane (one chart instance: candlestick main pane + volume/OFI/OBI/microprice sub-panes via addPane), not multiple manually-synced chart instances. Eliminates custom sync code (replaces dashboard.py's _syncChartXRange/_SYNCED_CHART_IDS Plotly-relayout-event approach entirely).
- (decision) Language: TypeScript for the whole frontend, matching the repo's existing strict-typing discipline (mypy disallow_incomplete_defs, READ-03).
- (decision) Build/deploy: multi-stage Dockerfile -- a node-based build stage runs vite build inside the existing thin troll/ image (layered on nautilus-trader-base), dist/ is COPY'd in and served as static files by the collapsed API service (FastAPI StaticFiles). No new container/service, no host Node.js dependency for deploys, still a fast thin-image rebuild.
- (decision) Incremental history API: GET /api/candles/{instrument_id}?before_ns=<cursor>&limit=<n>&bar_seconds=<n>, cursor-paginated backward from before_ns, reusing chart_data.compute_chart_series/candle_dicts_from_snapshots (AD-6, no reimplementation). Drives lightweight-charts' subscribeVisibleLogicalRangeChange scroll-back-preload.
- (decision) Live layer: single multiplexed WebSocket /ws/live, subscribe/unsubscribe messages per Redis channel (snapshots:raw:{iid}, rankings:live, bots:status) -- pure relay, never recomputing (SSOT-02); same staleness/heartbeat discipline as existing Redis channels.
- (decision) Routing: client-side React Router mirroring current pages (/, /coin/{id}, /coin/{id}/chart, /bots, /docs). API under /api/*, live under /ws/*, namespaced so they never collide with SPA client routes; FastAPI StaticFiles serves index.html as catch-all fallback for unmatched non-API paths.
- (decision) Testing: Vitest + React Testing Library for frontend (ships with Vite, zero extra config); pytest stays for backend. TEST-01/03 apply -- real data shapes, no mocking Nautilus-derived types.
- (decision) State: React Query owns all server state; indicator-config persistence stays a server-side REST resource (today's save_coin_indicator_config_handler moves into the collapsed API service). No Redux/Zustand -- YAGNI (DESIGN-01), nothing needs client-only global state React Query + component state doesn't already cover.
- (event) spine drafted (distill step complete), 5 new ADs (AD-F1..F5)
- (constraint) User correction: bots/live_paper data stays exclusively bot_tui's domain -- not part of this frontend at all (dashboard.py never had a bots page either; earlier draft's routes/bots.py + BotsPage + bots:status WS relay were scope creep, removed).
- (decision) Frontend page scope confirmed: rankings (= coin list), single-coin chart, 31-day metrics-history page, docs. All four in scope, all become React.
- (event) Reviewer gate ran: 4 parallel subagents (reconcile-vs-inputs, rubric-checklist, live-web version-verify, adversarial-incompatibility). Findings applied: restored DATA-01/AD-9-staleness half as AD-F6; fixed AD-F1/AD-F3 pagination contradiction (AD-F3 governs wire contract on relocation); pinned pagination response envelope {items, has_more}; added AD-F7 for the live/forming candle bar (server-aggregated via existing ml_signals.candles, one derived WS sub-channel, never client-aggregated); added pane registry to AD-F4; adopted FastAPI's native app.frontend() over manual StaticFiles+catch-all (AD-F1a); split data_api into its own dockerfile instead of bolting a Node stage onto the shared troll/collector.dockerfile (verified all other services share that file); named the decoupled-dev-vs-single-origin-deploy tradeoff explicitly; added initial-page-load-speed convention (code-splitting, cache headers, compression) for the user's 'blazingly fast loading' requirement; added AD-8 + list-refresh-key-stability convention. Versions re-verified live (not search-summary): all current, zero stale.
- (event) spine finalized
- (event) run closed; companion HTML walkthrough published as an Artifact
