---
title: 'Story 22.8: Spine, rules and docs updated for the multi-venue core'
type: chore
created: '2026-09-20'
status: done
baseline_revision: 2b6c4ff76650a86202f7d2b23a2bd17a249b13db
review_loop_iteration: 0
final_revision: 068b24816a25fd68845ba5ace8276e9708247460
followup_review_recommended: true
context:
  - '{project-root}/troll/CLAUDE.md'
warnings: [oversized]
---

<intent-contract>

## Intent

**Problem:** Epic 22.1–22.7 folded three sibling collectors into `troll/collector_core/`, but the documents of record still describe one dYdX collector. `ARCHITECTURE-SPINE.md` binds AD-1/AD-2/AD-4/AD-8 to `dydx_collector.collector`, cites `collector.py:643/735/549/660/662-671/288-302/549,792/369` line numbers that are all dead, lists a deleted `DydxMinuteBar` as a shared type, says the single write gate "holds by there being exactly one writer today", and its Redis row says "one producer per channel/key" while three collectors now publish `snapshots:raw`. `troll/CLAUDE.md` has no written-down recipe for adding a venue, and DATA-03 still names `collector._resync_book`/`_CROSSED_RESYNC_NS` as if they were core symbols. Repo-root `CLAUDE.md`, `project-context.md`, `troll/README.md` and `docs/DATABASE_SETUP.md` still state single-venue facts that are now false.

**Approach:** Documentation of record only, written against the merged tree and re-grounded on symbols/lines verified at time of writing: amend the spine's ADs, conventions, structural seed and deployment paragraph; re-point `troll/CLAUDE.md`'s stale symbols and add one "Adding a venue" section; correct the other docs' single-venue statements. Every amendment carries an `[amended 2026-09-20: Epic 22]` marker so the next audit sees what changed.

## Boundaries & Constraints

**Always:** Quote real symbol names and paths read from the merged tree — never a name recalled from the story text (the story says `CoreConfig`; the class is `CoreConfig` in `collector_core/config.py`, but `_resync_book` exists only on `DydxCollector`, and `_second_loop` has no venue override). Keep every DATA/OBS rule's substance and incident history intact; when a mechanism moved, say both what it was and where it lives now. Mark each amendment `[amended 2026-09-20: Epic 22]`. Where an `[ADOPTED]` claim is now false, state what is actually true rather than restating it.

**Block If:** Amending an AD would require changing what the rule *requires* (not just where it binds or what it cites) — an invariant change is an architecture decision, not a docs update.

**Never:** Touch any `.py`, `Makefile`, `Dockerfile` or compose file (docs-only story; `no code changes` per the story's Project Structure Notes). Never rewrite `DATA_INTEGRITY_AUDIT.md` §1's historical incident rows. Never add a new `troll/CLAUDE.md` rule beyond the single evidenced Epic 22 lesson allowed below. Never edit `sprint-status.yaml`. Never touch `nautilus_trader/` or `crates/`.

</intent-contract>

## Code Map

Ground truth verified in this worktree (2026-09-20):

- `troll/collector_core/collector.py` -- `Collector`, 938 lines. Gate = `_sample_tick` (`:612`); empty top-of-book skip `:627`; crossed skip `:630` → `_handle_crossed_book` (`:365`); staleness `_stale_reason` (`:673`); catalog write `_flush_once` (`:541`, `asyncio.to_thread`); instruments write `:830`; Redis publish `_second_loop` (`:699`, publish at `:718`); own loops assembled in `run()` (`:848-861`). Client contract is the module docstring (`:15-48`): 5 required + `subscribe_global`/`fetch_book_levels`/`resync_orderbook` optional.
- `troll/collector_core/config.py` -- `CoreConfig` with `stale_book_seconds` (`:36`), `crossed_resync_seconds` (`:37`), `stale_trade_seconds` (`:38`), `seen_trade_ids`, `feed_stale_seconds`, `book_crosscheck_seconds`. `_STALE_BOOK_NS`/`_STALE_TRADE_NS`/`_CROSSED_RESYNC_NS` no longer exist anywhere.
- `troll/dydx_collector/collector.py` -- `DydxCollector` overrides `_clear_book_state` (`:268`), `_apply_deltas` (`:283`), `_handle_crossed_book` (`:331`); `_resync_book` (`:346`) is dYdX-only. `troll/bybit_collector/collector.py` -- overrides `_apply_deltas` (`:116`, `u` canary), entrypoint `:153`. `troll/hyperliquid_collector/collector.py` -- no override.
- `troll/common/venues.py` -- `VENUE_KINDS` (`:8`), `venue_kind` (`:11`), `market_kind` (`:19`).
- `collector_core/collector.py:71-73` imports `ml_signals.candle_store`/`error_ledger`/`catalog_stats.query_second_ohlc` -- a writer→reader import that **contradicts AD-4's current "`dydx_collector` never imports from `ml_signals`" clause**. Pre-dates Epic 22 (`error_ledger` already imported at `d3ae27afaf`, Epic 21).
- Targets: `_bmad-output/planning-artifacts/architecture/architecture-nautilus_trader_fork-2026-07-01/ARCHITECTURE-SPINE.md` (AD-1 `:80-82`, AD-2 `:86-88`, AD-4 `:96-100`, AD-6 `:112`, AD-7 `:118`, AD-8 `:122-124`, AD-11 `:164,:171,:175`, Conventions `:180-181`, Seed `:203-208`, Deployment `:243`, Deferred `:253,:255`); `troll/CLAUDE.md` (header `:3-5` already core-scoped; DATA-03 `:43`; OBS-01 `:59`; Design Principles end `:69`); `CLAUDE.md` `:5,:7,:9,:15,:16,:18,:20`; `_bmad-output/project-context.md` `:21,:29,:37,:50,:72`; `troll/README.md` `:1,:3,:44,:76`; `troll/docs/DATABASE_SETUP.md` `:42,:63`; `_bmad-output/implementation-artifacts/19-3-*.md:71`, `19-4-*.md:61`.
- Already correct, verify-only (do not churn): `troll/docs/DATA_DICTIONARY.md` (§1 header, §1.7/§1.8, spot note `:131-133`), `troll/docs/DATA_INTEGRITY_AUDIT.md` (zero `_STALE_*`/dead-line cites), `troll/CLAUDE.md` DATA-01/05/06 and FORK-02. **Correction (2026-09-20 review pass): `troll/ARCHITECTURE.md` was *not* already correct** — it had zero dead line-citations, which is what was checked, but it still asserted the pre-Epic-22 catalog contents (`:280`), the clean AD-4 no-reverse-imports clause (`:42`), an `ml_signals` `ranking:control` publish (`:33`) and a single-venue data-flow diagram (`:53`). All four were corrected in that pass.

## Tasks & Acceptance

**Execution:**
- [x] `ARCHITECTURE-SPINE.md` AD-1 -- Binds → `collector_core.collector.Collector` + every subclass + any writer script; re-ground the `[ADOPTED]` citation on `_sample_tick:612`/`_flush_once:541`/`_second_loop:718`; replace "holds by there being exactly one writer today" with structural wording (every venue writes through the one core class; a venue may override `_apply_deltas`/`_handle_crossed_book` only, never the gate) -- AC #1.
- [x] `ARCHITECTURE-SPINE.md` AD-2 -- Binds → the core gate; re-ground `:627`/`:630`/`_stale_reason:673`/`crossed_resync_seconds`; add the per-venue crossed-book semantics (dYdX uncross override, Bybit skip+ledger+`u`-canary+resync, Hyperliquid skip-only) with a pointer to research §A/§B1.
- [x] `ARCHITECTURE-SPINE.md` AD-4 -- shared types → `DydxSecondSnapshot`, `OpenInterest`, `ohlc_outside_book` in `collector_core/` (drop `DydxMinuteBar`); Binds add `collector_core`, `bybit_collector`, `hyperliquid_collector`, `common`; re-ground the citation on the real post-22.3 import paths; state honestly that the core imports three `ml_signals` utility modules and pair it with a Deferred entry -- an `[ADOPTED]` marker over a false clause is the failure this story exists to prevent.
- [x] `ARCHITECTURE-SPINE.md` AD-6/AD-7/AD-8 -- correct the three dead line citations (`:112` `549,792` → `541,830`; `:118` `open_interest.py:109` → `:40`; `:124` `collector.py:369` → `run()`'s loop tuple) and AD-8's "driving `DydxHttpClient`/`DydxWebSocketClient`" → one duck-typed client per venue. Citation-only; rules unchanged.
- [x] `ARCHITECTURE-SPINE.md` AD-11 -- confirm 22.6's amendment at `:164`; fix the un-amended `DydxDataClientConfig` at `:171` and the `DydxSecondSnapshot`/`dydx_collector` framing at `:175`.
- [x] `ARCHITECTURE-SPINE.md` Consistency Conventions -- Redis row → "one producer per (channel, venue) for `snapshots:raw` (payload entries disjoint by `instrument_id`); one producer per channel/key otherwise", `collector:status`/`collector:control` noted dYdX-only; Module-dependencies row → `collector_core` -- AC #1.
- [x] `ARCHITECTURE-SPINE.md` Structural Seed + Deployment + Deferred -- seed gains `collector_core/`, the three venue packages and `common/` and loses `minute_bars.py`; deployment paragraph names the nine dirs the thin image bakes and the three collector services sharing one catalog root; Deferred "Shared validator extraction" → resolved by 22.1.
- [x] `troll/CLAUDE.md` -- DATA-03 `_resync_book`/`_CROSSED_RESYNC_NS` → `DydxCollector._resync_book` (dYdX-only) / `Collector._resync` + `CoreConfig.crossed_resync_seconds`, naming the old constant; OBS-01 `_second_loop` → `collector_core.collector.Collector._second_loop`, incident text untouched -- AC #2.
- [x] `troll/CLAUDE.md` -- new "Adding a venue" section after Design Principles (~12 lines): the eight duck-typed client methods (5 required + 3 optional, per the contract docstring) and which are optional; `config.toml` (+ `config.py` subclassing `CoreConfig` only if the venue needs extra keys); a ~15-line `XCollector(Collector)` + `run_forever` entrypoint; one `common/venues.py` line (`VENUE_KINDS`, plus `market_kind` if the id suffix is new); compose service + `collector.dockerfile` COPY + `Makefile` test path; a wire-behaviour investigation (crossed-book semantics, sequence semantics, precision, OI availability, subscribe replay, rate limits) **before** choosing which hooks to override, citing 19.3/19.4/22.5; `DATA_INTEGRITY_AUDIT.md` rows for that venue -- AC #2.
- [x] `troll/CLAUDE.md` -- at most one new rule, only for the evidenced Epic 22 lesson: a venue's sequence field means three different things (dYdX connection-global `message_id`, Bybit per-topic `u`, Hyperliquid full snapshots) -- never gap-check without that venue's own evidence. Cite 22.2/22.5 and audit D-41.
- [x] `CLAUDE.md` (repo root) -- "Project"/"Constraints": core + three venue collectors, correct `troll/collector.dockerfile`/`troll/docker-compose.yml` paths, `DydxOpenInterest` → `OpenInterest`; keep the `DataEngine`-OOM and precision incident notes verbatim.
- [x] `_bmad-output/project-context.md` -- `:21`/`:37` core wording, `:29` `client.py:45` → `:50`, `:50` test dirs, `:72` `_STALE_BOOK_NS` → `config.stale_book_seconds`.
- [x] `troll/README.md` -- title/intro cover three venues; configure section points at each venue's own `config.toml`; note the shared catalog root. Leave the tunnel/backtest/deploy sections alone.
- [x] `troll/docs/DATABASE_SETUP.md` -- `snapshots:raw` publisher row and the "`dydx_collector` is the sole writer of `snapshots:raw`" bullet → all three collectors (`collector:status`/`collector:control` stay dYdX-only). This bullet directly contradicts the amended convention, so it cannot be left.
- [x] `19-3-troll-bybit-collector.md`, `19-4-troll-hyperliquid-collector.md` -- append one Completion Notes line each: sibling-not-base deferral closed by Epic 22 (22.1 `collector_core`). `epics.md` left as historical.

**Acceptance Criteria:**
- Given the amended spine, when AD-1, AD-4 and AD-11 are read, then each names `collector_core.collector.Collector` as the single write gate, the moved shared types (no `DydxMinuteBar`) and per-venue clients, and the Consistency Conventions Redis row reads "one producer per (channel, venue)" (AC #1).
- Given `troll/CLAUDE.md`, when a contributor looks for how to add a venue, then the header scopes the rules to `collector_core/` and every venue collector and exactly one "Adding a venue" section states the recipe: client + config + entrypoint + one `common/venues.py` line (AC #2).
- Given any file path or symbol cited in an amended AD or rule, when it is checked against the tree, then it exists (verification commands below return no unexplained hits).
- Given `git diff --stat`, when the change is reviewed, then only `.md` files appear.

## Review Triage Log

### 2026-09-20 — Review pass (second follow-up, independent reviewers)
- intent_gap: 0
- bad_spec: 0
- patch: 21: (high 5, medium 8, low 8)
- defer: 5: (high 0, medium 3, low 2)
- reject: 1
- addressed_findings:
  - `[high]` `[patch]` The prior pass struck the Deferred entry "Dashboard cleanup — crossed-book skip only" as **Moot** on the premise that "`data_api` did not port the crossed-book skip — only the gap rendering", and `project-context.md:71` asserted the same ("deliberately not ported: under AD-3 a reader can never see a crossed book"). Both are false: `data_api/routes/snapshots.py:132` is `if bp >= ap:  # crossed/touched snapshot -- skip`, with an empty-top-of-book skip at `:129`, and that function's own module docstring (`:30-34`) says it is a verbatim relocation of `dashboard._price_series_rows` *including* the crossed-book skip. The entry is un-struck and repointed; `project-context.md` corrected. Worse, the same pass widened AD-3's binding to `data_api` — so an `[ADOPTED]` AD gained a live, unstated violation in the reader it had just been extended to cover, which is exactly the failure this story exists to prevent and which AD-4 was handled correctly for. AD-3 now carries the deviation explicitly.
  - `[high]` `[patch]` AD-2's re-grounded Binds enumerates the gate's checks "exhaustively" and lists three; there are four. `_sample_tick`'s first branch (`collector_core/collector.py:623`) skips the instrument when `self._live_books.get(iid)` is `None`, via `_handle_missing_book` (`:689`) — its own rejection path with its own rate-limited WARNING. Same "framed as complete but isn't" defect the prior pass fixed in AD-1 and AD-4 and left standing in AD-2.
  - `[high]` `[patch]` "Adding a venue" step 3 told a new venue to load through `core_config_from_dict` "which rejects an unknown key" without naming its `extra_keys` argument — and the rejection is `set(raw) - (_CORE_KEYS | set(extra_keys))` (`collector_core/config.py:64-68`). A venue following step 3 literally, with the venue-specific key step 3 itself tells it to add, gets `ValueError: unknown config keys [...]` from its own valid `config.toml` at startup. `bybit_collector/config.py:33-35` is the working pattern and is now cited.
  - `[high]` `[patch]` `troll/ARCHITECTURE.md` was recorded in the spec's Code Map and the story's Completion Notes as "already correct post-22.x — verified, not churned". The check performed was for dead `_STALE_*`/line citations, which it does pass; it still carried four statements this same commit falsified elsewhere. `:280` listed the catalog as holding "Trades, order book deltas, bars" — the exact phrasing the prior pass corrected in `DATABASE_SETUP.md:135-137` against audit D-45/D-35. `:42` asserted "The collectors never import from anything downstream of them", the clean form of the very AD-4 clause the spine was amended to retract. `:33` credited `ml_signals` with a `ranking:control` publish, contradicting the Redis conventions row the prior pass rewrote to name `bot_tui/ranking_state.py:128` as sole producer. `:53`'s data-flow diagram was still single-venue, contradicting this file's own three-collector summary at `:11-13`. All four corrected; the false "verified" claim corrected in both the spec and the story.
  - `[high]` `[patch]` DATA-08 asserts the Bybit `u` canary "has two deliberate no-verdict cases". There is a third, and unlike the two it is not deliberate: `_message_u` returns `None` for a zero-level message (an explicit wire case — `crates/adapters/bybit/src/websocket/parse.rs`'s `total_levels == 0` branch, and the function's own docstring says so), and `bybit_collector/collector.py:117` gates the *whole* canary, baseline write included, on `u is not None`. So a skipped message leaves `_last_u` one behind and the next message reads `u - last_u == 2` → `"gap"` → a `collector.book_sequence` ledger entry, a dropped book and a DATA-03 forced resync on a healthy stream. Named as an open DATA-02 question, which is what DATA-02 requires of an unmeasured false-positive path.
  - `[medium]` `[patch]` SEC-01's `network_mode: host` service enumeration — a security rule whose evidence is a one-by-one service list — still named only `collector`/`data_api`/`ranking_engine`/`bot_tui`/`live-paper`, omitting both Epic 22 collectors (`docker-compose.yml:83`, `:106`). The recipe's step 6 warns about exactly this class of stale list in the `Makefile`; SEC-01's own was not swept.
  - `[medium]` `[patch]` OBS-01's amendment reads "Both loops now live in the shared core", but one of the two named mechanisms no longer exists: `grep -rn "_bar_builder" --include='*.py' troll` returns nothing — minute rollups were retired (D-35). The amendment implied a deleted symbol had been relocated; DATA-01/DATA-03 in the same file model the correct treatment.
  - `[medium]` `[patch]` `DATABASE_SETUP.md:133` still read "read-write for `collector`" (singular) two lines above the §3 sentence the prior pass rewrote to "the three collectors"; `docker-compose.yml:53`/`:76` mount the same catalog read-write for all three.
  - `[medium]` `[patch]` Recipe step 6's compose checklist omitted the four keys all three existing collector services set: `REDIS_URL` (the `redis://127.0.0.1:6379` default misses `Makefile:20-23`'s `LOCAL_DEV` port shift, and `_publish_snapshot_batch` only warns on publish failure — so the new venue writes Parquet normally while being invisible in rankings, `bot_tui` and every live view), `network_mode: host`, `user: "1000:1000"` (three collectors share one catalog root, so a root-writing service leaves partition dirs the others cannot write into) and the `logging: *default-logging` anchor.
  - `[medium]` `[patch]` The recipe's seven steps would leave a completed venue publishing a **fabricated** `volume24h` of 0 on `rankings:live`, sorted to the bottom of the default mode: `ranking_engine/engine.py:180-211` sources volume from dYdX's indexer alone and keys it `-PERP.DYDX`, `:435` reads `_VOLUME_24H.get(iid, 0.0)`. That is DATA-01 / audit-D-27, not step 5's soft `"unknown"`, and it is true of Bybit and Hyperliquid today. Added as a step of its own pointing at Story 22.10; also deferred.
  - `[medium]` `[patch]` The Design Paradigm's headline absolute — the gate is "the only component in `troll/` that writes *raw* market data **anywhere**" — is contradicted by AD-1's own amended Binds and by AD-6 two sections later, both of which name `collector_core/repair_catalog.py` and the three other in-place rewriters. Scoped to the live ingest path, which is what AD-1/AD-6 now say.
  - `[medium]` `[patch]` `troll/README.md`'s shared-`CoreConfig` paragraph — written specifically to warn which keys dYdX's loader ignores — listed `environment` among them without noting that dYdX's TOML key is `network` (`dydx_collector/config.py:93`; `__post_init__` derives `environment` from it), so an `environment = ...` line in `troll/config.toml` is read by nothing. Same trap the paragraph exists to flag.
  - `[medium]` `[patch]` AD-2's rule text certified that "the crossed and stale paths both comply" with "a `logging.WARNING` line records the full offending payload". Neither logs a payload — `:391-397` logs `iid`/`bid`/`ask`/duration, `:635` logs `iid` plus a reason string. The payload half is aspirational everywhere, not only on the silent path the prior pass flagged.
  - `[low]` `[patch]` Recipe step 2's "miss this and the collector … writes nothing" is false: `fetch_instruments()` does carry data back and `run():830` writes the instrument definitions before `connect()` — AD-1 names it as residual write path (2) in the same commit. Corrected to "no *market-data message*".
  - `[low]` `[patch]` Recipe step 2 never stated that the contract's methods are coroutines, although the core `await`s all eight. A sync implementation raises `TypeError: object ... can't be used in 'await' expression` inside `run()`, which `run_forever` retries forever with backoff — a permanent restart loop whose traceback points at the core, not the new client.
  - `[low]` `[patch]` Recipe step 5 warns that a missed `VENUE_KINDS` entry "fails soft" but never gave the key's required casing — the exact way the entry silently misses (`common/venues.py:8` is keyed by the **uppercase** Nautilus venue token; `venue_kind` is a plain `.get()`). Step 6 specifies casing for `CANDLES_DB_PATH`, so the omission read as deliberate.
  - `[low]` `[patch]` Step 1 mandates investigating rate/subscribe limits, but nothing in the recipe said where the answer goes: `collector_core/collector.py:843-845` subscribes every instrument back-to-back with no spacing and no hook in between, so a venue whose pyo3 client has no built-in throttle bans itself at connect inside `run_forever`'s restart loop. Step 2 now places the cap in the venue client's own `subscribe()`, citing dYdX's `_MAX_COLLECTED_INSTRUMENTS` (`:106`) as the precedent the root `CLAUDE.md` "Rate limits" note omits.
  - `[low]` `[patch]` The structural seed's Bybit entry attributed the optional REST-vs-live cross-check to Hyperliquid alone; `bybit_collector/client.py:142` implements `fetch_book_levels` too, so Bybit runs the 22.5 cross-check as well.
  - `[low]` `[patch]` The spec's task text and the story's Task 2 both say "the seven duck-typed client methods", while the delivered `troll/CLAUDE.md` and AD-8 say five required + three optional, matching the contract docstring's eight entries. Spec and story corrected to eight.
  - `[low]` `[patch]` The story's Task 4 marked `[x]` on a dead-symbol grep "returning only historical/archive mentions (audit §1, story files)". It also returns `troll/frontend/src/pages/docs/kbData.ts:143` (`_CROSSED_RESYNC_NS`, on the live knowledge-base page `data_api` serves at `/docs`) and `dydx_collector/notebooks/candlestick_pattern_scanner.ipynb` (`DydxMinuteBar`). Neither is an audit row or a story file; both are non-`.md` and so out of this story's diff. Claim qualified, hits deferred.
  - `[low]` `[patch]` The spec's Code Map listed `troll/ARCHITECTURE.md` under "Already correct, verify-only (do not churn)" — the assessment that produced the high-severity finding above. Removed from that list, with the correction recorded in place.
  - Deferred (5): the reader-side crossed-book skip now living at `data_api/routes/snapshots.py:132` (AD-3 deviation, code change); `ranking_engine`'s fabricated `volume24h = 0` for Bybit and Hyperliquid (DATA-01/D-27, owned by Story 22.10); `kbData.ts`'s dead `_CROSSED_RESYNC_NS`/`dashboard.py` refs plus `DATA_DICTIONARY.md`'s seven remaining `dashboard` mentions; `bot_tui/coin_detail_state.py:24`'s now-false single-producer invariant on `snapshots:raw`; the spine Stack table's `plotly 6.9.0`/`pandas 3.0.5` rows, which match no pin in the tree and describe a dependency with no remaining consumer. All five are `.py`/`.ts` changes or out-of-scope pre-existing drift.
  - Rejected (1): `sprint-status.yaml` appearing in `git diff --stat` against the spec's "only `.md` files" acceptance criterion and its "never edit `sprint-status.yaml`" constraint — that row is the orchestrator's own bookkeeping, written outside this session and explicitly not this session's to revert.

### 2026-09-20 — Review pass (follow-up, independent reviewers)
- intent_gap: 0
- bad_spec: 0
- patch: 23: (high 4, medium 11, low 8)
- defer: 4: (high 0, medium 3, low 1)
- reject: 3
- addressed_findings:
  - `[high]` `[patch]` AD-1's "one residual path" paragraph was wrong about scope *and* about mechanism. It named only `dydx_collector/collector.py:305` and `bybit_collector/collector.py:148` and implied that was exhaustive; the largest bypass is in the core itself — `Collector._process_data`'s final `else` (`collector_core/collector.py:487`) buffers every mark/index price, funding rate and WS open-interest row for all three venues, always, and `run():830` writes instrument definitions directly. All four paths now enumerated. The same paragraph also claimed "the poll bypasses `_process_data` on purpose so REST data cannot count as WS feed liveness" as if true of both venues: Bybit obeys it, dYdX's `_open_interest_loop` (`:361`) calls `self._on_data(item)` and stamps `_last_feed_message_ns`, contradicting the core's own comment at `:290-291`. Stated honestly and deferred.
  - `[high]` `[patch]` AD-2's widened Binds listed **precision** among the gate's invariant checks, and the Design Paradigm repeated it — but no precision check exists anywhere in `collector_core/`, and the same diff says so twice (root `CLAUDE.md` and `project-context.md:29` both mark `_at_fixed_precision` dYdX-only). The gate's three checks are now enumerated exhaustively, with precision named as per-venue-at-ingest and pointed at the recipe's step-1 investigation.
  - `[high]` `[patch]` AD-2's rule asserts a `logging.WARNING` per rejected item, and its re-grounded citation is the counterexample: the empty-top-of-book skip (`collector_core/collector.py:627`) logs nothing, ledgers nothing, counts nothing — a live DATA-07 gap producing unexplained permanent catalog gaps. Named in AD-2 and in the Consistency Conventions row, and deferred.
  - `[high]` `[patch]` "Two-image Docker split" was false and was re-asserted in the paragraph claiming a 2026-09-20 re-read: `troll/` has three dockerfiles, and `troll/data_api.dockerfile:1-3` points back at this very section as the place recording why it is separate. Corrected in the spine's Deployment paragraph, `project-context.md:21` and root `CLAUDE.md`.
  - `[medium]` `[patch]` Recipe step 2's client contract omitted the `on_data` constructor callback — the one wiring without which data never arrives (all three clients take `on_data=self._on_data`). A literal reading produced a collector that connects, subscribes, logs `Started: N subscribed` and writes nothing. Added to steps 2 and 4.
  - `[medium]` `[patch]` Recipe step 6 stated the wrong consequence for omitting `CANDLES_DB_PATH`: separate containers never share the file. The real failure is a container-local SQLite at the unmounted `/app/candles`, discarded each rebuild, with `data_api` silently serving empty charts. Corrected, plus the unstated requirement that `<venue>` be the lowercase Nautilus venue token (`data_api/settings.py:25`).
  - `[medium]` `[patch]` Recipe step 1 cited `troll/scripts/capture_hl_ws.py` as the capture tool for a new venue; its `--venue` is a closed `choices` tuple and the subscribe payload a hard two-way branch, so a fourth venue's `--url` gets Hyperliquid's JSON. The mandatory step had no working tool. Step 1 now says to extend it first; deferred as code work.
  - `[medium]` `[patch]` Recipe step 4 mandated the `ml_signals.error_ledger` import that AD-4's own new Deferred entry records as a live violation, with no cross-reference — so the deviation count would grow with every venue. Tension now stated and adjudicated in place (DATA-07 wins; the venue adds to that entry rather than inventing a second error path).
  - `[medium]` `[patch]` DATA-01's amended "5 s is only `CoreConfig`'s default, set per venue" is false for dYdX: its key-by-key loader never reads `stale_book_seconds` (or five other `CoreConfig` fields) and ignores unknown keys, so an operator tuning it gets neither effect nor error. `troll/README.md` said this correctly in the same commit; DATA-01 now matches.
  - `[medium]` `[patch]` `troll/README.md:3` and root `CLAUDE.md` presented `store_order_book_deltas` as a general opt-in — it exists only on `DydxConfig`'s per-instrument entry; Bybit/Hyperliquid take a flat `tuple[str, ...]`. Both also omitted that a Bybit **spot** id (which README's own example config subscribes) yields no mark/index price, no funding rate (`bybit_collector/client.py:124-125` gates the ticker on `LINEAR`) and no open interest. Corrected in both.
  - `[medium]` `[patch]` `troll/docs/DATABASE_SETUP.md:135-137` still described the catalog as archiving "trades, order-book deltas, bars" — contradicted by the README rewrite in the same commit, by audit D-45 (trades folded, no `trade_tick/`) and D-35 (minute rollups retired). The diff had edited line 134 of that hunk and left the rest.
  - `[medium]` `[patch]` The amended Redis conventions row asserted "one producer per channel/key otherwise" while still naming the Story-15.10-retired `dashboard` as a second producer on `ranking:control` (sole producer: `bot_tui/ranking_state.py:128`) and on `bots:history:*`. Both corrected.
  - `[medium]` `[patch]` AD-4's deviation list — framed as complete — omitted `dydx_collector/collector.py:79-80`, the clearest instance of the very clause it quotes ("`dydx_collector` never imports from `ml_signals`"). Added.
  - `[medium]` `[patch]` AD-4's re-verification grep covered three directories while the amendment widened its binding to ten packages, and the Module-dependencies row omitted an entire cross-namespace import class: `ml_signals.venue`'s `venue_of`/`MalformedInstrumentId`, imported by `ranking_engine`, `live_paper` and six `data_api` modules. Both the evidence's scope and the row now cover it.
  - `[medium]` `[patch]` The spine's mermaid diagram — its most-read artefact — was untouched and still showed one dYdX source, `precision` as a gate check, and `dashboard` as the live reader, contradicting the re-scoped title and the AD-3/AD-4 amendments in the same commit. Redrawn: three venues, the core gate with its real checks, the ungated buffer path, `data_api` in place of `dashboard`. AD-9/AD-10's prose remains deferred Epic 15 cleanup, now noted in the diagram's own comment.
  - `[low]` `[patch]` Recipe step 6's "four packages already ship a `tests/test_collector.py`" — there are three (`collector_core`, `bybit_collector`, `hyperliquid_collector`; dYdX has none by that name).
  - `[low]` `[patch]` Recipe step 6 named `redeploy-all` and `build-insecure` as the `Makefile`'s explicit service lists; `frontend-dev` (`troll/Makefile:260`) is a third and is equally incomplete.
  - `[low]` `[patch]` The structural seed's dYdX entry listed three overrides while AD-1 listed four — `_instrument_ids` (`dydx_collector/collector.py:265`) was missing, one document disagreeing with itself. Added.
  - `[low]` `[patch]` AD-2 cited "`stale_book_seconds`/`stale_trade_seconds`/`crossed_resync_seconds` (`config.py:36-38`)"; the file's order is 36/37/38 = `stale_book_seconds`/`crossed_resync_seconds`/`stale_trade_seconds`. Each name now carries its own line.
  - `[low]` `[patch]` `19-4-troll-hyperliquid-collector.md`'s new completion line said "with no override at all"; `hyperliquid_collector/collector.py:42` overrides `__init__`. Given the same carve-out the spine makes explicit. The structural seed's Hyperliquid entry had the identical wording and was fixed too.
  - `[low]` `[patch]` `project-context.md:20` still described the "Dashboard stack" as aiohttp + plotly (it is FastAPI + uvicorn + React) and `:24` scoped the `nautilus_trader` pin to "the PyO3 dYdX client bindings" although it now gates three adapters. Both fixed, and the spine's Stack-table row given the same correction.
  - `[low]` `[patch]` The structural seed omitted `dydx_collector/normalize_snapshot_schema.py` — which AD-6, two sections earlier in the same amended document, names as one of three in-place catalog rewriters — and `hyperliquid_collector/book_snapshot.py`, which backs the `fetch_book_levels` optional step 2 describes. Both added.
  - `[low]` `[patch]` Recipe step 4's env-var rule had an in-tree counterexample it did not flag: `dydx_collector/collector.py:90` hardcodes `CONFIG_PATH` with no env var, yet host edits apply because compose bind-mounts over the baked 0-byte placeholder. Named, with the warning not to mix the two shapes.
  - Deferred (4): dYdX's OI poll stamping WS feed liveness; the silent empty-top-of-book skip; `troll/data_api.dockerfile` not copying `collector_core`/`common` although `data_api` imports both (dates from 22.3); `capture_hl_ws.py`'s closed `--venue` choices. All four are code changes, out of scope for a docs-only story; each is also recorded in the spine's Deferred section.
  - Rejected (3): `backtest_dydx.py:67-72` cited for "uses `BacktestNode`/`BacktestDataConfig`" (flagged as pointing at the import block rather than the construction sites — already adjudicated and rejected in the prior pass on the same reasoning; "see" pointing at the import block is accurate); `collector_core.integrity.ohlc_outside_book` listed among AD-4's allowed pure utilities (AD-4 enumerates permitted *categories* with examples, and it is imported cross-namespace by `dydx_collector/tests/test_integrity.py`); `error_ledger` being per-process so `GET /api/errors` under-reports (already an open entry in the deferred-work ledger from the prior pass).
  - Note, not a finding: the prior pass's ledger entry on Epic 15 drift listed the mermaid `DASH` node and `project-context.md:20` alongside AD-9/AD-10; this pass resolved the first two. The entry was left untouched per the orchestrator's ownership of the ledger — AD-9/AD-10 remain its live remainder.

### 2026-09-20 — Review pass
- intent_gap: 0
- bad_spec: 0
- patch: 21: (high 2, medium 14, low 5)
- defer: 3: (high 0, medium 3, low 0)
- reject: 2
- addressed_findings:
  - `[high]` `[patch]` "Adding a venue" step 6 omitted `CANDLES_DB_PATH` and the candles-dir mount — the core falls back to one shared `candles.db`, so a fourth venue following the recipe literally would put two candle writers on one file. Wiring step now names it, plus `<VENUE>_COLLECTOR_CONFIG`, both `__init__.py` files, and the `Makefile`'s other service lists.
  - `[high]` `[patch]` `troll/README.md` claimed "an unknown key is a hard error, never a silent default" in the paragraph introducing dYdX's own config: `dydx_collector/config.py`'s loader reads key-by-key with `raw.get()`, ignores unknown keys, and never reads six of `CoreConfig`'s fields at all. Replaced with a two-loader paragraph naming the six.
  - `[medium]` `[patch]` Recipe step 3 called `config.py` optional while step 4's entrypoint calls `load_config(CONFIG_PATH)`; the loader is mandatory, only the `CoreConfig` subclass is optional. Corrected, with `core_config_from_dict` named as the strict loader to copy.
  - `[medium]` `[patch]` Recipe had no DATA-07 error-ledger step and no note that `venue_kind`/`market_kind` return `"unknown"` rather than raising (skipping step 5 fails soft). Both added.
  - `[medium]` `[patch]` DATA-08 promoted D-41's Bybit `u` evidence into a rule without D-41's own scope caveat (one instrument, one topic; Bybit **spot** is collected and unmeasured) and without `_sequence_verdict`'s two no-verdict cases. Both restored.
  - `[medium]` `[patch]` DATA-01 attributed the stale-book skip to `_second_loop` (it is in `_sample_tick`, as the spine's own AD-2 amendment says) and hardcoded "(5 s)" although Hyperliquid runs 12 s. Repointed.
  - `[medium]` `[patch]` AD-1's new "held structurally" claim ignored the residual paths that reach `write_data` without `_sample_tick` (`dydx_collector/collector.py:305` raw deltas, `bybit_collector/collector.py:148` OI poll). Named, with why they are deliberate and what a new buffer item inherits.
  - `[medium]` `[patch]` AD-6 re-affirmed `[ADOPTED]` on a `write_data()` grep that structurally cannot see three `pq.write_table` catalog rewriters, two of them `collector_core/` modules this same rewrite added to the structural seed. All three named as the accepted rewrite-in-place exception.
  - `[medium]` `[patch]` AD-3 ("readers trust the gate") still bound only `ml_signals` while AD-4 was extended to `data_api` in the same pass — one document, two incompatible reader lists. AD-3's binding extended to match.
  - `[medium]` `[patch]` `troll/README.md:3` and `CLAUDE.md:7` asserted the archive holds raw trades and order book deltas; audit D-45 says trades are folded into the second's snapshot and discarded, and deltas are an opt-in nobody sets. Both corrected to what is actually written.
  - `[medium]` `[patch]` `CLAUDE.md`'s "Open interest" and "Rate limits" constraints kept dYdX-only facts under a heading rewritten to be multi-venue (Hyperliquid forwards OI over the WS; Bybit spot has none). Made per-venue; the precision constraint marked dYdX-only to match `project-context.md:29`.
  - `[medium]` `[patch]` `troll/docs/DATABASE_SETUP.md` was half-converted: `:42` named `data_api` while the adjacent `rankings:live` row and four later bullets still named the deleted `dashboard`. Converted the rest of the file and added a scope note.
  - `[medium]` `[patch]` `_bmad-output/project-context.md:71` was stale in exactly the way this story exists to fix (`collector._second_loop`, `dashboard._coin_chart_json`, a `test_dashboard_chart.py` that does not exist); `:72`'s dashboard half was left when its `_STALE_BOOK_NS` half was fixed. Both repointed.
  - `[medium]` `[patch]` Spine `:22`/`:74`/`:181` and the structural seed still named `ml_signals/dashboard.py` as a live reader on lines this diff was already editing, contradicting `DATABASE_SETUP.md:42` in the same commit. Repointed to `data_api` on the edited lines; the wider Epic 15 sweep deferred.
  - `[medium]` `[patch]` The two Deferred "dashboard" entries pointed at symbols deleted by Story 15.10 (`_coin_chart_json`, `_CHART_GAP_THRESHOLD_MS`, Plotly). First struck as moot; second repointed to `data_api/routes/snapshots.py:75`'s `_SNAPSHOT_GAP_THRESHOLD_MS`.
  - `[medium]` `[patch]` Spine frontmatter `name`/`scope`/`updated` and the H1 still scoped the document of record to `troll/dydx_collector` alone. Re-scoped to the core plus the three venue packages and `common/`.
  - `[low]` `[patch]` "no core override at all" / "overrides only these hooks" excluded `__init__`, which all three subclasses override and recipe step 4 mandates. Clarified as hooks.
  - `[low]` `[patch]` Structural seed's Bybit entry listed only `_apply_deltas`, omitting `_clear_book_state` — disagreeing with AD-1's list in the same document. Added.
  - `[low]` `[patch]` The AD-4 Deferred entry described the deviation as three utility modules and missed the stricter half: `build_candles.py:39` and `consolidate_catalog.py:47` import the private `ml_signals.catalog_stats._stamp_to_ns`. Added.
  - `[low]` `[patch]` `project-context.md:39-40` cited `ml_signals/backtest_dydx.py:48`/`:57` (module moved to `strategies/`), and `:50`'s new test-dir sentence overstated "all listed in the `Makefile`'s `test` target" (`live_paper/tests` is deliberately excluded). Both fixed.
  - `[low]` `[patch]` The spine's "New components' deployment shape (this update)" paragraph contradicted the service list directly above it that this diff rewrote. Marked historical.
  - `[low]` `[patch]` Three off-by-one citations in this spec's own Code Map (`:15-49`→`:15-48`, `:848-862`→`:848-861`, dYdX `_clear_book_state` `:270`→`:268`); the published docs had them right.
  - Rejected: AD-6's `backtest_dydx.py:67-72,99` cite (flagged as stale line numbers; verified — `:67-72` is the `BacktestNode`/`BacktestDataConfig`/`ImportableStrategyConfig` import block and `:99` an `ImportableStrategyConfig(` construction, which is exactly what the sentence claims); `epic-22-context.md`'s "dYdX is the only subclass" (a compiled distillation of pre-implementation planning docs, not a document of record).

## Verification

**Commands:**
- `grep -rn "DydxMinuteBar\|DydxOpenInterest\|_STALE_BOOK_NS\|_STALE_TRADE_NS\|_CROSSED_RESYNC_NS\|dydx_collector.second_snapshot\|dydx_collector.integrity" troll _bmad-output/planning-artifacts/architecture _bmad-output/project-context.md CLAUDE.md` -- expected: only historical/archive mentions (audit §1 D-40, story files, research docs, and DATA-03's explicit "was named X" note).
- `grep -rn "collector\.py:[0-9]" _bmad-output/planning-artifacts/architecture/architecture-nautilus_trader_fork-2026-07-01/ARCHITECTURE-SPINE.md` then verify each with `sed -n '<N>p' troll/collector_core/collector.py` -- expected: every cited line is the construct it claims.
- `ls troll/collector_core/collector.py troll/collector_core/config.py troll/common/venues.py troll/bybit_collector/collector.py troll/hyperliquid_collector/collector.py troll/dydx_collector/uncross.py` -- expected: all exist.
- `git diff --stat` -- expected: `.md` files only, no `.py`/`Makefile`/`Dockerfile`/`.yml`.
- `pre-commit run --files <changed .md files>` -- expected: pass (markdown-only; `check-copyright-year` is code-only).
- No tests: docs-only change, TEST-02.


## Auto Run Result

Status: `done` (second follow-up review pass on an already-`done` spec; no implementation loopback was needed).

**Change:** documentation of record only. The architecture spine, `troll/CLAUDE.md`, `troll/ARCHITECTURE.md`, repo-root `CLAUDE.md`, `_bmad-output/project-context.md`, `troll/README.md` and `troll/docs/DATABASE_SETUP.md` describe the multi-venue collector core rather than a single dYdX collector, with every symbol and line citation re-grounded on the merged tree. This pass added 21 corrections; five of them change what a reader would believe about the system, and one reverses a prior pass's own conclusion.

**Files changed (since `2b6c4ff766`):**
- `ARCHITECTURE-SPINE.md` — Design Paradigm's "writes raw market data anywhere" absolute scoped to the live ingest path (AD-1/AD-6 name the offline rewriters); AD-2's "exhaustive" gate-check list gained the missing-book path and lost the false "crossed and stale paths both comply"; AD-3 now states the `data_api` crossed-book-skip deviation instead of implying none exists; the "Dashboard cleanup — crossed-book skip only" Deferred entry un-struck and repointed at `data_api/routes/snapshots.py:132`; structural seed's Bybit entry gained `fetch_book_levels`.
- `troll/CLAUDE.md` — SEC-01's `network_mode: host` list gained both Epic 22 collectors; OBS-01 no longer implies the deleted `_bar_builder` was relocated; DATA-08 gained the third (undeliberate) no-verdict case and its false-gap consequence; "Adding a venue" gained `core_config_from_dict`'s `extra_keys`, the coroutine requirement, the subscribe-throttle location, `VENUE_KINDS`' casing, four missing compose keys, and a new step 7 on the fabricated `volume24h = 0` a completed venue still gets.
- `troll/ARCHITECTURE.md` — **not previously touched by this story**, having been assessed as already correct. Four statements corrected: pre-D-45 catalog contents (`:280`), the clean AD-4 no-reverse-imports clause (`:42`), an `ml_signals` `ranking:control` publish (`:33`), and the single-venue data-flow diagram (`:53`).
- `troll/docs/DATABASE_SETUP.md` — §3's catalog mount is read-write for all three collector services, not `collector` alone.
- `troll/README.md` — the shared-`CoreConfig` paragraph now flags that dYdX's TOML key is `network`, so an `environment` line there is read by nothing.
- `_bmad-output/project-context.md` — the reader-side crossed-book skip is recorded as ported and tracked, not as removed.
- `spec-22-8-*.md` / `22-8-*.md` — "seven" client methods → eight; the false "`troll/ARCHITECTURE.md` already correct" assessment corrected in both; Task 4's dead-symbol-grep claim qualified with the two live non-archive hits it did not account for.
- `deferred-work.md` — five new entries appended (no existing entry touched, per the orchestrator's ownership).

**Review findings:** 21 patched (5 high, 8 medium, 8 low), 5 deferred, 1 rejected, 0 intent_gap, 0 bad_spec. Full breakdown in the Review Triage Log entry above.

**Verification performed:**
- Every line citation introduced by this pass resolved with `sed -n '<N>p'` against the merged tree: `collector_core/collector.py` `:417-418, 623, 689, 739, 828, 830, 832, 834, 843-845, 876, 391-397`; `collector_core/config.py:64-68`; `bybit_collector/config.py:33-35`; `bybit_collector/collector.py:117`; `bybit_collector/client.py:142`; `dydx_collector/collector.py:106`; `common/venues.py:8`; `ranking_engine/engine.py:208-209, 435, 461`; `data_api/routes/snapshots.py:30-34, 129-133`; `docker-compose.yml:37,44,59,61 / :73,77,83,85 / :97,100,106,108`; `Makefile:20-23`; `frontend/src/pages/docs/kbData.ts:143,195,202`; `bot_tui/coin_detail_state.py:24`; `DATA_DICTIONARY.md:309`. One correction made during the pass: `resync_orderbook` is awaited at `:418`, not `:417`.
- `git diff --name-only 2b6c4ff766` — 13 `.md` files plus `sprint-status.yaml`, the latter written by the orchestrator outside this session and deliberately left alone. No `.py`, `.ts`, `Makefile`, `Dockerfile` or compose file touched.
- Dead-symbol grep (`DydxMinuteBar`, `DydxOpenInterest`, `_STALE_BOOK_NS`, `_STALE_TRADE_NS`, `_CROSSED_RESYNC_NS`) over the six documents of record — every remaining hit is an explicit "was named X / no longer exists" historical note.
- `pre-commit` is not installed in this worktree; its `.md`-relevant checks were run by hand: no trailing whitespace on any added line, final newline present in all nine edited files, code fences balanced in each.
- No tests run: docs-only change, TEST-02.

**Residual risks:**
- Three successive review passes have each found ~20 real defects in this document set, two of them (`AD-4`'s false `[ADOPTED]` clause, this pass's "Moot" reversal) being *wrong conclusions recorded by the preceding pass*. The documents are now materially more accurate than the tree's prior state, but the rate of findings has not yet fallen, so "verified" should be read as "verified by this pass", not as clean.
- `troll/ARCHITECTURE.md` was edited for the first time in this story by the review pass itself, so its four corrections and its redrawn data-flow diagram have had no independent review.
- The nine deferred code defects now recorded against this story remain live. Three touch data integrity directly: the silent empty-top-of-book skip (unexplained permanent catalog gaps), `ranking_engine`'s fabricated `volume24h = 0` for Bybit and Hyperliquid (DATA-01/D-27), and `data_api`'s image not shipping `collector_core`/`common`.
- AD-3 now joins AD-4 in carrying an `[ADOPTED]` marker over a rule with an enumerated live violation. That is deliberate — changing what an AD requires is an architecture decision, not a docs update — but both markers mean "adopted with tracked deviations", not "clean".
