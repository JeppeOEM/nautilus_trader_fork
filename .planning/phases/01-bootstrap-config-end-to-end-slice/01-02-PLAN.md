---
phase: 01-bootstrap-config-end-to-end-slice
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/bybit_recorder/__init__.py
  - scripts/bybit_recorder/config.py
  - scripts/bybit_recorder/recorder.toml
autonomous: true
requirements: [CONF-01, CONF-02, REC-07]
must_haves:
  truths:
    - "Operator can list linear-USDT + spot instruments with per-instrument depth and bar_intervals in a TOML file and the loader returns them as structured config"
    - "Loading a TOML with a malformed instrument id fails fast"
    - "A StreamingConfig built by the recorder uses SCHEDULED_DATES daily UTC rotation and include_types=[TradeTick] (no custom writer)"
    - "D-02: conversion_interval_minutes is a configurable [recorder] field (PositiveInt, default 60), not hardcoded, so it can be tuned per environment"
  artifacts:
    - path: "scripts/bybit_recorder/config.py"
      provides: "RecorderConfig + InstrumentEntry dataclasses, load_recorder_config(), build_streaming_config()"
      exports: ["RecorderConfig", "InstrumentEntry", "load_recorder_config", "build_streaming_config"]
      min_lines: 60
    - path: "scripts/bybit_recorder/recorder.toml"
      provides: "Example/default config matching the D-07/D-08 schema"
      contains: "[[instruments.linear]]"
  key_links:
    - from: "scripts/bybit_recorder/config.py"
      to: "nautilus_trader.model.identifiers.InstrumentId"
      via: "InstrumentId.from_str on each entry id"
      pattern: "InstrumentId\\.from_str"
    - from: "scripts/bybit_recorder/config.py"
      to: "nautilus_trader.persistence.config.StreamingConfig"
      via: "build_streaming_config returns StreamingConfig with SCHEDULED_DATES"
      pattern: "RotationMode\\.SCHEDULED_DATES"
---

<objective>
Build the recorder's config layer: stdlib `tomllib` loader + frozen dataclasses for the `[recorder]` table and `[[instruments.linear]]`/`[[instruments.spot]]` array-of-tables (D-07, D-08), plus a `build_streaming_config()` helper that produces the daily-rotating `StreamingConfig` (REC-07 + Pitfall 1). Turns the `test_recorder_config.py` tests from Plan 01 GREEN.

Purpose: This is the first real vertical-slice increment — after this plan an operator can author a `recorder.toml` and the loader turns it into the exact `InstrumentId` list and `StreamingConfig` the node will consume. The streaming config carries the load-bearing daily-rotation decision that makes the catalog day-partitioned.
Output: `scripts/bybit_recorder/__init__.py`, `config.py`, and an example `recorder.toml`.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-RESEARCH.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/SKELETON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config dataclasses + tomllib loader (CONF-01, CONF-02)</name>
  <files>scripts/bybit_recorder/__init__.py, scripts/bybit_recorder/config.py</files>
  <behavior>
    - load_recorder_config(path) parses the [recorder] table into a RecorderConfig and the instrument arrays into ordered InstrumentEntry objects (linear first, then spot), and exposes the resolved list[InstrumentId] (linear then spot order).
    - Each InstrumentEntry carries id (InstrumentId), depth (int), bar_intervals (list[str]) — depth/bar_intervals parsed now per CONF-02 but not consumed in Phase 1.
    - A malformed instrument id (e.g. "not a valid id") raises during load (InstrumentId.from_str boundary, V5).
    - conversion_interval_minutes and depth use a positive-int type so <=0 is rejected.
  </behavior>
  <read_first>
    - scripts/bybit_recorder/__init__.py (will be created — confirm not pre-existing)
    - nautilus_trader/persistence/config.py (StreamingConfig frozen-config field idiom, lines 62-73)
    - nautilus_trader/common/config.py (PositiveInt usage and InstrumentProviderConfig, ~lines 441-483)
    - nautilus_trader/model/identifiers.pyx or .pyi (InstrumentId.from_str behavior)
    - tests/unit_tests/persistence/recorder/test_recorder_config.py (the RED tests this task must turn GREEN)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md (config.py analog + TOML loader pattern, lines 84-115)
  </read_first>
  <action>
    Create an empty `__init__.py` for the `scripts/bybit_recorder/` package and `config.py` (14-line repo copyright header). In `config.py`:
    - Define `InstrumentEntry` (frozen dataclass / NautilusConfig frozen) with fields `id: InstrumentId`, `depth: int`, `bar_intervals: list[str]`. Use `PositiveInt` (from `nautilus_trader.common.config`) for `depth` to get >0 validation for free (Security V5).
    - Define `RecorderConfig` (frozen) with fields `trader_id: str`, `catalog_path: str`, `streaming_path: str`, `conversion_interval_minutes: PositiveInt = 60`, `environment: str = "mainnet"`, and `instruments: list[InstrumentEntry]`. Per CONTEXT discretion, the exact additional field names are yours, but these five `[recorder]` keys (D-08) plus the instruments list MUST be present.
    - Implement `load_recorder_config(path: str | Path) -> RecorderConfig`: open in binary mode (`tomllib` requires it), `tomllib.load`, read the `recorder` table (raise `KeyError`/clear error if missing), read `raw.get("instruments", {}).get("linear", [])` and `...get("spot", [])` as `list[dict]`, build `InstrumentEntry` per row calling `InstrumentId.from_str(e["id"])` (this raises on bad ids — the V5 boundary), linear entries before spot entries to preserve a deterministic order.
    - Expose a property or helper `instrument_ids` returning `list[InstrumentId]` in linear-then-spot order for the node's `InstrumentProviderConfig(load_ids=...)` and the strategy config.
    - Module logger `logger = logging.getLogger(__name__)`; log a one-line summary (instrument count, NOT credentials — D-09/V7) before returning.
    Do NOT read `api_key`/`api_secret` from TOML (D-09). Do NOT build the node or strategy here.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -k "load_recorder_config" -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -k "load_recorder_config" -x -q` passes (the CONF-01/CONF-02 + malformed-id tests are GREEN).
    - `load_recorder_config` on the example config returns instrument ids equal to `[InstrumentId.from_str("BTCUSDT-LINEAR.BYBIT"), InstrumentId.from_str("ETHUSDT-SPOT.BYBIT")]` in that order.
    - Each parsed instrument entry exposes `depth == 50` and `bar_intervals == ["1-MINUTE"]`.
    - A TOML with a malformed `id` raises (no silent skip).
    - `grep -c "api_key\|api_secret" scripts/bybit_recorder/config.py` (filtering comment lines) returns 0.
  </acceptance_criteria>
  <done>config.py loads the TOML schema into typed config, resolves the instrument-id list, validates ids, and the load_recorder_config tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_streaming_config helper + example recorder.toml (REC-07)</name>
  <files>scripts/bybit_recorder/config.py, scripts/bybit_recorder/recorder.toml</files>
  <behavior>
    - build_streaming_config(recorder_cfg) returns a StreamingConfig with catalog_path set to the recorder's single shared root, fs_protocol="file", rotation_mode=RotationMode.SCHEDULED_DATES, rotation_interval=pd.Timedelta(days=1), rotation_time=time(0,0,0), rotation_timezone="UTC", include_types=[TradeTick].
    - The example recorder.toml parses cleanly via load_recorder_config and yields the two expected instrument ids.
  </behavior>
  <read_first>
    - scripts/bybit_recorder/config.py (the dataclasses + loader from Task 1)
    - nautilus_trader/persistence/config.py (StreamingConfig fields 62-73, confirmed via source)
    - nautilus_trader/persistence/writer.py (RotationMode enum ~lines 50-54)
    - nautilus_trader/model/data (TradeTick import path)
    - tests/unit_tests/persistence/recorder/test_recorder_config.py (the build_streaming_config RED test)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md (Shared Pattern: Streaming enablement, lines 242-261)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-CONTEXT.md (the verbatim TOML block, lines 84-101)
  </read_first>
  <action>
    Add `build_streaming_config(recorder_cfg: RecorderConfig) -> StreamingConfig` to `config.py`. Set `catalog_path` to the recorder's single shared streaming/catalog root so the conversion `ParquetDataCatalog` later finds feather files under `{root}/live/{instance_id}/` (Pitfall 5 / A4 — reconcile `streaming_path` and `catalog_path` to one root; document in a comment WHY they must share a root). Set `fs_protocol="file"`, `rotation_mode=RotationMode.SCHEDULED_DATES`, `rotation_interval=pd.Timedelta(days=1)`, `rotation_time=time(0, 0, 0)`, `rotation_timezone="UTC"`, `include_types=[TradeTick]` (Phase 1 records only trades; widened in Phase 2). Add a `# WHY: day-partitioning is a consequence of daily feather rotation, not a free catalog property (Pitfall 1)` comment.

    Create `scripts/bybit_recorder/recorder.toml` exactly matching the CONTEXT `<specifics>` shape: `[recorder]` with `trader_id="BYBIT-COLLECTOR-001"`, `catalog_path`, `streaming_path`, `conversion_interval_minutes=60`, `environment="mainnet"`; one `[[instruments.linear]]` (`BTCUSDT-LINEAR.BYBIT`, depth 50, `["1-MINUTE"]`) and one `[[instruments.spot]]` (`ETHUSDT-SPOT.BYBIT`, depth 50, `["1-MINUTE"]`). NO `api_key`/`api_secret` keys (D-09). Add a top-of-file comment that credentials come from env vars only.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -x -q` is fully GREEN (all config tests pass, including the SCHEDULED_DATES/daily/UTC/include_types assertions).
    - `build_streaming_config(...)` returns a `StreamingConfig` where `rotation_mode == RotationMode.SCHEDULED_DATES`, `rotation_interval == pd.Timedelta(days=1)`, `rotation_timezone == "UTC"`, and `include_types == [TradeTick]`.
    - `load_recorder_config("scripts/bybit_recorder/recorder.toml")` succeeds and returns the two expected instrument ids.
    - `recorder.toml` contains `[[instruments.linear]]` and `[[instruments.spot]]` and contains no `api_key`/`api_secret` keys.
  </acceptance_criteria>
  <done>build_streaming_config produces the daily-rotating StreamingConfig; the example TOML loads cleanly; all test_recorder_config.py tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML file → recorder config | Operator-supplied instrument ids, paths, and intervals parsed into config |
| recorder config → filesystem | `catalog_path`/`streaming_path` resolve to write locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | TOML instrument id / interval parsing | mitigate | `InstrumentId.from_str` rejects malformed ids; `PositiveInt` rejects non-positive `depth`/`conversion_interval_minutes` (V5) |
| T-01-04 | Information Disclosure | Credentials in config/logs | mitigate | D-09 — no `api_key`/`api_secret` keys read from TOML; log only instrument count, never secrets (V7); grep gate asserts absence |
| T-01-05 | Tampering | `catalog_path`/`streaming_path` traversal | accept | Phase 1 trusts the local operator-authored TOML; single shared root is operator-controlled (V12 — bounding deferred, low risk for a single-user local recorder) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No installs — stdlib `tomllib` + in-repo `nautilus_trader` only (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -x -q` fully GREEN.
- `python -c "from scripts.bybit_recorder.config import load_recorder_config, build_streaming_config"` imports cleanly.
</verification>

<success_criteria>
- An operator can author `recorder.toml` listing linear + spot instruments with per-instrument depth and bar_intervals (CONF-01, CONF-02).
- The loader resolves the instrument-id list and rejects malformed ids fail-fast.
- `build_streaming_config` produces a daily-rotating `StreamingConfig` with `include_types=[TradeTick]` and no custom writer (REC-07 / Pitfall 1).
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-config-end-to-end-slice/01-02-SUMMARY.md` when done.
</output>
