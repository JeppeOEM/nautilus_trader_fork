---
phase: 01-bootstrap-config-end-to-end-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit_tests/persistence/recorder/__init__.py
  - tests/unit_tests/persistence/recorder/conftest.py
  - tests/unit_tests/persistence/recorder/test_recorder_config.py
  - tests/unit_tests/persistence/recorder/test_recorder_strategy.py
  - tests/unit_tests/persistence/recorder/test_recorder_conversion.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, REC-01, REC-07, REL-01]
must_haves:
  truths:
    - "Running pytest on the recorder test dir collects 4 test files with no import errors"
    - "Every Wave-0 test fails for the right reason (missing recorder module / unmet behavior), not collection error"
    - "Fixtures provide a temp catalog dir, sample TOML text, a mock cache, and sample TradeTicks"
  artifacts:
    - path: "tests/unit_tests/persistence/recorder/conftest.py"
      provides: "Shared pytest fixtures: temp catalog dir, sample TOML, mock cache, sample TradeTicks"
      min_lines: 30
    - path: "tests/unit_tests/persistence/recorder/test_recorder_config.py"
      provides: "RED unit tests for CONF-01/CONF-02 + StreamingConfig wiring (REC-07)"
      contains: "def test_"
    - path: "tests/unit_tests/persistence/recorder/test_recorder_strategy.py"
      provides: "RED unit tests for CONF-03 (raise-all-missing) + REC-01 (subscribe per instrument)"
      contains: "def test_"
    - path: "tests/unit_tests/persistence/recorder/test_recorder_conversion.py"
      provides: "RED integration test for REL-01 feather->parquet->reload"
      contains: "convert_stream_to_data"
  key_links:
    - from: "tests/unit_tests/persistence/recorder/conftest.py"
      to: "nautilus_trader.test_kit.stubs.data.TestDataStubs"
      via: "import for sample TradeTicks"
      pattern: "TestDataStubs|TestInstrumentProvider"
---

<objective>
Create the Wave-0 test scaffold for the Bybit recorder: a dedicated test package with shared fixtures and three test files (config, strategy, conversion) that encode the phase's required behaviors as failing (RED) tests. This closes the Nyquist gap flagged in 01-VALIDATION.md — every later task's `<automated>` verify command points at one of these files.

Purpose: Without these test files, later tasks have no automated verification target. Writing the tests first (RED) locks the behavioral contract for CONF-01/02/03, REC-01/07, and REL-01 before any production code exists.
Output: `tests/unit_tests/persistence/recorder/` package with `__init__.py`, `conftest.py`, and three `test_*.py` files that fail cleanly (missing-module / unmet-behavior), not on collection.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-RESEARCH.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/SKELETON.md
</context>

<artifacts_this_phase_produces>
Symbols created across all of Phase 1 (excluded from cross-AI drift verification — these are NEW, not pre-existing):
- New package `scripts/bybit_recorder/` with modules `recorder.py`, `config.py`, `strategy.py`, and `recorder.toml`
- `RecorderConfig` dataclass (top-level `[recorder]` fields: `trader_id`, `catalog_path`, `streaming_path`, `conversion_interval_minutes`, `environment`)
- `InstrumentEntry` dataclass (per-instrument: `id`, `depth`, `bar_intervals`)
- `load_recorder_config(path)` function (TOML loader) returning the parsed config + instrument-id list
- `RecorderStrategyConfig` (frozen `StrategyConfig` subclass: `instrument_ids`, `catalog_path`, `instance_id_str`, `conversion_interval_minutes`)
- `RecorderStrategy(Strategy)` with `on_start`, `on_trade_tick`, `_convert_stream`
- `RECORDER_INSTANCE_ID` module constant (fixed RFC-4122 v4 UUID string)
- `build_streaming_config(...)` helper (returns `StreamingConfig` with `SCHEDULED_DATES` daily rotation)
- New test package `tests/unit_tests/persistence/recorder/` (`__init__.py`, `conftest.py`, `test_recorder_config.py`, `test_recorder_strategy.py`, `test_recorder_conversion.py`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create recorder test package and shared fixtures</name>
  <files>tests/unit_tests/persistence/recorder/__init__.py, tests/unit_tests/persistence/recorder/conftest.py</files>
  <read_first>
    - tests/unit_tests/persistence/conftest.py (existing tmp_path catalog fixture style — `setup_catalog`, `fixture_catalog`)
    - tests/unit_tests/persistence/test_streaming.py (imports of TestDataStubs / TestInstrumentProvider and StreamingFeatherWriter round-trip)
    - nautilus_trader/test_kit/stubs/data.py (TestDataStubs.trade_tick signature for building sample TradeTicks)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md (conftest analog section, lines on fixtures)
  </read_first>
  <action>
    Create an empty `__init__.py` (matching the package-marker convention of sibling test dirs). Create `conftest.py` with a 14-line repo copyright header (copy the header block verbatim from any existing file under tests/unit_tests/persistence/). Provide these `@pytest.fixture`s:
    - `catalog_dir` (uses `tmp_path`) returning a `Path` to a temp catalog root used as BOTH the streaming root and the conversion catalog root (Pitfall 5 — single shared root).
    - `sample_toml` returning a TOML string mirroring the CONTEXT `<specifics>` block: a `[recorder]` table with `trader_id="BYBIT-COLLECTOR-001"`, `catalog_path`, `streaming_path`, `conversion_interval_minutes=60`, `environment="mainnet"`, plus one `[[instruments.linear]]` entry (`id="BTCUSDT-LINEAR.BYBIT"`, `depth=50`, `bar_intervals=["1-MINUTE"]`) and one `[[instruments.spot]]` entry (`id="ETHUSDT-SPOT.BYBIT"`, `depth=50`, `bar_intervals=["1-MINUTE"]`).
    - `mock_cache` (use `pytest-mock`'s `mocker`) returning a mock whose `.instrument(instrument_id)` is configurable per-test to return either an instrument stub or `None`.
    - `sample_trade_ticks` returning a short list of `TradeTick` objects built via `TestDataStubs` with strictly monotonically increasing `ts_init` (required by the catalog's monotonic-ts_init contract noted in STATE.md).
    Do not implement recorder production logic here — fixtures only.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit_tests/persistence/recorder/ --collect-only -q` runs without an `ImportError`/`CollectError` originating from `conftest.py`.
    - `conftest.py` defines fixtures named `catalog_dir`, `sample_toml`, `mock_cache`, and `sample_trade_ticks` (grep: `grep -E "def (catalog_dir|sample_toml|mock_cache|sample_trade_ticks)" conftest.py` returns 4 matches, filtering out comment lines).
    - `sample_toml` text contains both `[[instruments.linear]]` and `[[instruments.spot]]`.
    - No production recorder module is imported (the recorder package does not exist yet).
  </acceptance_criteria>
  <done>Test package exists; pytest collects the directory with no conftest-level errors; all four fixtures are defined.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED tests for config, strategy, and conversion behaviors</name>
  <files>tests/unit_tests/persistence/recorder/test_recorder_config.py, tests/unit_tests/persistence/recorder/test_recorder_strategy.py, tests/unit_tests/persistence/recorder/test_recorder_conversion.py</files>
  <read_first>
    - tests/unit_tests/persistence/recorder/conftest.py (the fixtures created in Task 1)
    - tests/unit_tests/trading/test_config.py (frozen-config construction/assertion style)
    - tests/acceptance_tests/test_backtest.py (StreamingConfig instantiation ~line 2603 and convert_stream_to_data + reload flow ~line 2715)
    - nautilus_trader/persistence/catalog/parquet.py (convert_stream_to_data signature, lines 2523-2560; idempotency/disjoint check 2602-2612)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-RESEARCH.md (Test Map and Wave 0 Gaps sections)
  </read_first>
  <action>
    Write three RED test files (14-line copyright header each, `test_{function}_{scenario}_{expected}` naming, AAA, one logical assertion per test). Import the not-yet-existing recorder modules from `scripts.bybit_recorder.config` and `scripts.bybit_recorder.strategy` so the tests fail on missing module / unmet behavior (NOT on collection — guard imports so collection still succeeds; e.g. import inside the test body or mark `pytest.importorskip` is NOT acceptable, instead let the import raise inside the test).

    `test_recorder_config.py` (CONF-01, CONF-02, REC-07):
    - `test_load_recorder_config_parses_linear_and_spot_instrument_ids`: write `sample_toml` to a temp file, call `load_recorder_config`, assert returned instrument-id list equals `[InstrumentId.from_str("BTCUSDT-LINEAR.BYBIT"), InstrumentId.from_str("ETHUSDT-SPOT.BYBIT")]`.
    - `test_load_recorder_config_parses_depth_and_bar_intervals`: assert each parsed instrument entry carries `depth=50` and `bar_intervals=["1-MINUTE"]` (CONF-02 — fields present even though unused in Phase 1).
    - `test_build_streaming_config_uses_scheduled_dates_daily_rotation`: assert the produced `StreamingConfig` has `rotation_mode == RotationMode.SCHEDULED_DATES`, `rotation_interval == pd.Timedelta(days=1)`, `rotation_timezone == "UTC"`, and `include_types == [TradeTick]` (REC-07 + Pitfall 1).
    - `test_load_recorder_config_rejects_malformed_instrument_id`: a TOML with `id="not a valid id"` raises on load (V5 input-validation boundary via `InstrumentId.from_str`).

    `test_recorder_strategy.py` (CONF-03, REC-01):
    - `test_on_start_raises_listing_all_missing_instruments`: configure `mock_cache.instrument` to return `None` for two configured ids; assert `on_start` raises `RuntimeError` whose message contains BOTH missing id strings (D-06 — use `pytest.raises(RuntimeError, match=...)` with a pattern matching both ids).
    - `test_on_start_subscribes_trade_ticks_per_instrument`: configure `mock_cache.instrument` to return a stub for all ids; spy on `subscribe_trade_ticks`; assert it is called once per configured instrument (REC-01).
    - `test_on_start_sets_conversion_timer`: assert `clock.set_timer` is called once with `name="convert-stream"` and an interval derived from `conversion_interval_minutes` (D-01).

    `test_recorder_conversion.py` (REL-01, success criterion 5):
    - `test_convert_stream_to_data_roundtrips_trade_ticks`: write `sample_trade_ticks` to a feather stream under `{catalog_dir}/live/{instance_id}/`, call `ParquetDataCatalog(catalog_dir).convert_stream_to_data(instance_id, TradeTick, subdirectory="live")`, then assert `catalog.trade_ticks(instrument_ids=[...])` returns a non-empty list where `all(isinstance(t, TradeTick))`. Use a hardcoded valid v4 UUID string for `instance_id`.
    - Add a `@pytest.mark.skip(reason="A2 — empirical: verified in plan 04")` placeholder test `test_double_conversion_same_day_behavior` documenting the partial-day re-conversion question (Pitfall 2 / A2) so the contract is recorded but not yet asserted.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/ -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/unit_tests/persistence/recorder/ --collect-only -q` succeeds (no collection errors) — every test function is discovered.
    - Running the suite, the config and strategy tests FAIL (RED) with `ModuleNotFoundError`/`ImportError` for `scripts.bybit_recorder.*` or with an assertion/attribute error — NOT with a collection error.
    - `grep -E "def test_" test_recorder_config.py` returns >= 4 (filtering comment lines); `test_recorder_strategy.py` >= 3; `test_recorder_conversion.py` >= 1 active test.
    - `test_recorder_strategy.py` contains a `pytest.raises(RuntimeError` for the missing-instruments case and references both `BTCUSDT-LINEAR.BYBIT` and `ETHUSDT-SPOT.BYBIT` (or two distinct configured ids) in the match.
    - `test_recorder_conversion.py` calls `convert_stream_to_data(` with `subdirectory="live"`.
  </acceptance_criteria>
  <done>Three RED test files exist, collect cleanly, and fail for the intended reasons (missing recorder modules / unmet behaviors), establishing the behavioral contract for Plans 02-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML file → recorder process | Operator-supplied config text is parsed and turned into instrument ids and paths (untrusted text crosses here) |
| Bybit WS → DataEngine | External market data crosses into the process (handled by adapter; out of scope for tests) |
| recorder process → local filesystem | Feather + parquet writes under the configured catalog root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | TOML instrument id parsing | mitigate | `test_load_recorder_config_rejects_malformed_instrument_id` asserts `InstrumentId.from_str` rejects bad ids (V5 boundary) |
| T-01-02 | Repudiation | Missing-instrument validation | mitigate | `test_on_start_raises_listing_all_missing_instruments` asserts a raise (not silent stop) so failures are surfaced (V7) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: N/A — stdlib + in-repo workspace only) |
</threat_model>

<verification>
- `python -m pytest tests/unit_tests/persistence/recorder/ --collect-only -q` collects all test functions with no errors.
- Full recorder suite runs RED (production modules do not exist yet) without any collection-time failures.
</verification>

<success_criteria>
- A new `tests/unit_tests/persistence/recorder/` package exists with `__init__.py`, `conftest.py`, and three `test_*.py` files.
- Fixtures `catalog_dir`, `sample_toml`, `mock_cache`, `sample_trade_ticks` are defined and importable.
- All tests collect cleanly; config/strategy/conversion tests fail RED for the right reasons, ready to be turned GREEN by Plans 02-04.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-config-end-to-end-slice/01-01-SUMMARY.md` when done.
</output>
