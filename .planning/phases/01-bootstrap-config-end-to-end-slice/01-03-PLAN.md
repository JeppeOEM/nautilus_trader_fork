---
phase: 01-bootstrap-config-end-to-end-slice
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/bybit_recorder/strategy.py
  - scripts/bybit_recorder/recorder.py
autonomous: true
requirements: [CONF-03, REC-01, REC-07, REL-01]
must_haves:
  truths:
    - "On startup the strategy validates every configured instrument against the cache and raises a single error listing ALL missing ids"
    - "When all instruments are present the strategy subscribes to trade ticks for each one"
    - "The strategy schedules an in-process conversion timer that calls convert_stream_to_data with subdirectory=live"
    - "The entrypoint builds a TradingNode with streaming enabled and a fixed UUID4 instance_id shared with the strategy"
  artifacts:
    - path: "scripts/bybit_recorder/strategy.py"
      provides: "RecorderStrategy + RecorderStrategyConfig (validation, subscribe, timer-driven conversion)"
      exports: ["RecorderStrategy", "RecorderStrategyConfig"]
      min_lines: 50
    - path: "scripts/bybit_recorder/recorder.py"
      provides: "main entrypoint: load TOML, build TradingNodeConfig with streaming + fixed instance_id, run node"
      contains: "RECORDER_INSTANCE_ID"
  key_links:
    - from: "scripts/bybit_recorder/strategy.py"
      to: "self.cache.instrument"
      via: "on_start validation collecting all misses then raising"
      pattern: "cache\\.instrument"
    - from: "scripts/bybit_recorder/strategy.py"
      to: "ParquetDataCatalog.convert_stream_to_data"
      via: "timer callback _convert_stream"
      pattern: "convert_stream_to_data"
    - from: "scripts/bybit_recorder/recorder.py"
      to: "nautilus_trader.persistence.config.StreamingConfig"
      via: "TradingNodeConfig.streaming = build_streaming_config(...)"
      pattern: "streaming="
---

<objective>
Build the recorder's strategy and entrypoint — the core vertical slice that turns config into a running node: fail-fast instrument validation (CONF-03), trade-tick subscription (REC-01), native streaming wiring on the node (REC-07), and an in-process timer that drives feather→parquet conversion (REL-01). Turns the `test_recorder_strategy.py` tests from Plan 01 GREEN.

Purpose: After this plan a single command builds a real `TradingNode` that connects to Bybit, validates instruments, records trades to feather, and schedules conversion — the full walking-skeleton pipeline minus the empirical conversion/E2E verification (Plan 04). The fixed UUID4 `instance_id` (D-03) and `subdirectory="live"` (Pitfall 3) are the load-bearing wiring this plan must get exactly right.
Output: `scripts/bybit_recorder/strategy.py` and `scripts/bybit_recorder/recorder.py`.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-RESEARCH.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/SKELETON.md
@scripts/bybit_recorder/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecorderStrategy — validate, subscribe, schedule conversion (CONF-03, REC-01, REL-01)</name>
  <files>scripts/bybit_recorder/strategy.py</files>
  <behavior>
    - on_start collects every configured instrument id for which self.cache.instrument(id) is None, and if the collected list is non-empty raises RuntimeError whose message lists ALL missing ids in one string (D-05/D-06) — it does NOT call self.stop().
    - When all instruments are present, on_start calls self.subscribe_trade_ticks(id) once per configured instrument (REC-01).
    - on_start calls self.clock.set_timer(name="convert-stream", interval=pd.Timedelta(minutes=conversion_interval_minutes), callback=self._convert_stream) (D-01).
    - _convert_stream(event) constructs ParquetDataCatalog(catalog_path) and calls convert_stream_to_data(instance_id=<fixed uuid str>, data_cls=TradeTick, subdirectory="live").
    - on_trade_tick is a no-op/debug-log handler (trades auto-flow to the StreamingFeatherWriter via the kernel's "*" subscription — no manual write).
  </behavior>
  <read_first>
    - scripts/bybit_recorder/config.py (RecorderConfig, instrument_ids, build_streaming_config from Plan 02)
    - nautilus_trader/examples/strategies/ema_cross.py (StrategyConfig subclass + on_start + cache.instrument pattern, lines 46-158)
    - nautilus_trader/test_kit/strategies/tester_data.py (per-instrument subscribe loop, lines 160-172)
    - nautilus_trader/trading/strategy.pyx (real set_timer call shape, lines ~1802-1810)
    - nautilus_trader/common/component.pyx (set_timer signature/docs, lines 419-455)
    - nautilus_trader/persistence/catalog/parquet.py (convert_stream_to_data signature 2523-2531; idempotency/disjoint behavior 2602-2612)
    - nautilus_trader/common/actor.pyx (subscribe_trade_ticks 1722, on_trade_tick 479)
    - tests/unit_tests/persistence/recorder/test_recorder_strategy.py (the RED tests to turn GREEN)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md (strategy.py section, lines 119-173 — note the explicit deviation: raise, NOT stop())
  </read_first>
  <action>
    Create `strategy.py` (14-line copyright header). Define `RecorderStrategyConfig(StrategyConfig, frozen=True)` with fields `instrument_ids: list[InstrumentId]`, `catalog_path: str`, `instance_id_str: str` (the fixed UUID4 string, same value the node uses — D-03), `conversion_interval_minutes: PositiveInt = 60`. Define `RecorderStrategy(Strategy)`:
    - `on_start`: build `missing: list[str]`, loop configured ids, append `str(id)` when `self.cache.instrument(id) is None`; if `missing` raise `RuntimeError(f"Missing instruments: {', '.join(sorted(missing))}")` (D-06 — collect ALL then raise; do NOT `self.stop()` — this deviates from the ema_cross/options-collector analogs which use stop(), and the deviation is intentional per D-05 so the process exits non-zero). After the validation passes, loop ids calling `self.subscribe_trade_ticks(id)` (REC-01), then `self.clock.set_timer(name="convert-stream", interval=pd.Timedelta(minutes=self.config.conversion_interval_minutes), callback=self._convert_stream)` (D-01).
    - `_convert_stream(self, event: TimeEvent) -> None`: `catalog = ParquetDataCatalog(self.config.catalog_path)` then `catalog.convert_stream_to_data(instance_id=self.config.instance_id_str, data_cls=TradeTick, subdirectory="live")`. Add `# WHY: subdirectory default is "backtest"; live runs write under live/ (Pitfall 3)` comment. Wrap the conversion in a try/except that logs the error and (for now) re-raises NOT — log and continue so a transient conversion error does not kill the recorder; record a note that Pitfall 2 (non-disjoint intervals on mid-day re-conversion) is empirically verified in Plan 04.
    - `on_trade_tick(self, tick: TradeTick) -> None`: debug-log only (no manual persistence — REC-07 auto-flow).
    Module logger via `logging.getLogger(__name__)`; never log credentials (V7/D-09).
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/test_recorder_strategy.py -x -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit_tests/persistence/recorder/test_recorder_strategy.py -x -q` is fully GREEN.
    - With two configured ids both absent from the cache, `on_start` raises `RuntimeError` whose message contains BOTH id strings (assert via the RED test's `pytest.raises(... match=...)`).
    - With all ids present, `subscribe_trade_ticks` is called exactly once per configured instrument.
    - `clock.set_timer` is called once with `name="convert-stream"`.
    - `grep -c "self.stop()" scripts/bybit_recorder/strategy.py` (filtering comments) returns 0 in the validation path (the strategy raises, never silently stops on missing instruments).
    - `_convert_stream` calls `convert_stream_to_data(` with `subdirectory="live"`.
  </acceptance_criteria>
  <done>RecorderStrategy validates fail-fast (raise-all-missing), subscribes per instrument, and schedules the conversion timer; strategy tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: recorder.py entrypoint — build node with streaming + fixed instance_id (REC-07, REL-01 wiring)</name>
  <files>scripts/bybit_recorder/recorder.py</files>
  <read_first>
    - scripts/bybit_recorder/config.py (load_recorder_config, build_streaming_config, instrument_ids)
    - scripts/bybit_recorder/strategy.py (RecorderStrategy, RecorderStrategyConfig from Task 1)
    - examples/live/bybit/bybit_data_tester.py (verified node bootstrap template, lines 17-106 — TradingNodeConfig → TradingNode → add_strategy → add_data_client_factory → build → run)
    - nautilus_trader/core/uuid.pyx (UUID4.from_str v4 RFC-4122 requirement, lines 75-103)
    - nautilus_trader/adapters/bybit/config.py (BybitDataClientConfig: environment, product_types, env-var credentials, lines 34-90)
    - nautilus_trader/system/kernel.py (streaming path derivation `{catalog_path}/live/{instance_id}`, lines 587-611)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md (recorder.py bootstrap section + Fixed instance_id shared pattern, lines 28-80 and 263-271)
  </read_first>
  <action>
    Create `recorder.py` (14-line copyright header). Define module constant `RECORDER_INSTANCE_ID = "<a valid RFC-4122 v4 UUID string>"` (generate one real v4 UUID, e.g. via `python -c "import uuid; print(uuid.uuid4())"`, and hardcode it; add `# WHY: must be a valid v4 UUID — UUID4.from_str rejects human labels (Pitfall 4); fixed so the feather dir is stable across restarts (D-03)`). Implement `main(config_path: str) -> None` and a `if __name__ == "__main__":` guard that reads the TOML path from `sys.argv[1]` (default to the package's `recorder.toml`):
    - `recorder_cfg = load_recorder_config(config_path)`; `ids = recorder_cfg.instrument_ids`.
    - `streaming = build_streaming_config(recorder_cfg)`.
    - Build `TradingNodeConfig(trader_id=TraderId(recorder_cfg.trader_id), instance_id=UUID4.from_str(RECORDER_INSTANCE_ID), logging=LoggingConfig(log_level="INFO", log_level_file="INFO", log_directory="logs", log_file_name="bybit_recorder", use_pyo3=True), streaming=streaming, data_clients={BYBIT: BybitDataClientConfig(environment=BybitEnvironment.MAINNET, product_types=(BybitProductType.LINEAR, BybitProductType.SPOT), instrument_provider=InstrumentProviderConfig(load_ids=frozenset(ids)))}, timeout_connection=20.0, timeout_disconnection=10.0, timeout_post_stop=1.0)`. (D-10 mainnet; D-07 both product types; D-09 — no credentials passed, adapter sources env vars.)
    - `strategy_cfg = RecorderStrategyConfig(instrument_ids=ids, catalog_path=recorder_cfg.catalog_path, instance_id_str=RECORDER_INSTANCE_ID, conversion_interval_minutes=recorder_cfg.conversion_interval_minutes)`. CRITICAL: `catalog_path` passed to the strategy MUST equal the `catalog_path` used in `build_streaming_config` so conversion finds feather under `{root}/live/{instance_id}/` (Pitfall 5 / A4).
    - `node = TradingNode(config=config_node)`; `node.trader.add_strategy(RecorderStrategy(config=strategy_cfg))`; `node.add_data_client_factory(BYBIT, BybitLiveDataClientFactory)`; `node.build()`; then `try: node.run() finally: node.dispose()`.
    Imports one-per-line, alphabetized (ruff isort). Do NOT add a custom parquet writer (REC-07 / anti-pattern).
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -c "import uuid; from scripts.bybit_recorder.recorder import RECORDER_INSTANCE_ID; assert uuid.UUID(RECORDER_INSTANCE_ID).version == 4, 'instance_id must be a v4 UUID'; print('OK', RECORDER_INSTANCE_ID)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import uuid; from scripts.bybit_recorder.recorder import RECORDER_INSTANCE_ID; assert uuid.UUID(RECORDER_INSTANCE_ID).version == 4"` exits 0 (the constant is a real v4 UUID, not a human label).
    - `recorder.py` imports cleanly: `python -c "import scripts.bybit_recorder.recorder"` exits 0.
    - The entrypoint sets `TradingNodeConfig.streaming` to the `build_streaming_config` result and passes the SAME `instance_id`/`catalog_path` to both the node and the strategy config (source assertion: `RECORDER_INSTANCE_ID` referenced for both `UUID4.from_str(...)` and `instance_id_str=`).
    - `BybitDataClientConfig` uses `environment=BybitEnvironment.MAINNET` and `product_types` including both LINEAR and SPOT; no `api_key`/`api_secret` literals appear (grep returns 0, filtering comments).
    - `node.run()` is wrapped in `try/finally` with `node.dispose()`.
  </acceptance_criteria>
  <done>recorder.py builds a TradingNode with native streaming enabled, a fixed v4 UUID instance_id shared with the strategy, mainnet Bybit data client (env-var creds), and runs the node; module imports cleanly and the UUID gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorder config → TradingNode | Parsed config drives node construction, instance_id, and streaming wiring |
| Bybit WS (mainnet) → DataEngine → StreamingFeatherWriter | External trade ticks cross into the process and are persisted |
| strategy timer → filesystem | Conversion writes parquet under the catalog root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Repudiation/Availability | Missing-instrument startup validation | mitigate | `on_start` raises (does not `self.stop()`) so a config error becomes a non-zero exit surfaced by systemd/journald (D-05/V7); strategy test asserts the raise |
| T-01-07 | Information Disclosure | Bybit credentials | mitigate | D-09 — no credentials in code/TOML; adapter sources env vars; logging never emits secrets (grep gate asserts no `api_key`/`api_secret` literals) (V7) |
| T-01-08 | Tampering | `subdirectory`/`instance_id` mismatch causing silent no-op conversion | mitigate | Fixed v4 UUID shared by node+strategy (D-03); `subdirectory="live"` hard-coded (Pitfall 3); shared catalog root (Pitfall 5) — verified empirically in Plan 04 |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No installs — in-repo `nautilus_trader` + stdlib only (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pytest tests/unit_tests/persistence/recorder/test_recorder_strategy.py -x -q` GREEN.
- `python -c "import scripts.bybit_recorder.recorder"` imports cleanly and the v4-UUID gate passes.
- Note: live node `run()` is exercised only as the E2E smoke in Plan 04 (requires Bybit connectivity).
</verification>

<success_criteria>
- The strategy validates every configured instrument and raises one error listing all missing ids (CONF-03/D-06).
- The strategy subscribes to trade ticks per instrument (REC-01) and schedules the in-process conversion timer (REL-01/D-01).
- The entrypoint builds a TradingNode with native streaming enabled (REC-07) and a fixed v4 UUID instance_id shared with the strategy (D-03), wired to mainnet Bybit with env-var credentials (D-09/D-10).
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-config-end-to-end-slice/01-03-SUMMARY.md` when done.
</output>
