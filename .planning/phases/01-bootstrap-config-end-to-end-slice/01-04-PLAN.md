---
phase: 01-bootstrap-config-end-to-end-slice
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - tests/unit_tests/persistence/recorder/test_recorder_conversion.py
autonomous: false
requirements: [REL-01]
must_haves:
  truths:
    - "Synthetic trade ticks written to feather convert to parquet and reload as native TradeTick objects (success criterion 5)"
    - "The behavior of converting the same UTC day twice (open/partial-day re-conversion) is empirically established and the strategy handles it without crashing (A2)"
    - "A missing-instrument config makes the live process exit non-zero (A3 / D-05)"
    - "A live ~2-minute run against BTCUSDT-LINEAR.BYBIT produces a catalog that reloads >=1 TradeTick"
  artifacts:
    - path: "tests/unit_tests/persistence/recorder/test_recorder_conversion.py"
      provides: "GREEN integration test for feather->parquet->reload, plus the now-active A2 double-conversion test"
      contains: "convert_stream_to_data"
  key_links:
    - from: "tests/unit_tests/persistence/recorder/test_recorder_conversion.py"
      to: "ParquetDataCatalog.trade_ticks"
      via: "reload assertion after conversion"
      pattern: "trade_ticks\\("
---

<objective>
Close the end-to-end proof: turn the conversion integration test GREEN (REL-01 + success criterion 5), empirically resolve the two open assumptions the research flagged as needing verification (A2 — partial-day re-conversion behavior; A3 — non-zero exit on `on_start` raise), and run the live Bybit smoke test that proves the full walking-skeleton pipeline (config → subscribe → feather → parquet → reload as native `TradeTick`).

Purpose: This plan converts "wired up" into "proven working." The two empirical tasks de-risk decisions D-04 (convert all available feather every run, incl. partial day) and D-05 (raise → non-zero exit) that the research could only assume. The live smoke is the walking-skeleton acceptance gate.
Output: GREEN `test_recorder_conversion.py` (incl. the A2 double-conversion test), documented A2/A3 findings, and a verified live recording.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-CONTEXT.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-RESEARCH.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/01-PATTERNS.md
@.planning/phases/01-bootstrap-config-end-to-end-slice/SKELETON.md
@scripts/bybit_recorder/strategy.py
@scripts/bybit_recorder/recorder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GREEN conversion round-trip + empirical A2 double-conversion test (REL-01)</name>
  <files>tests/unit_tests/persistence/recorder/test_recorder_conversion.py</files>
  <read_first>
    - tests/unit_tests/persistence/recorder/test_recorder_conversion.py (the RED test + skipped A2 placeholder from Plan 01)
    - tests/unit_tests/persistence/test_streaming.py (StreamingFeatherWriter setup + catalog round-trip imports, lines 22-56)
    - tests/acceptance_tests/test_backtest.py (convert_stream_to_data → reload flow, lines ~2715-2727)
    - nautilus_trader/persistence/catalog/parquet.py (convert_stream_to_data 2523-2560; _convert_feather_table_to_parquet idempotency "already exists, skipping write" 2602-2604 and non-disjoint ValueError 2608-2612)
    - scripts/bybit_recorder/strategy.py (_convert_stream — confirm it matches the test's conversion call shape and how it handles a raised ValueError)
  </read_first>
  <action>
    Make `test_convert_stream_to_data_roundtrips_trade_ticks` GREEN: using a temp catalog root, write `sample_trade_ticks` to a feather stream under `{catalog_dir}/live/{instance_id}/` via a `StreamingFeatherWriter` (mirror `test_streaming.py` setup), call `ParquetDataCatalog(catalog_dir).convert_stream_to_data(instance_id, TradeTick, subdirectory="live")`, then assert `catalog.trade_ticks(instrument_ids=[...])` returns a non-empty list with `all(isinstance(t, TradeTick))` (success criterion 5).

    Replace the skipped A2 placeholder with a REAL test `test_double_conversion_same_day_behavior`: write feather data for one UTC day, run `convert_stream_to_data` twice in a row WITHOUT rotating (simulating the open/partial-day file converted twice — Pitfall 2). Observe and ASSERT the actual behavior: either (a) the second call is idempotent ("already exists, skipping write" — no exception and the same row count), OR (b) it raises `ValueError` on non-disjoint intervals. Pick whichever the framework actually does and pin it with an assertion (use `pytest.raises(ValueError)` if it raises, else assert stable row count). Add a docstring recording the observed behavior and a `# A2 RESULT:` comment.

    If the observed behavior is (b) raise-on-second-conversion, confirm `scripts/bybit_recorder/strategy.py::_convert_stream` already catches/logs-and-continues so a mid-day re-conversion does not kill the recorder; if it does NOT, this task must update `_convert_stream` to catch `ValueError` from non-disjoint intervals, log a warning, and continue (do NOT re-raise) — keeping the recorder alive between daily rotations. Record the decision in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/test_recorder_conversion.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit_tests/persistence/recorder/test_recorder_conversion.py -x -q` is fully GREEN (no skips remaining for the A2 test).
    - The round-trip test reloads >=1 object and asserts `all(isinstance(t, TradeTick))`.
    - `test_double_conversion_same_day_behavior` is active (not skipped) and asserts a concrete behavior (either idempotent skip with stable count, or `pytest.raises(ValueError)`).
    - If the observed A2 behavior is a raise, `scripts/bybit_recorder/strategy.py::_convert_stream` contains a `ValueError` handler that logs and continues (grep: `except ValueError` present), so the live recorder survives mid-day re-conversion.
  </acceptance_criteria>
  <done>Conversion round-trip is GREEN (REL-01/criterion 5); A2 partial-day re-conversion behavior is empirically pinned by an active test and the strategy handles it without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Verify non-zero exit on missing-instrument raise (A3 / D-05)</name>
  <files>tests/unit_tests/persistence/recorder/test_recorder_conversion.py</files>
  <read_first>
    - scripts/bybit_recorder/recorder.py (main + node.run()/dispose() flow)
    - scripts/bybit_recorder/strategy.py (on_start raise path)
    - nautilus_trader/live/node.py (how TradingNode.run() surfaces an on_start exception to the caller / process)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-RESEARCH.md (A3 assumption + Pattern 3 note on exit-code propagation, lines 204-228, 404)
    - .planning/phases/01-bootstrap-config-end-to-end-slice/01-VALIDATION.md (Manual-Only Verifications row for non-zero exit)
  </read_first>
  <action>
    Add an exit-code verification. Preferred: a fast unit-level proof that `on_start` raising propagates out of the run path (e.g. a test asserting `RuntimeError` propagates from a driven `on_start` with a mocked cache returning `None`, OR a subprocess test that runs the entrypoint with a deliberately-invalid instrument id and asserts a non-zero return code WITHOUT requiring live Bybit connectivity — e.g. point at a config whose validation fails before any network call, or mock the data client).

    Because instrument validation happens in `on_start` AFTER the provider loads the cache (which needs the adapter), prefer the unit-level assertion that the raise is NOT swallowed into a clean `stop()`: assert that driving `on_start` with all-missing instruments raises `RuntimeError` and that the strategy never calls `self.stop()` on that path (re-use the strategy harness from `test_recorder_strategy.py`). Then document — in a `# A3 RESULT:` comment and the SUMMARY — whether a full live subprocess exit-code check is deferred to the human-verify checkpoint (Task 3) because it requires a real connection. If a credential-free subprocess exit-code check IS feasible (validation fails before network), implement it and assert `returncode != 0`.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest tests/unit_tests/persistence/recorder/test_recorder_conversion.py tests/unit_tests/persistence/recorder/test_recorder_strategy.py -k "exit or missing or raise" -q 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts that the missing-instrument path raises `RuntimeError` and the strategy does NOT call `self.stop()` on that path (A3 — raise, not clean stop).
    - The full recorder suite `pytest tests/unit_tests/persistence/recorder/ -q` is GREEN.
    - A `# A3 RESULT:` comment records whether exit-code propagation was proven at unit level, via subprocess, or deferred to the live checkpoint, with the reason.
  </acceptance_criteria>
  <done>A3 is resolved: the missing-instrument raise is proven to propagate (not collapse into a clean stop), and the resolution path (unit/subprocess/deferred) is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live E2E smoke — record real Bybit trades and reload from catalog</name>
  <what-built>
    The complete walking-skeleton pipeline: `python scripts/bybit_recorder/recorder.py scripts/bybit_recorder/recorder.toml` builds a TradingNode that connects to Bybit mainnet, validates the configured instruments, records trade ticks for BTCUSDT-LINEAR.BYBIT via the native StreamingFeatherWriter, and an in-process timer converts the feather stream into the day-partitioned ParquetDataCatalog — which reloads as native TradeTick objects.
  </what-built>
  <how-to-verify>
    1. Ensure the example `scripts/bybit_recorder/recorder.toml` lists `BTCUSDT-LINEAR.BYBIT` (a liquid instrument with continuous trade flow) and a short `conversion_interval_minutes` for a fast smoke (temporarily set it to 1 if needed). No API keys are required for public market data (D-10).
    2. Run: `python scripts/bybit_recorder/recorder.py scripts/bybit_recorder/recorder.toml` and let it run ~2 minutes so trades flow and at least one conversion timer fires. Confirm the logs show trade ticks arriving and a conversion running without a fatal error.
    3. Stop the process (Ctrl-C / SIGTERM).
    4. Confirm parquet files exist under `{catalog_root}/data/trade_tick/BTCUSDT-LINEAR.BYBIT/` (filenames are `{start_ts}_{end_ts}.parquet`).
    5. Reload and assert: `python -c "from nautilus_trader.persistence.catalog.parquet import ParquetDataCatalog; from nautilus_trader.model.data import TradeTick; c=ParquetDataCatalog('<catalog_root>'); ts=c.trade_ticks(instrument_ids=['BTCUSDT-LINEAR.BYBIT']); print(len(ts)); assert ts and all(isinstance(t, TradeTick) for t in ts)"` — expect a non-zero count and no assertion error.
    6. (A3 live confirmation, optional but recommended) Edit the TOML to include a bogus instrument id (e.g. `ZZZNONEXISTENT-LINEAR.BYBIT`), run the recorder, and confirm it exits non-zero with a log line `Missing instruments: ...ZZZNONEXISTENT...` (D-05/D-06). Restore the TOML afterward.
  </how-to-verify>
  <resume-signal>Type "approved" if a non-zero count of native TradeTick objects reloaded from the catalog (and, if checked, the bogus-instrument run exited non-zero). Otherwise describe what failed (no trades, conversion error, empty catalog, clean exit on bad instrument).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bybit WS (mainnet) → recorder | Live external trade ticks enter the process during the smoke run |
| recorder → local filesystem | Feather + parquet written under the catalog root |
| operator TOML → validation | A bogus instrument id is intentionally injected to prove fail-fast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Availability | Mid-day re-conversion (non-disjoint intervals) | mitigate | A2 task empirically pins the behavior and ensures `_convert_stream` survives a `ValueError` without killing the recorder (Pitfall 2) |
| T-01-10 | Repudiation/Availability | Silent data loss masked as clean exit | mitigate | A3 task + live checkpoint confirm a missing-instrument config exits non-zero so systemd/journald surface it (D-05/V7) |
| T-01-11 | Integrity | Conversion silently writing nothing (wrong subdirectory) | mitigate | Live checkpoint asserts a non-zero reloaded count from the actual catalog (Pitfall 3 — `subdirectory="live"` proven end-to-end) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No installs — in-repo `nautilus_trader` + stdlib only (RESEARCH audit N/A) |
</threat_model>

<verification>
- `pytest tests/unit_tests/persistence/recorder/ -q` fully GREEN (config + strategy + conversion, no skips).
- A2 and A3 behaviors empirically pinned by active tests + documented results.
- Live smoke: catalog reloads >=1 native `TradeTick`; bogus-instrument run exits non-zero.
</verification>

<success_criteria>
- Synthetic feather data converts to parquet and reloads as native `TradeTick` (REL-01 / criterion 5).
- The two research assumptions (A2 partial-day re-conversion, A3 non-zero exit) are resolved by tests and documented; the recorder survives mid-day re-conversion.
- The end-to-end live pipeline is verified by a human: real Bybit trades land in the day-partitioned catalog and reload as native objects.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-config-end-to-end-slice/01-04-SUMMARY.md` when done. Record the A2 and A3 findings explicitly.
</output>
