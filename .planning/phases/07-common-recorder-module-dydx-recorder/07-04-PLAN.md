---
phase: 07-common-recorder-module-dydx-recorder
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified: []
autonomous: false
requirements: [DYDX-03, DYDX-07]

must_haves:
  truths:
    - "The dYdX recorder connects to dYdX mainnet and records all 7 data types into the shared catalog"
    - "All 7 dYdX feeds (trade, quote, order_book_deltas, bar, mark, index, funding) land as parquet in catalog/streaming"
    - "A SIGTERM flush+convert produces no data loss (DYDX-07 reliability parity)"
  artifacts: []
  key_links:
    - from: "dydx_recorder (live)"
      to: "shared ParquetDataCatalog"
      via: "DydxDataClient WS → StreamingFeatherWriter → conversion timer"
      pattern: "catalog/streaming"
---

<objective>
Run the dYdX recorder against dYdX mainnet and verify all 7 perpetual data types land in the shared ParquetDataCatalog, plus a SIGTERM flush+convert loses no data — the live confirmation of DYDX-03 and DYDX-07. This mirrors the HOT-01 precedent: the live smoke is a manual checkpoint that may be deferred and run separately, because mainnet connectivity is a runtime dependency not available to autonomous execution (RESEARCH Environment Availability).

Purpose: Unit tests prove the wiring; only a live run proves the dYdX adapter actually streams all 7 feeds end-to-end into the catalog under real network conditions. Mark and index are expected to be identical on dYdX (both from oracle_price) but both must appear in the catalog (RESEARCH State of the Art).

Output: No code artifacts — a verification record (and any catalog inspection notes) captured in the SUMMARY.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-common-recorder-module-dydx-recorder/07-RESEARCH.md
@scripts/dydx_recorder/recorder.py
@scripts/dydx_recorder/recorder.toml
</context>

<phase_goal>
**As a** recorder operator, **I want to** confirm the dYdX recorder records all 7 data types from live mainnet into the shared catalog with no data loss on restart, **so that** I can trust it for 24/7 archival.
</phase_goal>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live dYdX mainnet smoke — all 7 feeds + SIGTERM flush</name>
  <files>scripts/dydx_recorder/recorder.toml (temporary smoke-only interval edits, reverted after)</files>
  <action>
    Prepare and run the live mainnet smoke. Before pausing for human verification, do the automatable setup: confirm the dYdX adapter imports (the `dydx OK mainnet` check in step 1 below), and temporarily lower conversion_interval_minutes and rotation_interval_minutes to 1 in scripts/dydx_recorder/recorder.toml so a rotation+conversion cycle completes within a short run (revert these to their committed values after the smoke). Then hand off to the operator to run the recorder against mainnet, observe the 7 feeds + heartbeats, send SIGTERM, and inspect the catalog per the how-to-verify steps. This is a human-verify checkpoint because live mainnet connectivity is a runtime dependency not available to autonomous execution (HOT-01 precedent) — it may be marked deferred.
  </action>
  <what-built>
    A runnable dYdX recorder (scripts/dydx_recorder/recorder.py + recorder.toml) that reuses the shared RecorderStrategy and wires DydxLiveDataClientFactory + DydxDataClientConfig + DydxNetwork. Unit tests already prove the config validation, the 7-feed subscribe, and funding dedup. This checkpoint is the LIVE confirmation against dYdX mainnet.
  </what-built>
  <how-to-verify>
    1. Confirm the dYdX adapter imports: `python -c "from nautilus_trader.adapters.dydx.factories import DydxLiveDataClientFactory; from nautilus_trader.core.nautilus_pyo3 import DydxNetwork; print('dydx OK', DydxNetwork.MAINNET.name)"` → expect `dydx OK mainnet`.
    2. Use a short conversion interval for the smoke: temporarily set `conversion_interval_minutes = 1` and `rotation_interval_minutes = 1` in scripts/dydx_recorder/recorder.toml so feather files rotate+convert within the run (revert after).
    3. Run the recorder against mainnet: `python -m scripts.dydx_recorder.recorder scripts/dydx_recorder/recorder.toml` (from repo root). No credentials needed — the dYdX data client is public.
    4. Let it run ~3-5 minutes so at least one bar closes and at least one rotation+conversion cycle fires. Watch the logs for INFO heartbeats and per-feed activity; confirm NO repeated "Stale stream: quote ..." WARNINGs (DYDX-05 relaxed quote threshold working).
    5. Send SIGTERM (Ctrl-C once / `kill -TERM <pid>`) and confirm the on_stop flush+convert runs cleanly (no traceback; DYDX-07).
    6. Inspect the catalog for all 7 data types under the shared root. Confirm parquet exists for trades, quotes, order_book_deltas, bars, mark prices, index prices, and funding rates for the configured dYdX perps. (Mark and index will be equal — expected, both from oracle_price.) You may load via `catalog.trade_ticks(...)`, `catalog.quote_ticks(...)`, etc., pointing at catalog/streaming.
    7. Confirm dYdX data co-exists with any Bybit data in the same catalog root without collision (venue suffix .DYDX vs .BYBIT).
  </how-to-verify>
  <verify>
    <human-check>All 7 dYdX feeds present as parquet in the shared catalog; clean SIGTERM flush+convert (no traceback); no spurious stale-quote WARNINGs. May be marked deferred per HOT-01 precedent if mainnet is unavailable.</human-check>
  </verify>
  <done>All 7 dYdX data types recorded from live mainnet into the shared catalog with a clean SIGTERM flush — or explicitly deferred (HOT-01 precedent) with the unit-verified wiring standing in.</done>
  <resume-signal>Type "approved" (all 7 feeds landed + clean SIGTERM flush) or describe what failed. If mainnet connectivity is unavailable, type "deferred" to mark this as a deferred live smoke (HOT-01 precedent) — the unit-verified wiring still satisfies DYDX-02/03/04/05/06 and the structural DYDX-07.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dYdX mainnet WS/Indexer → recorder | Live untrusted market data (public, no credentials) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Denial of Service | live feed goes quiet / disconnects | accept | Adapter-driven reconnect/resubscribe (REL-04 parity, zero custom code); heartbeat surfaces stale streams. Verified structurally; live run observes it. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- All 7 data types present as parquet in the shared catalog for the configured dYdX perps.
- Clean SIGTERM flush+convert (no traceback; no data loss).
- No spurious stale-quote WARNINGs (DYDX-05 relaxed threshold confirmed live).
- May be marked deferred per HOT-01 precedent if mainnet is unavailable.
</verification>

<success_criteria>
- The dYdX recorder records all 7 perpetual data types from live mainnet into the shared ParquetDataCatalog (DYDX-03 live confirmation).
- A SIGTERM flush+convert loses no data (DYDX-07 live confirmation).
- Or: explicitly deferred (HOT-01 precedent) with the unit-verified wiring standing in for the live run.
</success_criteria>

<artifacts_this_phase_produces>
- (no code artifacts) — live verification record captured in the SUMMARY
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-common-recorder-module-dydx-recorder/07-04-SUMMARY.md` when done
</output>
