---
phase: 07-common-recorder-module-dydx-recorder
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - scripts/dydx_recorder/recorder.py
  - tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py
autonomous: true
requirements: [DYDX-02, DYDX-03, DYDX-04, DYDX-07]

must_haves:
  truths:
    - "scripts/dydx_recorder/recorder.py builds a TradingNode with DydxDataClientConfig + DydxLiveDataClientFactory and DydxNetwork registered in CUSTOM_ENCODINGS"
    - "The dYdX recorder reuses the shared RecorderStrategy (no subclass) and injects the data client + dYdX config loader after build"
    - "The strategy subscribes all 7 dYdX feeds per instrument: trades, quotes, L2 deltas, bars, mark, index, funding"
    - "Funding dedup keys on (instrument_id, rate) and drops unchanged rates (DYDX-04) — verified against the shared strategy with dYdX instrument ids"
    - "All dYdX perps are treated as linear (mark/index/funding subscribed) per RESEARCH A1"
  artifacts:
    - path: "scripts/dydx_recorder/recorder.py"
      provides: "dYdX TradingNode wiring: CUSTOM_ENCODINGS[DydxNetwork], factory, strategy reuse, loader+client injection, main()"
      contains: "DydxLiveDataClientFactory"
    - path: "tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py"
      provides: "7-feed subscribe + funding dedup tests using dYdX instrument ids against the shared RecorderStrategy"
      contains: "DYDX"
  key_links:
    - from: "scripts/dydx_recorder/recorder.py"
      to: "scripts.common_recorder.strategy.RecorderStrategy"
      via: "instantiate + set_data_client + set_config_loader(load_dydx_recorder_config)"
      pattern: "RecorderStrategy"
    - from: "scripts/dydx_recorder/recorder.py"
      to: "DydxNetwork"
      via: "CUSTOM_ENCODINGS[DydxNetwork] = lambda v: v.name"
      pattern: "CUSTOM_ENCODINGS\\[DydxNetwork\\]"
    - from: "scripts/dydx_recorder/recorder.py"
      to: "DYDX data client registry"
      via: "node.kernel.data_engine._clients[ClientId(DYDX)]"
      pattern: "_clients\\[ClientId\\(DYDX\\)\\]"
---

<objective>
Wire scripts/dydx_recorder/recorder.py: register DydxNetwork in CUSTOM_ENCODINGS, build a TradingNode with DydxDataClientConfig + DydxLiveDataClientFactory and InstrumentProviderConfig(load_ids=...), reuse the shared RecorderStrategy (no subclass — RESEARCH anti-pattern), inject the live data client + the dYdX config loader after build, and run with raise_exception=True. Add a unit-test file proving the shared strategy subscribes all 7 dYdX feeds per instrument (DYDX-03) and that funding dedup keys on (instrument_id, rate) for dYdX ids (DYDX-04). This is the venue wiring that, with Plan 02's config, makes the dYdX recorder runnable; the 24/7 reliability parity (DYDX-07) is inherited from the shared strategy (flush+convert, heartbeat, restart-gap, adapter-driven reconnect).

Purpose: dYdX recording differs from Bybit ONLY in the factory/enum/config swap and the all-perps-are-linear mapping. The strategy is reused verbatim via composition. Mark and index on dYdX both derive from oracle_price (identical) but are recorded for parity per RESEARCH State of the Art.

Output: scripts/dydx_recorder/recorder.py; tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-common-recorder-module-dydx-recorder/07-RESEARCH.md
@scripts/bybit_recorder/recorder.py
@tests/unit_tests/persistence/recorder/test_recorder_strategy.py
@tests/unit_tests/persistence/recorder/conftest.py
</context>

<phase_goal>
**As a** recorder operator, **I want to** run a dYdX recorder that records all 7 perpetual data types into the shared catalog, **so that** dYdX market data is archived with the same reliability as Bybit without a forked codebase.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: dYdX recorder.py wiring (CUSTOM_ENCODINGS, factory, strategy reuse, injection)</name>
  <files>scripts/dydx_recorder/recorder.py</files>
  <read_first>
    - scripts/bybit_recorder/recorder.py (the mirror — copy its structure: CUSTOM_ENCODINGS registration at module level, main() building TradingNodeConfig with streaming + data_clients, node.build(), data-client injection via node.kernel.data_engine._clients[ClientId(...)], strategy.set_data_client + set_config_loader, node.run(raise_exception=True))
    - scripts/dydx_recorder/config.py (Plan 02 — import load_dydx_recorder_config, build_streaming_config, DydxRecorderConfig, _map_network)
    - scripts/common_recorder/strategy.py (Plan 01 — RecorderStrategy + RecorderStrategyConfig, set_data_client, set_config_loader)
    - .planning/phases/07-common-recorder-module-dydx-recorder/07-RESEARCH.md (Pattern 2 dydx recorder.py, Pitfall 3 lowercase enum, A1 all-perps-linear)
  </read_first>
  <action>
    Create scripts/dydx_recorder/recorder.py mirroring scripts/bybit_recorder/recorder.py, swapping the venue symbols:

    Imports: `from nautilus_trader.adapters.dydx import DYDX`, `from nautilus_trader.adapters.dydx.config import DydxDataClientConfig`, `from nautilus_trader.adapters.dydx.factories import DydxLiveDataClientFactory`, `from nautilus_trader.core.nautilus_pyo3 import DydxNetwork`, `from nautilus_trader.common.config import CUSTOM_ENCODINGS`. Keep the core imports (InstrumentProviderConfig, LoggingConfig, TradingNodeConfig, UUID4, TradingNode, ClientId, TraderId). Import from scripts.dydx_recorder.config: load_dydx_recorder_config, build_streaming_config. Import RecorderStrategy + RecorderStrategyConfig from scripts.common_recorder.strategy (NOT from a dydx strategy module — there is no subclass, RESEARCH anti-pattern).

    Module-level: a fixed `RECORDER_INSTANCE_ID` valid UUID4 string DISTINCT from the Bybit one (so the two recorders' feather dirs never collide under the shared catalog root). Register `CUSTOM_ENCODINGS[DydxNetwork] = lambda value: value.name` with the WHY comment (streaming serializes TradingNodeConfig.json(); pyo3 enum has no default encoder; NOTE DydxNetwork.MAINNET.name == "mainnet" lowercase — Pitfall 3).

    main(config_path):
    - `recorder_cfg, instrument_ids = load_dydx_recorder_config(config_path)`.
    - `streaming = build_streaming_config(recorder_cfg)`.
    - Build TradingNodeConfig with trader_id, instance_id=UUID4.from_str(RECORDER_INSTANCE_ID), LoggingConfig (log_file_name="dydx_recorder"), streaming, and data_clients={DYDX: DydxDataClientConfig(environment=_map_network(recorder_cfg.environment), instrument_provider=InstrumentProviderConfig(load_ids=frozenset(instrument_ids)))}. Keep the timeout_* values from the Bybit recorder. DO NOT pass product_types (dYdX has no product-type axis).
    - Build RecorderStrategyConfig: instrument_ids; `linear_instrument_ids=instrument_ids` (ALL dYdX perps are linear → mark/index/funding subscribed, RESEARCH A1); `instrument_depths={id: 50 for id in instrument_ids}` (a dummy depth the dYdX adapter ignores — RESEARCH A1; comment this); instrument_bar_intervals from the parsed entries; catalog_path=recorder_cfg.streaming_path (MUST equal the streaming root — Pitfall 5/A4); instance_id_str=RECORDER_INSTANCE_ID; conversion_interval_minutes, rotation_interval_minutes, restart_gap_threshold_seconds, heartbeat_interval_seconds, stale_threshold_default_seconds, stale_threshold_seconds, max_hot_added_instruments from recorder_cfg; reload_config_path=str(config_path).
    - node = TradingNode(config=config_node); strategy = RecorderStrategy(config=strategy_cfg); node.trader.add_strategy(strategy); node.add_data_client_factory(DYDX, DydxLiveDataClientFactory); node.build().
    - After build: `data_client = node.kernel.data_engine._clients[ClientId(DYDX)]`; `strategy.set_data_client(data_client)`; `strategy.set_config_loader(load_dydx_recorder_config)`. Keep the WHY comment about the private _clients registry (mirrors Bybit; no public DataEngine.get_client, no core edit).
    - try: node.run(raise_exception=True) finally: node.dispose().
    - `if __name__ == "__main__":` block mirroring Bybit (argv[1] or default recorder.toml next to this file).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/dydx_recorder/recorder.py').read()); print('syntax ok')" && python -c "import scripts.dydx_recorder.recorder as r; from nautilus_trader.core.nautilus_pyo3 import DydxNetwork; from nautilus_trader.common.config import CUSTOM_ENCODINGS; print('encoder', CUSTOM_ENCODINGS[DydxNetwork](DydxNetwork.MAINNET))"</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/dydx_recorder/recorder.py exists and imports cleanly (the verify command prints `encoder mainnet`, confirming CUSTOM_ENCODINGS[DydxNetwork] is registered at import and emits lowercase).
    - `grep -n "CUSTOM_ENCODINGS\[DydxNetwork\]" scripts/dydx_recorder/recorder.py` matches.
    - `grep -n "DydxLiveDataClientFactory" scripts/dydx_recorder/recorder.py` and `grep -n "DydxDataClientConfig" scripts/dydx_recorder/recorder.py` match.
    - `grep -n "from scripts.common_recorder.strategy import RecorderStrategy" scripts/dydx_recorder/recorder.py` matches (reuse, no subclass).
    - `grep -c "class .*Strategy" scripts/dydx_recorder/recorder.py` returns 0 (no dYdX strategy subclass defined anywhere).
    - `grep -n "set_config_loader(load_dydx_recorder_config)" scripts/dydx_recorder/recorder.py` matches.
    - `grep -n "_clients\[ClientId(DYDX)\]" scripts/dydx_recorder/recorder.py` matches.
    - `grep -n "linear_instrument_ids=instrument_ids" scripts/dydx_recorder/recorder.py` matches (all perps linear, A1).
    - RECORDER_INSTANCE_ID differs from the Bybit recorder's value: `grep -o "8f1b9c2e-1d3a-4b6c-8e7f-0a1b2c3d4e5f" scripts/dydx_recorder/recorder.py` returns nothing.
  </acceptance_criteria>
  <done>scripts/dydx_recorder/recorder.py builds a dYdX TradingNode, reuses the shared RecorderStrategy with the all-perps-linear mapping, injects the client + dYdX loader, registers the DydxNetwork encoder, and imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: dYdX strategy tests — 7-feed subscribe (DYDX-03) + funding dedup (DYDX-04)</name>
  <files>tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py</files>
  <read_first>
    - tests/unit_tests/persistence/recorder/test_recorder_strategy.py (the harness — copy the _build_strategy pattern: TestClock, MessageBus, Portfolio, RecorderStrategyConfig, strategy.register(...); and the subscribe-assertion + funding-dedup test patterns using mocker.spy / mocker.patch.object on subscribe_* and the on_funding_rate dedup test)
    - tests/unit_tests/persistence/recorder/conftest.py (mock_cache fixture — a real Cache; add_instrument to register, leave unregistered to simulate missing)
    - scripts/common_recorder/strategy.py (the strategy under test — _subscribe_instrument issues the 7 subscribe calls; on_funding_rate dedup gate)
  </read_first>
  <action>
    Create tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py with a license header and a local `_build_dydx_strategy(mocker, mock_cache, instrument_ids, ...)` helper adapting the Bybit `_build_strategy` harness for dYdX: same TestClock/MessageBus/Portfolio/register wiring, but pass `linear_instrument_ids=instrument_ids` (all perps linear, A1) and `instrument_depths={id: 50 for id in instrument_ids}` (dummy). Use dYdX ids `BTC-USD-PERP.DYDX` and `ETH-USD-PERP.DYDX` via InstrumentId.from_str. Import RecorderStrategy/RecorderStrategyConfig from scripts.common_recorder.strategy.

    Tests (DYDX-03): register the instruments in mock_cache, then assert that calling _subscribe_instrument (or on_start with the cache populated and subscribe_* spied) issues, for a single dYdX perp: subscribe_trade_ticks, subscribe_quote_ticks, subscribe_order_book_deltas (book_type=BookType.L2_MBP), one subscribe_bars per interval with BarType ending "-LAST-EXTERNAL", subscribe_mark_prices, subscribe_index_prices, subscribe_funding_rates — i.e. all 7 feeds, because is_linear is True for every dYdX perp. Spy/patch the subscribe_* methods (mirroring how the Bybit strategy tests assert subscriptions) and assert call args. Assert the bar-type string contains the dYdX instrument id and "-LAST-EXTERNAL".

    Tests (DYDX-04): drive on_funding_rate with two FundingRateUpdate objects for the same dYdX instrument id with the SAME rate, and assert _persist_funding_rate is called once (dedup drops the unchanged second). Then a third with a DIFFERENT rate triggers a second persist. Mirror the existing Bybit funding-dedup test exactly, only swapping the instrument id to the dYdX one — proving the dedup is exchange-agnostic (keys on (instrument_id, rate)). Patch _persist_funding_rate to count calls (do NOT actually write feather files).
  </action>
  <verify>
    <automated>uv run --no-sync pytest tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py exists.
    - `grep -c "DYDX" tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py` is >= 1 (uses dYdX instrument ids).
    - A test asserts all 7 subscribe_* methods are invoked for a single dYdX perp (trade, quote, order_book_deltas, bars, mark_prices, index_prices, funding_rates).
    - A test asserts funding dedup: identical-rate second update does NOT call _persist_funding_rate again; a changed-rate update does.
    - The test file passes: `uv run --no-sync pytest tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py -q` shows all passed, 0 failed.
    - The full recorder suite still passes (no regression to the shared strategy): `uv run --no-sync pytest tests/unit_tests/persistence/recorder/ -q` shows 0 failed.
  </acceptance_criteria>
  <done>The dYdX strategy tests prove all 7 feeds subscribe for every dYdX perp (DYDX-03) and that funding dedup keys on (instrument_id, rate) for dYdX ids (DYDX-04), with the full suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dYdX Indexer/WS → recorder | Untrusted external market data crosses into the recorder (data-only; no credentials) |
| recorder config → streaming serialization | TradingNodeConfig.json() serializes the DydxNetwork pyo3 enum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Denial of Service | TradingNodeConfig.json() fails on unencodable DydxNetwork enum | mitigate | CUSTOM_ENCODINGS[DydxNetwork] = lambda v: v.name registered at module import (Pitfall 3); verified by import-time encoder check. |
| T-07-07 | Spoofing/Tampering | dYdX data client credentials | accept | dYdX DATA client uses no credentials (wallet/key are execution-only, RESEARCH Security Domain); no auth surface. |
| T-07-08 | Tampering | feather-dir collision with Bybit under shared catalog | mitigate | dYdX uses a distinct RECORDER_INSTANCE_ID and venue-suffixed instrument ids (*-USD-PERP.DYDX), so no collision in the shared catalog (RESEARCH Runtime State Inventory). |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- recorder.py imports + encoder registered: verify command in Task 1 prints `encoder mainnet`.
- dYdX strategy tests pass: `uv run --no-sync pytest tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py -q`.
- Full recorder suite green (no regression): `uv run --no-sync pytest tests/unit_tests/persistence/recorder/ -q`.
- DYDX-07 reliability parity is structural (inherited from shared strategy): `grep -rn "reconnect\|resubscribe" scripts/dydx_recorder/` returns nothing (adapter-driven, zero custom reconnect code — REL-04 parity); flush+convert/heartbeat/restart-gap live in the shared strategy already verified by the full suite.
</verification>

<success_criteria>
- scripts/dydx_recorder/recorder.py runs a dYdX TradingNode reusing the shared strategy, with DydxNetwork registered in CUSTOM_ENCODINGS and the client+loader injected (DYDX-02).
- The shared strategy subscribes all 7 dYdX feeds per perp (DYDX-03) and dedups funding on (instrument_id, rate) (DYDX-04), proven by the new test file.
- DYDX-07 reliability parity is inherited from the shared strategy (flush+convert, heartbeat, restart-gap, adapter-driven reconnect) — no custom reconnect code in dydx_recorder.
- The full recorder suite stays green.
</success_criteria>

<artifacts_this_phase_produces>
- `scripts.dydx_recorder.recorder.main` — dYdX recorder entrypoint (build + run TradingNode)
- `scripts.dydx_recorder.recorder.RECORDER_INSTANCE_ID` — fixed dYdX instance id (distinct from Bybit)
- `CUSTOM_ENCODINGS[DydxNetwork]` registration — lowercase-name encoder for streaming serialization
- `tests/unit_tests/persistence/recorder/test_dydx_recorder_strategy.py` — 7-feed + funding-dedup tests for dYdX
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-common-recorder-module-dydx-recorder/07-03-SUMMARY.md` when done
</output>
