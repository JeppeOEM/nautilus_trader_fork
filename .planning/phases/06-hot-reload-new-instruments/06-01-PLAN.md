---
phase: 06-hot-reload-new-instruments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bybit_recorder/config.py
  - scripts/bybit_recorder/strategy.py
  - tests/unit_tests/persistence/recorder/conftest.py
  - tests/unit_tests/persistence/recorder/test_recorder_config.py
  - tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py
autonomous: true
requirements: [HOT-01]

must_haves:
  truths:
    - "A `config-reload` timer is registered in on_start at the heartbeat cadence (D-01/D-02)"
    - "Editing recorder.toml to REMOVE an instrument unsubscribes all its feeds and prunes its _last_seen entries (D-06)"
    - "Editing an instrument's depth re-subscribes order book deltas at the new depth via unsubscribe-then-subscribe (D-05)"
    - "Editing an instrument's bar_intervals subscribes only added intervals and unsubscribes only removed ones (D-05)"
    - "The new max_hot_added_instruments config knob is validated > 0 at load time (D-08)"
    - "A parse/validation error during a reload poll is logged and swallowed; the recorder keeps running on the last-good config"
  artifacts:
    - path: "scripts/bybit_recorder/config.py"
      provides: "max_hot_added_instruments PositiveInt field + fail-fast > 0 validation"
      contains: "max_hot_added_instruments"
    - path: "scripts/bybit_recorder/strategy.py"
      provides: "_on_config_reload timer callback, diff engine, removal/param-swap branches, bookkeeping fields, set_data_client setter"
      contains: "_on_config_reload"
    - path: "tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py"
      provides: "Unit tests for timer registration, diff, removal, depth-swap order, bar-interval delta, threshold warning, linear gating"
      contains: "def test_"
  key_links:
    - from: "scripts/bybit_recorder/strategy.py::on_start"
      to: "config-reload timer"
      via: "self.clock.set_timer(name='config-reload', ...)"
      pattern: "set_timer.*config-reload"
    - from: "scripts/bybit_recorder/strategy.py::_on_config_reload"
      to: "load_recorder_config"
      via: "re-parse recorder.toml each poll"
      pattern: "load_recorder_config"
---

<objective>
Add the config-reload polling timer, the diff engine, and the two fully unit-testable diff branches — REMOVALS (D-06) and PARAM CHANGES (depth/bar-interval clean-swap, D-05) — to `RecorderStrategy`, plus the new `max_hot_added_instruments` config knob (D-08) and the bookkeeping fields/setter that the Wave-2 ADDITION branch will consume. This plan deliberately STOPS SHORT of the new-instrument runtime load (Pattern 3 / live wiring), which is Plan 02 — everything here is exercisable with mocked subscribe/unsubscribe spies and a real `Cache`/`TestClock`.

Purpose: HOT-01 requires detecting recorder.toml changes while running and applying additions, removals, and param changes live. Removals and param-swaps are low-risk (all primitives already exist and are implemented for Bybit) and form the diff backbone; the new-instrument load is the one risky seam, isolated into Plan 02 behind a human-verify smoke.
Output: Re-callable reload poll wired into on_start, removal + param-swap diff branches, the D-08 knob, the bookkeeping/setter scaffolding, and the new hot-reload test file + extended config tests.
</objective>

<artifacts_this_phase_produces>
New symbols introduced by Phase 6 (exclude these from drift/source-grounding — they do not exist yet at plan time):

- `scripts/bybit_recorder/config.py`:
  - `RecorderConfig.max_hot_added_instruments` (field, `PositiveInt = 50`)
  - `max_hot_added_instruments` `recorder.toml` key + `<= 0` ValueError validation in `load_recorder_config`
- `scripts/bybit_recorder/strategy.py`:
  - `RecorderStrategyConfig.max_hot_added_instruments` (field, `PositiveInt = 50`)
  - `RecorderStrategy._on_config_reload(self, event)` (timer callback)
  - `RecorderStrategy._subscribed_params: dict[InstrumentId, tuple[int, frozenset[str]]]`
  - `RecorderStrategy._failed_instrument_ids: set[InstrumentId]`
  - `RecorderStrategy._last_config_signature: int | None`
  - `RecorderStrategy._hot_added_count: int`
  - `RecorderStrategy._bybit_client` (defaults to `None`; set by Plan 02's `set_data_client`)
  - `RecorderStrategy.set_data_client(self, client)` (setter; consumed by Plan 02)
  - `RecorderStrategy._diff_config(self, parsed)` (compute additions/removals/param-changes)
  - `RecorderStrategy._unsubscribe_instrument(...)` (removal helper)
  - `RecorderStrategy._apply_param_change(...)` (depth + bar-interval swap helper)
  - `RecorderStrategy._config_signature(self, parsed)` (stable signature for failed-set reset)
  - `"config-reload"` timer name
- `tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py` (new file)
- `conftest.py` / `_build_strategy` extension: optional `recorder_config_path` + `data_client` injection params
</artifacts_this_phase_produces>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hot-reload-new-instruments/06-CONTEXT.md
@.planning/phases/06-hot-reload-new-instruments/06-RESEARCH.md
@.planning/phases/06-hot-reload-new-instruments/06-PATTERNS.md
@.planning/phases/06-hot-reload-new-instruments/06-VALIDATION.md
@scripts/bybit_recorder/config.py
@scripts/bybit_recorder/strategy.py
@tests/unit_tests/persistence/recorder/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 test scaffold + max_hot_added_instruments config knob (D-08)</name>
  <files>scripts/bybit_recorder/config.py, tests/unit_tests/persistence/recorder/test_recorder_config.py, tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py, tests/unit_tests/persistence/recorder/conftest.py</files>
  <read_first>
    - scripts/bybit_recorder/config.py (RecorderConfig fields lines 122-132; the four existing `recorder_raw.get(...)` + `<= 0` raise blocks lines 256-300; the count-only INFO log lines 302-307)
    - tests/unit_tests/persistence/recorder/conftest.py (`sample_toml` fixture lines 46-69, `mock_cache` fixture lines 72-81)
    - tests/unit_tests/persistence/recorder/test_recorder_config.py (existing threshold-validation tests for `heartbeat_interval_seconds`/`restart_gap_threshold_seconds` — mirror their structure)
    - tests/unit_tests/persistence/recorder/test_recorder_strategy.py (the `_build_strategy` helper lines 36-77 — you will extend it in Task 3)
  </read_first>
  <action>
    In `scripts/bybit_recorder/config.py`: add field `max_hot_added_instruments: PositiveInt = 50` to `RecorderConfig` immediately after `heartbeat_interval_seconds` (line 128), with a docstring entry matching the sibling-threshold docstring style (note it is WARNING-only per D-08 — informational, never blocks). In `load_recorder_config`, after the `restart_gap_threshold_seconds` block (line 286), add `max_hot_added_instruments = recorder_raw.get("max_hot_added_instruments", 50)` and a `if max_hot_added_instruments <= 0: raise ValueError(f"Invalid max_hot_added_instruments {max_hot_added_instruments}: must be positive")` block mirroring the existing `<= 0` raise shape verbatim. Thread `max_hot_added_instruments=max_hot_added_instruments` into the `RecorderConfig(...)` constructor (lines 288-300).

    In `tests/unit_tests/persistence/recorder/test_recorder_config.py`: add two tests mirroring the existing `restart_gap_threshold_seconds` tests — one writing a TOML with `max_hot_added_instruments = 0` (and one negative) under `[recorder]` and asserting `load_recorder_config` raises `ValueError` whose message contains `max_hot_added_instruments`; one asserting that with the key OMITTED, `load_recorder_config(...)[0].max_hot_added_instruments == 50`. Write the TOML to a tmp file (reuse the existing file-writing pattern in that test module — do NOT pass the raw string directly since `load_recorder_config` opens a path).

    Create NEW file `tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py` with the standard license header (copy from conftest.py lines 1-14), imports, and ten test STUBS (each a `def test_<name>(...)` body raising `pytest.fail("not implemented — Wave 1")` or `pytest.skip`), one per row in 06-VALIDATION.md's Per-Task Verification Map: `test_timer_registered`, `test_detects_addition`, `test_addition_subscribes`, `test_removal_unsubscribes`, `test_depth_swap_order`, `test_bar_interval_delta`, `test_failed_not_retried`, `test_failed_resets_on_change`, `test_threshold_warning`, `test_linear_gating`. The `-k` selectors in 06-VALIDATION.md (`timer_registered`, `detects_addition`, etc.) MUST match these test names. These stubs are filled in by Tasks 2-3 (this plan) and `addition_subscribes`/`failed_*` by Plan 02; leave the Plan-02 ones as explicit `pytest.skip("covered in Plan 02 — runtime instrument load")` so the suite stays green between waves.

    Do NOT modify the data_client injection in conftest yet — that is Task 3.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork &amp;&amp; uv run pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -k max_hot_added -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "max_hot_added_instruments" scripts/bybit_recorder/config.py` shows the field declaration, the `.get(..., 50)` read, the `<= 0` ValueError, and the constructor kwarg (4 occurrences minimum).
    - `uv run pytest tests/unit_tests/persistence/recorder/test_recorder_config.py -k max_hot_added -x -q` passes (validation + default tests green).
    - `tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py` exists and `uv run pytest tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py --collect-only -q` lists all ten `test_` names matching the `-k` selectors in 06-VALIDATION.md.
    - `uv run pytest tests/unit_tests/persistence/recorder/ -q` is green (stubs skip, not fail).
  </acceptance_criteria>
  <done>The D-08 knob exists with fail-fast `> 0` validation and a default of 50; the new hot-reload test file exists with all ten stub tests collectable; config validation tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reload timer + diff engine + REMOVAL and PARAM-CHANGE branches (D-01/D-02/D-03/D-05/D-06)</name>
  <files>scripts/bybit_recorder/strategy.py</files>
  <read_first>
    - scripts/bybit_recorder/strategy.py (RecorderStrategyConfig lines 106-117; `__init__` lines 133-150; `on_start` subscribe loop + timer registration lines 165-210; `_heartbeat` callback signature + body lines 289-313; `_run_conversion` per-step error-swallow precedent lines 463-496; `_last_seen` key shape `(stream, instrument_id)` used in every on_* handler e.g. line 323)
    - scripts/bybit_recorder/config.py (`load_recorder_config` signature + return tuple lines 175-309; `InstrumentEntry` fields lines 73-76; `RecorderConfig.instrument_ids`/`linear_instrument_ids` properties lines 134-156)
    - .planning/phases/06-hot-reload-new-instruments/06-PATTERNS.md (Pattern assignments for strategy.py — exact subscribe/unsubscribe call forms, the `_last_seen` prune loop, bookkeeping fields)
    - .planning/phases/06-hot-reload-new-instruments/06-RESEARCH.md (Pitfall 2 depth-swap order; Pitfall 4 _last_seen prune; Pitfall 5 linear gating; Code Examples `_unsubscribe_instrument` and depth/bar-interval swaps with verified unsubscribe_* line refs)
  </read_first>
  <action>
    Add `max_hot_added_instruments: PositiveInt = 50` to `RecorderStrategyConfig` (after `heartbeat_interval_seconds`, line 115), with a docstring entry.

    In `RecorderStrategy.__init__` (after line 150), add the bookkeeping fields (Pattern 2 from RESEARCH): `self._subscribed_params: dict[InstrumentId, tuple[int, frozenset[str]]] = {}`, `self._failed_instrument_ids: set[InstrumentId] = set()`, `self._last_config_signature: int | None = None`, `self._hot_added_count: int = 0`, `self._bybit_client = None`. Add a `set_data_client(self, client) -> None` method that stores `self._bybit_client = client` (consumed by Plan 02; harmless no-op state here).

    Determine the reload config source path: add a `reload_config_path: str | None = None` field to `RecorderStrategyConfig` (the absolute recorder.toml path so `_on_config_reload` can re-read it). Wiring of this value from `recorder.py` is Plan 02; here, if `reload_config_path` is None the reload callback logs once and returns (no path → nothing to diff). The timer is still registered.

    In `on_start`, AFTER the existing heartbeat timer (line 210), seed `self._subscribed_params` from `self.config` (for each instrument_id: `(self.config.instrument_depths[id], frozenset(self.config.instrument_bar_intervals[id]))`) so the first reload diffs against real startup state, and register the new timer: `self.clock.set_timer(name="config-reload", interval=pd.Timedelta(seconds=self.config.heartbeat_interval_seconds), callback=self._on_config_reload)` (D-02: reuse heartbeat cadence).

    Add `_config_signature(self, parsed_cfg) -> int`: a stable hash of the parsed instrument entries (e.g. `hash(tuple(sorted((str(e.id), e.depth, tuple(e.bar_intervals), e.product_type) for e in parsed_cfg.instruments)))`) used to reset `_failed_instrument_ids` when the toml changes (D-07).

    Add `_diff_config(self, parsed_cfg) -> tuple[list, list, list]` returning `(additions, removals, param_changes)`: additions = instrument ids in parsed but NOT in `self._subscribed_params`; removals = ids in `self._subscribed_params` but NOT in parsed; param_changes = ids in both whose `(depth, frozenset(bar_intervals))` differs from `self._subscribed_params[id]`. Carry each item's parsed `InstrumentEntry` (need product_type + new depth/intervals).

    Add `_unsubscribe_instrument(self, instrument_id, depth, bar_intervals, is_linear) -> None` (D-06, verified call forms from RESEARCH Code Examples): `unsubscribe_trade_ticks`, `unsubscribe_quote_ticks`, `unsubscribe_order_book_deltas(id)` (NO depth param), `unsubscribe_bars(BarType.from_str(f"{id}-{interval}-LAST-EXTERNAL"))` per interval, and if `is_linear`: `unsubscribe_mark_prices`/`unsubscribe_index_prices`/`unsubscribe_funding_rates`. Then prune `_last_seen`: `for stream in ("trade","quote","deltas","bar","mark","index","funding"): self._last_seen.pop((stream, instrument_id), None)` (Pitfall 4), and `self._subscribed_params.pop(instrument_id, None)`.

    Add `_apply_param_change(self, instrument_id, old_depth, old_intervals, new_depth, new_intervals) -> None` (D-05): if `new_depth != old_depth`, call `self.unsubscribe_order_book_deltas(instrument_id)` FIRST then `self.subscribe_order_book_deltas(instrument_id, book_type=BookType.L2_MBP, depth=new_depth)` (Pitfall 2 — order is load-bearing; the engine dedups order-book subs by instrument_id only, so subscribe-before-unsubscribe silently drops the new depth). For bar intervals: `added = set(new_intervals) - set(old_intervals)`, `removed = set(old_intervals) - set(new_intervals)`; `unsubscribe_bars(...)` each removed, `subscribe_bars(...)` each added (only the delta — trade/quote/mark/index/funding are untouched per D-05). Update `self._subscribed_params[instrument_id] = (new_depth, frozenset(new_intervals))`.

    Add `_on_config_reload(self, event: TimeEvent) -> None`: wrap the WHOLE body in `try/except Exception: logger.exception("Config reload failed"); return` (mirrors `_run_conversion` swallow — a malformed/half-written toml must never fault the component; Security V7 / TOCTOU). If `self.config.reload_config_path is None`: `logger.debug("No reload_config_path configured; skipping reload"); return`. Else call `parsed_cfg, _ = load_recorder_config(self.config.reload_config_path)`. Compute signature; if it differs from `self._last_config_signature`, clear `self._failed_instrument_ids` and store the new signature (D-07). Call `_diff_config`. For each REMOVAL: call `_unsubscribe_instrument` (derive `is_linear` from the REMOVED instrument's last-known product type — store product_type in `_subscribed_params` too, OR track a parallel `_product_types: dict[InstrumentId, str]`; pick one and seed it in on_start). For each PARAM-CHANGE: call `_apply_param_change`. For ADDITIONS: leave a clearly-marked `# Plan 02: runtime instrument load (Pattern 3) + subscribe` placeholder that does nothing yet (Plan 02 fills it) — do NOT subscribe additions in this plan. Log a single INFO summarizing counts (e.g. `"Config reload: %d added, %d removed, %d changed"`) — counts/ids only, never credentials (Security V7).

    Import `load_recorder_config` at module top (`from scripts.bybit_recorder.config import load_recorder_config`).
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork &amp;&amp; uv run ruff check scripts/bybit_recorder/strategy.py &amp;&amp; uv run python -c "import ast,sys; ast.parse(open('scripts/bybit_recorder/strategy.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "config-reload" scripts/bybit_recorder/strategy.py` shows the `set_timer` registration.
    - `grep -n "def _on_config_reload\|def _diff_config\|def _unsubscribe_instrument\|def _apply_param_change\|def _config_signature\|def set_data_client" scripts/bybit_recorder/strategy.py` shows all six methods.
    - In `_apply_param_change`, `unsubscribe_order_book_deltas` appears textually BEFORE `subscribe_order_book_deltas` (Pitfall 2 order). Verify: `grep -n "unsubscribe_order_book_deltas\|subscribe_order_book_deltas" scripts/bybit_recorder/strategy.py` — the unsubscribe line number is lower than the depth-swap subscribe line within `_apply_param_change`.
    - `_on_config_reload` body is wrapped in `try/except Exception` with `logger.exception` (grep shows `logger.exception` inside the method).
    - `uv run ruff check scripts/bybit_recorder/strategy.py` passes; module parses.
  </acceptance_criteria>
  <done>The config-reload timer is registered at heartbeat cadence; the diff engine plus removal and param-swap branches are implemented with the load-bearing unsubscribe-then-subscribe ordering for depth changes and `_last_seen` pruning on removal; additions are a marked placeholder for Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for diff, removal, depth-swap order, bar-interval delta, threshold, linear gating</name>
  <files>tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py, tests/unit_tests/persistence/recorder/conftest.py</files>
  <behavior>
    - test_timer_registered: after `on_start()`, `"config-reload" in clock.timer_names`.
    - test_detects_addition: `_diff_config(parsed)` with a parsed set containing one new id (not in `_subscribed_params`) returns it in the additions list (removals/changes empty).
    - test_removal_unsubscribes: a parsed set MISSING a previously-subscribed instrument → `_on_config_reload` (or `_unsubscribe_instrument`) calls every `unsubscribe_*` for that id AND every `(stream, id)` key is removed from `_last_seen`.
    - test_depth_swap_order: a depth change → `unsubscribe_order_book_deltas` is called BEFORE `subscribe_order_book_deltas(depth=new)` (assert call order via a single mock parent or `mocker.patch.object` with recorded call sequence).
    - test_bar_interval_delta: intervals `["1-MINUTE"] -> ["1-MINUTE","5-MINUTE"]` → `subscribe_bars` called only for `5-MINUTE`, `unsubscribe_bars` not called; reverse case unsubscribes only the removed interval.
    - test_threshold_warning: with `max_hot_added_instruments=1` and `_hot_added_count` pushed over threshold, a WARNING is logged (assert via `caplog`). (This test may drive the counter directly since the ADD branch lands in Plan 02 — assert the threshold-check helper logs when count exceeds the knob.)
    - test_linear_gating: removing a SPOT instrument does NOT call `unsubscribe_mark_prices/index/funding`; removing a LINEAR instrument DOES.
  </behavior>
  <read_first>
    - tests/unit_tests/persistence/recorder/test_recorder_strategy.py (`_build_strategy` helper lines 36-77; `test_on_start_sets_conversion_timer`-style timer assertions; the `mocker.patch.object` subscribe-spy pattern)
    - tests/unit_tests/persistence/recorder/conftest.py (`mock_cache` lines 72-81 — register "present" instruments via `cache.add_instrument`; `sample_toml` lines 46-69)
    - scripts/bybit_recorder/strategy.py (the methods implemented in Task 2 — exact signatures and the `_subscribed_params` seeding in on_start)
    - .planning/phases/06-hot-reload-new-instruments/06-PATTERNS.md (test harness section: extend `_build_strategy` to inject a mock data-client; subscribe/unsubscribe spy + call-order assertion guidance)
  </read_first>
  <action>
    Extend `_build_strategy` in test_recorder_strategy.py (or move it to conftest.py if cleaner for reuse — keep backward compatibility for existing callers) to accept two optional kwargs: `reload_config_path: str | None = None` (threaded into `RecorderStrategyConfig.reload_config_path`) and `data_client=None` (if provided, call `strategy.set_data_client(data_client)` after register). Existing callers must keep passing (defaults preserve current behavior). If you relocate the helper, import it where the existing tests use it.

    Fill in the seven Task-3 stubs in `test_recorder_hot_reload.py` per the `<behavior>` block. Use a real `TestClock`/`MessageBus`/`Portfolio`/`Cache` via `_build_strategy`. For subscribe/unsubscribe spies, `mocker.patch.object(strategy, "subscribe_order_book_deltas")` etc.; for call-order assertions, attach all spied methods to a single `mocker.Mock()` parent via `mock_parent.attach_mock(...)` and assert on `mock_parent.mock_calls` order (so `unsubscribe_order_book_deltas` precedes the depth-swap `subscribe_order_book_deltas`). For the diff tests, seed `strategy._subscribed_params` (and the product-type tracking dict) directly to represent the "currently running" set, then build a `parsed_cfg` via `load_recorder_config` on a tmp toml OR construct a `RecorderConfig`/`InstrumentEntry` list directly, and call `strategy._diff_config(parsed_cfg)`. For `test_threshold_warning`, use `caplog.at_level(logging.WARNING)`.

    Leave `test_addition_subscribes`, `test_failed_not_retried`, `test_failed_resets_on_change` as `pytest.skip("Plan 02 — runtime instrument load")` (those need the Pattern-3 mock client and ADD branch).
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork &amp;&amp; uv run pytest tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py -k "timer_registered or detects_addition or removal_unsubscribes or depth_swap_order or bar_interval_delta or threshold_warning or linear_gating" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit_tests/persistence/recorder/test_recorder_hot_reload.py -k "timer_registered or detects_addition or removal_unsubscribes or depth_swap_order or bar_interval_delta or threshold_warning or linear_gating" -x -q` — all seven tests pass.
    - The depth-swap test asserts call ORDER (unsubscribe before subscribe), not just both-called.
    - The removal test asserts `_last_seen` has zero `(_, removed_id)` entries afterward.
    - The linear-gating test asserts spot removal does NOT touch mark/index/funding unsubscribes.
    - `uv run pytest tests/unit_tests/persistence/recorder/ -q` — full recorder suite green (Plan-02 tests skip, none fail).
  </acceptance_criteria>
  <done>All seven non-addition hot-reload behaviors are covered by passing unit tests, including the load-bearing depth-swap ordering, the `_last_seen` prune on removal, and linear-only gating; the three addition/load tests are explicitly skipped pending Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → recorder.toml → reload poll | The operator edits a local, filesystem-permission-governed config file; the reload poll re-reads it untrusted each cycle (V5 input-validation boundary). |
| reload poll → DataEngine subscribe/unsubscribe | Diff-driven subscribe/unsubscribe commands cross into the adapter; fire-and-forget, no untrusted external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | `_on_config_reload` re-parsing recorder.toml | mitigate | Whole-body `try/except Exception: logger.exception(...); return` (mirrors `_run_conversion`); a malformed/half-written toml (TOCTOU during operator edit) is logged once and skipped — recorder keeps running on last-good config. `tomllib.load` reads the file atomically per poll. |
| T-06-02 | Denial of Service | `max_hot_added_instruments` validation | mitigate | New knob validated `> 0` at load (fail-fast), default 50; over-threshold hot-adds log a WARNING (D-08) surfacing runaway config thrash. |
| T-06-03 | Information Disclosure | reload-path logging | mitigate | Log instrument COUNT and ids only, never credentials (Security V7; existing `config.py:303` precedent). |
| T-06-04 | Tampering | recorder.toml access | accept | Single local process; file access governed by filesystem perms / systemd `WorkingDirectory`. No new network/auth surface added by this phase. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (all primitives exist in the fork; RESEARCH confirms no `## Package Legitimacy Audit` needed). |
</threat_model>

<verification>
- `uv run pytest tests/unit_tests/persistence/recorder/ -q` — full recorder suite green.
- `uv run ruff check scripts/bybit_recorder/ tests/unit_tests/persistence/recorder/` — clean.
- `grep -rn "config-reload\|_on_config_reload\|max_hot_added_instruments" scripts/bybit_recorder/` confirms all new symbols present.
- No edits under `nautilus_trader/` (CLAUDE.md constraint): `git diff --name-only` must show only `scripts/bybit_recorder/*` and `tests/unit_tests/persistence/recorder/*`.
</verification>

<success_criteria>
- The `config-reload` timer is registered in on_start at the heartbeat cadence (D-01/D-02).
- Removal branch unsubscribes all feeds and prunes `_last_seen` (D-06), with linear-only gating (Pitfall 5).
- Param-change branch performs the depth clean-swap in the correct unsubscribe-then-subscribe order (D-05/Pitfall 2) and only the bar-interval delta.
- The `max_hot_added_instruments` knob is validated `> 0` with default 50 (D-08).
- A reload-time parse/validation error is logged and swallowed (component survives, last-good config retained).
- The seven non-addition hot-reload unit tests pass; addition tests are scaffolded and skipped for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/06-hot-reload-new-instruments/06-01-SUMMARY.md` when done.
</output>
