---
phase: 02-dashboard-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - troll/ml_signals/dashboard.py
  - troll/ml_signals/test_dashboard_metrics.py
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
must_haves:
  truths:
    - "Each rankings-table row's instrument label is a link to /coin/{id}"
    - "The rankings table shows OFI_10, OBI_10, CVD, spread, microprice lean, volume delta, and buy/sell count columns"
    - "/coin/{id} renders a live indicator panel (OFI_3/5/10, OBI_3/5/10, microprice, spread, CVD, volume delta, buy/sell count, avg trade size) read from _LIVE — no Parquet read"
    - "/data/coin/{id} returns JSON with ts/mid/bid/ask/micro arrays built from the _second_rolling deque"
    - "The /coin/{id} page embeds JS that polls /data/coin/{id} every 1s and calls Plotly.react with mid, bid, ask, microprice lines"
  artifacts:
    - path: "troll/ml_signals/dashboard.py"
      provides: "_coin_chart_json(), _render_live_coin_page(), _ROLLING global, /data/coin/ route, live /coin/ route, expanded RANKING_COLS, /coin/ row link"
      contains: "_coin_chart_json"
    - path: "troll/ml_signals/test_dashboard_metrics.py"
      provides: "Tests for _coin_chart_json (None guard, ns→ms conversion)"
      contains: "test_chart_json"
  key_links:
    - from: "troll/ml_signals/dashboard.py:_Handler.do_GET"
      to: "troll/ml_signals/dashboard.py:_coin_chart_json"
      via: "/data/coin/ branch serializes the deque via _coin_chart_json(symbol, _ROLLING)"
      pattern: "/data/coin/"
    - from: "troll/ml_signals/dashboard.py:_render_live_coin_page (embedded JS)"
      to: "troll/ml_signals/dashboard.py:/data/coin/ route"
      via: "fetch('/data/coin/'+iid) on setInterval(1000) → Plotly.react"
      pattern: "setInterval"
    - from: "troll/ml_signals/dashboard.py:serve_in_background"
      to: "troll/ml_signals/dashboard.py:_ROLLING"
      via: "global _ROLLING = rolling so request handlers can read the deque"
      pattern: "_ROLLING"
---

<objective>
Deliver the user-facing half of the dashboard upgrade: expand the rankings table to show
the DASH-01 columns, make each row click through to a single-coin view (DASH-02), and
replace the Parquet-backed `/coin/{id}` page with a live, deque-only view (DASH-03) that
includes a 1-second real-time line chart of mid/bid/ask/microprice (DASH-04) fed by a new
`/data/coin/{id}` JSON endpoint. All live data comes from the in-process `_second_rolling`
deque — no Parquet reads on the live path.

Purpose: Plan 01 made the metrics available in `_LIVE`; this plan makes them visible and
navigable, and replaces the forbidden Parquet-reading coin page with a deque-backed live
view. The existing Parquet-based microstructure view stays reachable at `/chart/{id}`.
Output: Expanded `RANKING_COLS`, updated row link, `_coin_chart_json()`,
`_render_live_coin_page()`, `_ROLLING` global, two new routes, and chart-JSON tests.
</objective>

<execution_context>
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/workflows/execute-plan.md
@/home/mrqdt/code/nautilus_trader_fork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-upgrade/02-RESEARCH.md
@troll/CLAUDE.md
@troll/ml_signals/dashboard.py
@.planning/phases/02-dashboard-upgrade/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand RANKING_COLS and make each row link to /coin/{id}</name>
  <files>troll/ml_signals/dashboard.py</files>
  <read_first>
    - troll/ml_signals/dashboard.py (RANKING_COLS lines 88-96; row rendering lines 195-205; _sort_link/_cell already handle any column generically)
  </read_first>
  <action>
    Replace `RANKING_COLS` (dashboard.py) so it lists, in this order, columns whose `store_key`
    matches the metric keys produced by `_metrics_from_rolling()`:
    `("ofi_10","OFI10", lambda v: f"{v:+.1f}", <green/red color_fn>)`, then `ofi_5` "OFI5", `ofi_3` "OFI3"
    (same format/color), `("obi_10","OBI10", lambda v: f"{v:.3f}", <obi color_fn>)`, then `obi_5`, `obi_3`,
    `("cvd","CVD", lambda v: f"{v:+.2f}", <green/red>)`, `("spread","Spread", lambda v: f"{v:.6f}", None)`,
    `("microprice_lean","u lean", lambda v: f"{v:+.6f}", <green/red>)`,
    `("volume_delta","Vol d", lambda v: f"{v:+.2f}", <green/red>)`,
    `("buy_count","Buy#", lambda v: f"{int(v)}", None)`, `("sell_count","Sell#", lambda v: f"{int(v)}", None)`.
    Keep the existing slow-loop columns `("price","Price",...)`, `("pct_1h",...)`, `("pct_24h",...)`,
    `("volatility",...)` appended after the live columns (they provide ranking context per RESEARCH Open
    Question 1). Reuse the existing inline green/red color lambda style already present on the `ofi` row
    (`lambda v: "#2a9d2a" if v > 0 else "#c0392b"`) for the green/red columns; for OBI use a lambda that
    colors relative to 0.5 (e.g. `lambda v: "#2a9d2a" if v > 0.5 else "#c0392b"`). Header labels must be plain
    ASCII (no subscript characters) to keep the HTML and sort links simple. Do not invent new metric keys —
    every `store_key` must already be a key in the `_metrics_from_rolling()` output dict.

    In the row-rendering loop (currently
    `f"<td><a href='/chart/{iid}'>{label}</a> <small><a href='/coin/{iid}'>cov</a></small></td>"`),
    swap the destinations so the primary label links to the live coin view and the secondary link goes to the
    historical chart: `f"<td><a href='/coin/{iid}'>{label}</a> <small><a href='/chart/{iid}'>chart</a></small></td>"`.
    Leave the `/history/{iid}` "31d" link unchanged.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -c "import sys; sys.path.insert(0,'troll'); import ml_signals.dashboard as d; keys={k for k,*_ in d.RANKING_COLS}; need={'ofi_10','ofi_5','ofi_3','obi_10','obi_5','obi_3','cvd','spread','microprice_lean','volume_delta','buy_count','sell_count'}; assert need<=keys, sorted(need-keys); import inspect; r=inspect.getsource(d._render_rankings_page); assert \"/coin/{iid}'>{label}\" in r, 'row label must link to /coin/{id}'"</automated>
  </verify>
  <acceptance_criteria>
    - `{k for k,*_ in RANKING_COLS}` is a superset of {ofi_10, ofi_5, ofi_3, obi_10, obi_5, obi_3, cvd, spread, microprice_lean, volume_delta, buy_count, sell_count}.
    - The rankings row template links the instrument `{label}` to `/coin/{iid}` and the secondary link to `/chart/{iid}`.
    - Every `store_key` in RANKING_COLS is a key produced by `_metrics_from_rolling()` or the slow-loop snapshot (price/pct_1h/pct_24h/volatility).
  </acceptance_criteria>
  <done>The rankings table shows all DASH-01 columns and each row's instrument label navigates to /coin/{id} (DASH-02).</done>
</task>

<task type="auto">
  <name>Task 2: Add _coin_chart_json(), _ROLLING global, and the /data/coin/{id} JSON route</name>
  <files>troll/ml_signals/dashboard.py</files>
  <read_first>
    - troll/ml_signals/dashboard.py (_Handler.do_GET lines 677-723; serve_in_background lines 729-750; imports block lines 39-71)
    - troll/dydx_collector/second_snapshot.py (DydxSecondSnapshot fields used: bid_prices, ask_prices, bid_sizes, ask_sizes, ts_event)
    - .planning/phases/02-dashboard-upgrade/02-RESEARCH.md (_coin_chart_json reference implementation, Pitfall 2 ns→ms, Pitfall 5 None guard)
  </read_first>
  <action>
    Add `import json` to the dashboard.py stdlib import block (alphabetical near `import html`).
    Add a module-level `_ROLLING: dict | None = None` near the other module globals (next to `_LIVE`).
    In `serve_in_background()`, at the top of the function body, add `global _ROLLING` and set
    `_ROLLING = rolling` so the synchronous request handlers can read the same deque the `_fast_loop` thread uses
    (the deque is GIL-atomic; snapshot it with `list(...)` — no extra lock, consistent with RESEARCH thread-safety note).

    Add a helper `def _coin_chart_json(iid: str, rolling: dict | None) -> str:` that returns a JSON string.
    If `rolling is None`, return `json.dumps({"ts": [], "mid": [], "bid": [], "ask": [], "micro": []})` (standalone-mode
    guard, Pitfall 5). Otherwise snapshot `snaps = list(rolling.get(iid, []))`, then for each snap skip when
    `not s.bid_prices or not s.ask_prices`; bind `bp, ap = s.bid_prices[0], s.ask_prices[0]` and
    `bs, as_ = s.bid_sizes[0], s.ask_sizes[0]`, `total = bs + as_`; append `s.ts_event // 1_000_000` to ts
    (nanoseconds → milliseconds, Pitfall 2), `(bp + ap) / 2` to mid, `bp` to bid, `ap` to ask, and
    `(bp * as_ + ap * bs) / total if total > 0 else (bp + ap) / 2` to micro. Return
    `json.dumps({"ts": ts, "mid": mid_vals, "bid": bid_vals, "ask": ask_vals, "micro": micro_vals})`.
    Keep the function under ~30 lines (READ-01) and type-hinted (READ-03).

    In `_Handler.do_GET`, add a branch BEFORE the `/coin/` branch (so `/data/coin/...` is not captured by
    `startswith("/coin/")`... note `/data/coin/` does not start with `/coin/`, but order it before the generic
    fallback): `elif path.startswith("/data/coin/"):` → `symbol = path.removeprefix("/data/coin/")`,
    `payload = _coin_chart_json(symbol, _ROLLING).encode()`, then `send_response(200)`,
    `send_header("Content-Type", "application/json")`, `send_header("Content-Length", str(len(payload)))`,
    `end_headers()`, `self.wfile.write(payload)`, and `return` — do not fall through to the HTML send block at the
    bottom of `do_GET`. This route must NOT call `list_instruments` or any catalog API (live path only).
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -c "import sys; sys.path.insert(0,'troll'); import json,inspect; import ml_signals.dashboard as d; assert d._coin_chart_json('X', None) == json.dumps({'ts':[],'mid':[],'bid':[],'ask':[],'micro':[]}); assert hasattr(d,'_ROLLING'); h=inspect.getsource(d._Handler.do_GET); assert '/data/coin/' in h and 'application/json' in h; assert 'ParquetDataCatalog' not in inspect.getsource(d._coin_chart_json) and 'trade_ticks' not in inspect.getsource(d._coin_chart_json)"</automated>
  </verify>
  <acceptance_criteria>
    - `_coin_chart_json('X', None)` returns the empty-arrays JSON object (standalone guard).
    - `dashboard._ROLLING` exists at module level and `serve_in_background` assigns it from its `rolling` argument.
    - `do_GET` contains a `/data/coin/` branch that sets `Content-Type: application/json` and returns without the HTML send path.
    - `inspect.getsource(_coin_chart_json)` contains neither `ParquetDataCatalog` nor `trade_ticks` (no Parquet on the live path, MEM-01/DASH-04).
  </acceptance_criteria>
  <done>/data/coin/{id} serves deque-derived JSON (ns→ms timestamps, None-safe) with no Parquet read.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the /coin/{id} handler with a live deque-backed view and add chart-JSON tests</name>
  <files>troll/ml_signals/dashboard.py, troll/ml_signals/test_dashboard_metrics.py</files>
  <read_first>
    - troll/ml_signals/dashboard.py (_render_coin_page lines 453-558 — the Parquet view being replaced; do_GET /coin/ branch lines 688-697; _page helper lines 145-151; _METRICS_LOCK/_LIVE lines 103-104)
    - .planning/phases/02-dashboard-upgrade/02-RESEARCH.md (_render_live_coin_page reference implementation incl. JS setInterval block)
    - troll/ml_signals/test_dashboard_metrics.py (created in plan 01 — extend it)
  </read_first>
  <action>
    Add `def _render_live_coin_page(symbol: str, rolling: dict | None) -> str:` that renders a deque-only live
    view (no `ParquetDataCatalog`, no `catalog.*`, no `compute_chart_series`). Read the metrics under the lock:
    `with _METRICS_LOCK: m = _LIVE.get(symbol, {})`. Build an indicator panel `<table>` with one row per DASH-03
    indicator using a local `_row(label, key, fmt)` that renders `m.get(key)` formatted, or `&mdash;` when None:
    OFI10 (`ofi_10`), OFI5 (`ofi_5`), OFI3 (`ofi_3`), OBI10 (`obi_10`), OBI5 (`obi_5`), OBI3 (`obi_3`),
    Microprice (`microprice`), microprice lean (`microprice_lean`), Spread (`spread`), CVD (`cvd`),
    Vol delta (`volume_delta`), Buy# (`buy_count`), Sell# (`sell_count`), Avg trade size (`avg_trade_size`).
    Below the panel add an empty `<div id="live-chart" style="height:350px"></div>` and a `<script>` block that
    polls the JSON endpoint. To embed the instrument id safely into JS, build the script using
    `iid_js = json.dumps(symbol)` and interpolate that (a JSON-encoded string is a valid, properly-escaped JS
    string literal — do NOT interpolate the raw symbol into the script, which would allow script-context injection).
    The script: define `layout = {height:350, template:"plotly_dark", xaxis:{type:"date"}, margin:{t:30,b:30}, legend:{orientation:"h"}}`;
    an `update(d)` function that maps `d.ts` to `new Date(t)` and calls
    `Plotly.react("live-chart", [ {mid #aaa}, {bid #26a69a}, {ask #ef5350}, {microprice #f0883e dot} ], layout)`
    with each trace `mode:"lines"`; a `poll()` that does
    `fetch("/data/coin/"+encodeURIComponent(<iid_js>)).then(r=>r.json()).then(update).catch(()=>{})`; then
    `poll(); setInterval(poll, 1000);`. Wrap it in an IIFE. Because the page builds the chart via Plotly.react on
    an empty div, the page must load the Plotly CDN script tag itself — include
    `<script src="https://cdn.plot.ly/plotly-2.35.2.min.js"></script>` once in the body (the JS-driven page does not
    call `fig.to_html`, so no other CDN tag is emitted; Pitfall 3). Render via
    `return _page(symbol, body, refresh_seconds=86400)` so the page does NOT use `<meta refresh>` (the JS handles
    live updates). Header: `f"<h1>{html.escape(symbol)} <small><a href='/chart/{html.escape(symbol)}'>historical chart</a></small></h1>"`.
    When `rolling is None`, still render the panel (values show as `&mdash;`) plus a short note like
    "Live data unavailable - running standalone." Keep helper functions under ~30 lines (extract the panel builder
    if needed).

    In `_Handler.do_GET`, change the `/coin/` branch to call the live view: keep the existing
    `list_instruments(CATALOG_PATH)` membership guard returning 404 for unknown symbols (directory listing, not a
    Parquet data read — acceptable), then set `html_doc = _render_live_coin_page(symbol, _ROLLING)`. Remove the
    `timeframe` parsing for this branch. Leave the old `_render_coin_page` function in place but unreferenced is NOT
    acceptable per DESIGN-03 — delete `_render_coin_page` and its now-unused helpers only if they are not used by any
    other route (verify `_render_footprint_chart`, `build_footprint`, `build_candles`, `coverage`, `likely_outages`,
    `top_of_book_series` are unused elsewhere before removing their imports; if any remain used by `/chart/`, keep them).

    Extend `troll/ml_signals/test_dashboard_metrics.py` with:
    - `test_chart_json_no_rolling`: `json.loads(_coin_chart_json("ETH-USD-PERP.DYDX", None))` equals
      `{"ts": [], "mid": [], "bid": [], "ask": [], "micro": []}`.
    - `test_chart_json_ts_conversion`: build a `_rolling(_snap(..., ts_event=1_700_000_000_000_000_000))`, call
      `_coin_chart_json("ETH-USD-PERP.DYDX", rolling)`, and assert the first `ts` value equals
      `1_700_000_000_000_000_000 // 1_000_000` (nanoseconds converted to milliseconds).
    Add `from ml_signals.dashboard import _coin_chart_json` to the test imports.
  </action>
  <verify>
    <automated>cd /home/mrqdt/code/nautilus_trader_fork && python -m pytest troll/ml_signals/test_dashboard_metrics.py -x -q && python -c "import sys; sys.path.insert(0,'troll'); import inspect; import ml_signals.dashboard as d; s=inspect.getsource(d._render_live_coin_page); assert 'ParquetDataCatalog' not in s and 'trade_ticks' not in s and 'compute_chart_series' not in s, 'live coin page must not read Parquet'; assert 'setInterval' in s and 'Plotly.react' in s and 'json.dumps(symbol)' in s; assert 'refresh_seconds=86400' in s; h=inspect.getsource(d._Handler.do_GET); assert '_render_live_coin_page' in h"</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.getsource(_render_live_coin_page)` contains none of: `ParquetDataCatalog`, `trade_ticks`, `compute_chart_series` (DASH-03/04 live path, MEM-01).
    - `_render_live_coin_page` source contains `setInterval`, `Plotly.react`, and `json.dumps(symbol)` (safe JS embedding) and renders the chart div `id="live-chart"`.
    - The page is rendered with `refresh_seconds=86400` (no `<meta refresh>` driving the chart).
    - `do_GET`'s `/coin/` branch calls `_render_live_coin_page(symbol, _ROLLING)`.
    - `pytest troll/ml_signals/test_dashboard_metrics.py -x` passes including `test_chart_json_no_rolling` and `test_chart_json_ts_conversion`.
  </acceptance_criteria>
  <done>/coin/{id} is a deque-backed live view with an indicator panel (DASH-03) and a 1s-updating 4-line chart (DASH-04); no Parquet on the live path (DASH-05/MEM-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard HTTP server | URL path `{id}` reflected into HTML and into a `<script>` JS string literal |
| collector asyncio thread → HTTP handler thread | `_second_rolling` deque read via `list(...)` snapshot (GIL-atomic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (XSS, script context) | `/coin/{id}` embedded JS | mitigate | embed `iid` via `json.dumps(symbol)` (proper JS string escaping), not raw f-string interpolation; symbol also gated by `list_instruments` 404 check |
| T-02-04 | Tampering (XSS, HTML context) | `/coin/{id}` headings | mitigate | `html.escape(symbol)` on all HTML-context interpolations |
| T-02-05 | Information Disclosure | `/data/coin/{id}` | accept | dashboard binds 127.0.0.1 only; serves public market data; no auth needed |
</threat_model>

<verification>
- `python -m pytest troll/ml_signals/test_dashboard_metrics.py -x -q` passes (plan 01 + plan 02 tests).
- RANKING_COLS keys are a superset of the DASH-01 columns; rows link to `/coin/{id}`.
- `_render_live_coin_page` and `_coin_chart_json` contain no Parquet/catalog calls.
- `/data/coin/{id}` returns `application/json`; `/coin/{id}` returns the live HTML page.
</verification>

<success_criteria>
- DASH-01: rankings table shows OFI_10, OBI_10, CVD, spread, microprice lean, volume delta, buy/sell count.
- DASH-02: clicking a coin row navigates to `/coin/{id}`.
- DASH-03: single-coin view shows a panel of all derivable indicators.
- DASH-04: single-coin view includes a 4-line (mid/bid/ask/microprice) 1s-updating chart over the rolling window.
- All live data comes from `_second_rolling` / `_LIVE` — no Parquet reads on the live path.
</success_criteria>

<output>
Create `.planning/phases/02-dashboard-upgrade/02-02-SUMMARY.md` when done.
</output>
